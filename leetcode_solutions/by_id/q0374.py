# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 374
标题: Guess Number Higher or Lower
难度: easy
链接: https://leetcode.cn/problems/guess-number-higher-or-lower/
题目类型: 二分查找、交互
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
374. 猜数字大小 - 我们正在玩猜数字游戏。猜数字游戏的规则如下： 我会从 1 到 n 随机选择一个数字。 请你猜选出的是哪个数字。（我选的数字在整个游戏中保持不变）。 如果你猜错了，我会告诉你，我选出的数字比你猜测的数字大了还是小了。 你可以通过调用一个预先定义好的接口 int guess(int num) 来获取猜测结果，返回值一共有三种可能的情况： * -1：你猜的数字比我选出的数字大 （即 num > pick）。 * 1：你猜的数字比我选出的数字小 （即 num < pick）。 * 0：你猜的数字与我选出的数字相等。（即 num == pick）。 返回我选出的数字。 示例 1： 输入：n = 10, pick = 6 输出：6 示例 2： 输入：n = 1, pick = 1 输出：1 示例 3： 输入：n = 2, pick = 1 输出：1 提示： * 1 <= n <= 231 - 1 * 1 <= pick <= n
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 二分查找

算法步骤:
1. 使用二分查找在[1, n]范围内查找
2. 根据guess函数的返回值调整搜索范围
3. 返回找到的数字

关键点:
- guess返回-1表示大了，1表示小了，0表示正确
- 使用标准二分查找模板
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(logn) - 二分查找
空间复杂度: O(1) - 只使用常数空间
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def guess_number(n: int) -> int:
    """
    函数式接口 - 猜数字大小
    
    实现思路:
    使用二分查找在[1, n]范围内查找目标数字。
    
    Args:
        n: 数字范围上限
        
    Returns:
        猜中的数字
        
    Example:
        >>> guess_number(10)  # 假设pick=6
        6
    """
    left, right = 1, n
    
    while left <= right:
        mid = left + (right - left) // 2
        result = guess(mid)  # 假设guess函数已定义
        
        if result == 0:
            return mid
        elif result == -1:  # mid > pick
            right = mid - 1
        else:  # result == 1, mid < pick
            left = mid + 1
    
    return left


# 辅助函数（实际LeetCode会提供）
def guess(num: int) -> int:
    """LeetCode提供的guess函数接口"""
    # 这里只是占位，实际由LeetCode提供
    pass


# 自动生成Solution类（无需手动编写）
Solution = create_solution(guess_number)
