# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2538
标题: Minimum Cost to Make Array Equal
难度: hard
链接: https://leetcode.cn/problems/minimum-cost-to-make-array-equal/
题目类型: 贪心、数组、二分查找、前缀和、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2448. 使数组相等的最小开销 - 给你两个下标从 0 开始的数组 nums 和 cost ，分别包含 n 个 正 整数。 你可以执行下面操作 任意 次： * 将 nums 中 任意 元素增加或者减小 1 。 对第 i 个元素执行一次操作的开销是 cost[i] 。 请你返回使 nums 中所有元素 相等 的 最少 总开销。 示例 1： 输入：nums = [1,3,5,2], cost = [2,3,1,14] 输出：8 解释：我们可以执行以下操作使所有元素变为 2 ： - 增加第 0 个元素 1 次，开销为 2 。 - 减小第 1 个元素 1 次，开销为 3 。 - 减小第 2 个元素 3 次，开销为 1 + 1 + 1 = 3 。 总开销为 2 + 3 + 3 = 8 。 这是最小开销。 示例 2： 输入：nums = [2,2,2,2,2], cost = [4,2,8,1,3] 输出：0 解释：数组中所有元素已经全部相等，不需要执行额外的操作。 提示： * n == nums.length == cost.length * 1 <= n <= 105 * 1 <= nums[i], cost[i] <= 106 * 测试用例确保输出不超过 253-1。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用中位数作为目标值来最小化总开销。

算法步骤:
1. 将 nums 和 cost 组合成一个列表，并按 nums 的值进行排序。
2. 计算总成本的一半，找到中位数的位置。
3. 遍历组合后的列表，计算将所有元素变为中位数的总开销。

关键点:
- 使用中位数可以最小化绝对差的总和。
- 通过排序和累加成本，可以在 O(n log n) 时间内找到中位数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log n)
空间复杂度: O(n)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def min_cost(nums: List[int], cost: List[int]) -> int:
    """
    返回使 nums 中所有元素相等的最少总开销。
    """
    # 将 nums 和 cost 组合成一个列表，并按 nums 的值进行排序
    pairs = sorted(zip(nums, cost))
    
    # 计算总成本的一半
    total_cost = sum(cost)
    half_cost = total_cost // 2
    
    # 找到中位数的位置
    current_cost = 0
    for num, c in pairs:
        current_cost += c
        if current_cost > half_cost:
            target = num
            break
    
    # 计算将所有元素变为中位数的总开销
    return sum(c * abs(num - target) for num, c in pairs)

Solution = create_solution(min_cost)