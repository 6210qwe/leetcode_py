# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1194
标题: Path In Zigzag Labelled Binary Tree
难度: medium
链接: https://leetcode.cn/problems/path-in-zigzag-labelled-binary-tree/
题目类型: 树、数学、二叉树
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1104. 二叉树寻路 - 在一棵无限的二叉树上，每个节点都有两个子节点，树中的节点 逐行 依次按 “之” 字形进行标记。 如下图所示，在奇数行（即，第一行、第三行、第五行……）中，按从左到右的顺序进行标记； 而偶数行（即，第二行、第四行、第六行……）中，按从右到左的顺序进行标记。 [https://assets.leetcode.cn/aliyun-lc-upload/uploads/2019/06/28/tree.png] 给你树上某一个节点的标号 label，请你返回从根节点到该标号为 label 节点的路径，该路径是由途经的节点标号所组成的。 示例 1： 输入：label = 14 输出：[1,3,4,14] 示例 2： 输入：label = 26 输出：[1,2,6,10,26] 提示： * 1 <= label <= 10^6
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 利用数学方法计算路径。首先找到给定 label 所在的层级，然后通过反向计算其父节点的方式逐步向上回溯到根节点。

算法步骤:
1. 计算 label 所在的层级 level。
2. 从 label 开始，逐步向上回溯到根节点，计算每一层的父节点。
3. 由于树是“之”字形标记的，需要根据层级的奇偶性调整父节点的计算方式。
4. 将路径反转，得到从根节点到 label 的路径。

关键点:
- 通过位运算和数学公式快速计算父节点。
- 根据层级的奇偶性调整父节点的计算方式。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(log n)，其中 n 是 label 的值。因为每次回溯都减少一层。
空间复杂度: O(log n)，存储路径所需的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def pathInZigZagTree(label: int) -> List[int]:
    """
    函数式接口 - 返回从根节点到 label 节点的路径
    """
    # 计算 label 所在的层级
    level = label.bit_length()
    
    # 初始化路径
    path = []
    
    # 从 label 开始向上回溯
    while label >= 1:
        path.append(label)
        level -= 1
        # 计算父节点
        if level % 2 == 0:
            label = (2 ** (level + 1) - 1 - label) // 2
        else:
            label = (2 ** level - 1 - label) // 2
    
    # 反转路径
    path.reverse()
    return path

Solution = create_solution(pathInZigZagTree)