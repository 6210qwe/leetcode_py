# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 680
标题: Valid Palindrome II
难度: easy
链接: https://leetcode.cn/problems/valid-palindrome-ii/
题目类型: 贪心、双指针、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
680. 验证回文串 II - 给你一个字符串 s，最多 可以从中删除一个字符。 请你判断 s 是否能成为回文字符串：如果能，返回 true ；否则，返回 false 。 示例 1： 输入：s = "aba" 输出：true 示例 2： 输入：s = "abca" 输出：true 解释：你可以删除字符 'c' 。 示例 3： 输入：s = "abc" 输出：false 提示： * 1 <= s.length <= 105 * s 由小写英文字母组成
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用双指针从两端向中间遍历字符串，遇到不匹配的字符时，尝试跳过左指针或右指针指向的字符，然后继续检查剩余部分是否为回文。

算法步骤:
1. 初始化两个指针 left 和 right 分别指向字符串的开头和结尾。
2. 使用 while 循环，当 left < right 时：
   - 如果 s[left] == s[right]，则移动两个指针。
   - 如果 s[left] != s[right]，则尝试跳过左指针或右指针指向的字符，检查剩余部分是否为回文。
3. 如果在任何情况下都能找到回文，则返回 True，否则返回 False。

关键点:
- 使用双指针从两端向中间遍历字符串。
- 在遇到不匹配字符时，尝试跳过左指针或右指针指向的字符。
- 检查剩余部分是否为回文。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串的长度。每个字符最多被访问两次。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def valid_palindrome(s: str) -> bool:
    """
    函数式接口 - 判断字符串 s 是否可以通过删除一个字符变成回文字符串
    """
    def is_palindrome_range(start: int, end: int) -> bool:
        """检查指定范围内的子字符串是否为回文"""
        while start < end:
            if s[start] != s[end]:
                return False
            start += 1
            end -= 1
        return True

    left, right = 0, len(s) - 1
    while left < right:
        if s[left] == s[right]:
            left += 1
            right -= 1
        else:
            # 尝试跳过左指针或右指针指向的字符
            return is_palindrome_range(left + 1, right) or is_palindrome_range(left, right - 1)
    return True


Solution = create_solution(valid_palindrome)