# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1335
标题: Maximum Candies Allocated to K Children
难度: medium
链接: https://leetcode.cn/problems/maximum-candies-allocated-to-k-children/
题目类型: 数组、二分查找
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2226. 每个小孩最多能分到多少糖果 - 给你一个 下标从 0 开始 的整数数组 candies 。数组中的每个元素表示大小为 candies[i] 的一堆糖果。你可以将每堆糖果分成任意数量的 子堆 ，但 无法 再将两堆合并到一起。 另给你一个整数 k 。你需要将这些糖果分配给 k 个小孩，使每个小孩分到 相同 数量的糖果。每个小孩可以拿走 至多一堆 糖果，有些糖果可能会不被分配。 返回每个小孩可以拿走的 最大糖果数目 。 示例 1： 输入：candies = [5,8,6], k = 3 输出：5 解释：可以将 candies[1] 分成大小分别为 5 和 3 的两堆，然后把 candies[2] 分成大小分别为 5 和 1 的两堆。现在就有五堆大小分别为 5、5、3、5 和 1 的糖果。可以把 3 堆大小为 5 的糖果分给 3 个小孩。可以证明无法让每个小孩得到超过 5 颗糖果。 示例 2： 输入：candies = [2,5], k = 11 输出：0 解释：总共有 11 个小孩，但只有 7 颗糖果，但如果要分配糖果的话，必须保证每个小孩至少能得到 1 颗糖果。因此，最后每个小孩都没有得到糖果，答案是 0 。 提示： * 1 <= candies.length <= 105 * 1 <= candies[i] <= 107 * 1 <= k <= 1012
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用二分查找来确定每个小孩可以分到的最大糖果数。

算法步骤:
1. 定义一个辅助函数 `can_distribute`，用于判断是否可以将糖果分配给 k 个小孩，使得每个小孩至少得到 `mid` 颗糖果。
2. 使用二分查找来确定最大糖果数：
   - 初始化左边界 `left` 为 0，右边界 `right` 为 `sum(candies) // k`。
   - 在每次迭代中，计算中间值 `mid`，并使用 `can_distribute` 函数检查是否可以分配。
   - 如果可以分配，则更新左边界 `left` 为 `mid + 1`，否则更新右边界 `right` 为 `mid - 1`。
3. 返回 `right` 作为结果。

关键点:
- 使用二分查找来高效地确定最大糖果数。
- 辅助函数 `can_distribute` 用于验证当前糖果数是否可行。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log(sum(candies) // k))，其中 n 是 candies 的长度。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def can_distribute(candies: List[int], mid: int, k: int) -> bool:
    count = 0
    for candy in candies:
        count += candy // mid
        if count >= k:
            return True
    return False

def solution_function_name(candies: List[int], k: int) -> int:
    """
    函数式接口 - 使用二分查找来确定每个小孩可以分到的最大糖果数
    """
    left, right = 0, sum(candies) // k
    while left < right:
        mid = (left + right + 1) // 2
        if can_distribute(candies, mid, k):
            left = mid
        else:
            right = mid - 1
    return right

Solution = create_solution(solution_function_name)