# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1000282
标题: 每日温度
难度: medium
链接: https://leetcode.cn/problems/iIQa4I/
题目类型: 栈、数组、单调栈
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
LCR 038. 每日温度 - 请根据每日 气温 列表 temperatures ，重新生成一个列表，要求其对应位置的输出为：要想观测到更高的气温，至少需要等待的天数。如果气温在这之后都不会升高，请在该位置用 0 来代替。 示例 1： 输入：temperatures = [73,74,75,71,69,72,76,73] 输出：[1,1,4,2,1,1,0,0] 示例 2： 输入：temperatures = [30,40,50,60] 输出：[1,1,1,0] 示例 3： 输入：temperatures = [30,60,90] 输出：[1,1,0] 提示： * 1 <= temperatures.length <= 105 * 30 <= temperatures[i] <= 100 注意：本题与主站 739 题相同： https://leetcode.cn/problems/daily-temperatures/ [https://leetcode.cn/problems/daily-temperatures/]
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用单调栈来解决这个问题。我们从后向前遍历温度列表，并维护一个单调递减栈。对于每个温度，我们找到第一个比它高的温度的位置。

算法步骤:
1. 初始化一个结果数组 res，长度与输入数组相同，初始值全部为 0。
2. 初始化一个空栈 stack，用于存储温度的索引。
3. 从后向前遍历温度列表：
   - 如果栈不为空且当前温度大于等于栈顶温度，则弹出栈顶元素。
   - 如果栈不为空且当前温度小于栈顶温度，则计算当前温度与栈顶温度之间的天数差，并将结果存入 res。
   - 将当前温度的索引压入栈中。
4. 返回结果数组 res。

关键点:
- 使用单调栈可以有效地找到下一个更高温度的位置。
- 从后向前遍历可以确保我们在处理当前温度时，栈中的温度都是未来可能的更高温度。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是温度列表的长度。每个元素最多只会被压入和弹出栈一次。
空间复杂度: O(n)，最坏情况下栈中会存储所有的温度索引。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def daily_temperatures(temperatures: List[int]) -> List[int]:
    """
    函数式接口 - 计算每日温度
    """
    n = len(temperatures)
    res = [0] * n
    stack = []

    for i in range(n - 1, -1, -1):
        while stack and temperatures[i] >= temperatures[stack[-1]]:
            stack.pop()
        if stack:
            res[i] = stack[-1] - i
        stack.append(i)

    return res


Solution = create_solution(daily_temperatures)