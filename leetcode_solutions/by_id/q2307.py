# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2307
标题: Replace Non-Coprime Numbers in Array
难度: hard
链接: https://leetcode.cn/problems/replace-non-coprime-numbers-in-array/
题目类型: 栈、数组、数学、数论
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2197. 替换数组中的非互质数 - 给你一个整数数组 nums 。请你对数组执行下述操作： 1. 从 nums 中找出 任意 两个 相邻 的 非互质 数。 2. 如果不存在这样的数，终止 这一过程。 3. 否则，删除这两个数，并 替换 为它们的 最小公倍数（Least Common Multiple，LCM）。 4. 只要还能找出两个相邻的非互质数就继续 重复 这一过程。 返回修改后得到的 最终 数组。可以证明的是，以 任意 顺序替换相邻的非互质数都可以得到相同的结果。 生成的测试用例可以保证最终数组中的值 小于或者等于 108 。 两个数字 x 和 y 满足 非互质数 的条件是：GCD(x, y) > 1 ，其中 GCD(x, y) 是 x 和 y 的 最大公约数 。 示例 1 ： 输入：nums = [6,4,3,2,7,6,2] 输出：[12,7,6] 解释： - (6, 4) 是一组非互质数，且 LCM(6, 4) = 12 。得到 nums = [12,3,2,7,6,2] 。 - (12, 3) 是一组非互质数，且 LCM(12, 3) = 12 。得到 nums = [12,2,7,6,2] 。 - (12, 2) 是一组非互质数，且 LCM(12, 2) = 12 。得到 nums = [12,7,6,2] 。 - (6, 2) 是一组非互质数，且 LCM(6, 2) = 6 。得到 nums = [12,7,6] 。 现在，nums 中不存在相邻的非互质数。 因此，修改后得到的最终数组是 [12,7,6] 。 注意，存在其他方法可以获得相同的最终数组。 示例 2 ： 输入：nums = [2,2,1,1,3,3,3] 输出：[2,1,1,3] 解释： - (3, 3) 是一组非互质数，且 LCM(3, 3) = 3 。得到 nums = [2,2,1,1,3,3] 。 - (3, 3) 是一组非互质数，且 LCM(3, 3) = 3 。得到 nums = [2,2,1,1,3] 。 - (2, 2) 是一组非互质数，且 LCM(2, 2) = 2 。得到 nums = [2,1,1,3] 。 现在，nums 中不存在相邻的非互质数。 因此，修改后得到的最终数组是 [2,1,1,3] 。 注意，存在其他方法可以获得相同的最终数组。 提示： * 1 <= nums.length <= 105 * 1 <= nums[i] <= 105 * 生成的测试用例可以保证最终数组中的值 小于或者等于 108 。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用栈来处理相邻的非互质数，逐步替换为它们的最小公倍数。

算法步骤:
1. 初始化一个空栈。
2. 遍历数组中的每个元素：
   - 如果栈不为空且当前元素与栈顶元素不互质，则计算它们的最小公倍数并将其压入栈中。
   - 否则，将当前元素压入栈中。
3. 最终栈中的元素即为结果数组。

关键点:
- 使用栈来处理相邻的非互质数，确保每次替换后的结果仍然保持在栈中。
- 通过计算最大公约数来判断两个数是否互质。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log(max(nums)))，其中 n 是数组长度，log(max(nums)) 是计算最大公约数的时间复杂度。
空间复杂度: O(n)，最坏情况下栈中存储了所有元素。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List
import math

def replace_non_coprimes(nums: List[int]) -> List[int]:
    """
    函数式接口 - 替换数组中的非互质数
    """
    stack = []
    for num in nums:
        while stack and math.gcd(stack[-1], num) > 1:
            gcd = math.gcd(stack[-1], num)
            lcm = (stack[-1] * num) // gcd
            stack.pop()
            num = lcm
        stack.append(num)
    return stack

Solution = create_solution(replace_non_coprimes)