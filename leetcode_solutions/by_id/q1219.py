# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1219
标题: Longest Well-Performing Interval
难度: medium
链接: https://leetcode.cn/problems/longest-well-performing-interval/
题目类型: 栈、数组、哈希表、前缀和、单调栈
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1124. 表现良好的最长时间段 - 给你一份工作时间表 hours，上面记录着某一位员工每天的工作小时数。 我们认为当员工一天中的工作小时数大于 8 小时的时候，那么这一天就是「劳累的一天」。 所谓「表现良好的时间段」，意味在这段时间内，「劳累的天数」是严格 大于「不劳累的天数」。 请你返回「表现良好时间段」的最大长度。 示例 1： 输入：hours = [9,9,6,0,6,6,9] 输出：3 解释：最长的表现良好时间段是 [9,9,6]。 示例 2： 输入：hours = [6,6,6] 输出：0 提示： * 1 <= hours.length <= 104 * 0 <= hours[i] <= 16
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用前缀和 + 哈希表来记录前缀和第一次出现的位置，从而快速找到满足条件的子数组。

算法步骤:
1. 将 `hours` 转换为二进制数组，其中 1 表示劳累的一天，0 表示不劳累的一天。
2. 计算前缀和，并使用哈希表记录每个前缀和第一次出现的位置。
3. 遍历前缀和数组，对于每个前缀和，查找哈希表中是否存在满足条件的前缀和（即当前前缀和减去 1 的位置），并更新最大长度。

关键点:
- 使用前缀和来表示累加的劳累天数与不劳累天数的差值。
- 使用哈希表来记录前缀和第一次出现的位置，以便快速查找满足条件的子数组。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(n)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def longest_well_performing_interval(hours: List[int]) -> int:
    """
    函数式接口 - 返回表现良好时间段的最大长度
    """
    n = len(hours)
    prefix_sum = 0
    max_length = 0
    first_occurrence = {0: -1}  # 前缀和为0的情况对应初始位置-1

    for i in range(n):
        if hours[i] > 8:
            prefix_sum += 1
        else:
            prefix_sum -= 1

        if prefix_sum not in first_occurrence:
            first_occurrence[prefix_sum] = i

        if (prefix_sum - 1) in first_occurrence:
            max_length = max(max_length, i - first_occurrence[prefix_sum - 1])

    return max_length


Solution = create_solution(longest_well_performing_interval)