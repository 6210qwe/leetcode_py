# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3416
标题: Sum of Digit Differences of All Pairs
难度: medium
链接: https://leetcode.cn/problems/sum-of-digit-differences-of-all-pairs/
题目类型: 数组、哈希表、数学、计数
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3153. 所有数对中数位差之和 - 你有一个数组 nums ，它只包含 正 整数，所有正整数的数位长度都 相同 。 两个整数的 数位差 指的是两个整数 相同 位置上不同数字的数目。 请你返回 nums 中 所有 整数对里，数位差之和。 示例 1： 输入：nums = [13,23,12] 输出：4 解释： 计算过程如下： - 13 和 23 的数位差为 1 。 - 13 和 12 的数位差为 1 。 - 23 和 12 的数位差为 2 。 所以所有整数数对的数位差之和为 1 + 1 + 2 = 4 。 示例 2： 输入：nums = [10,10,10,10] 输出：0 解释： 数组中所有整数都相同，所以所有整数数对的数位不同之和为 0 。 提示： * 2 <= nums.length <= 105 * 1 <= nums[i] < 109 * nums 中的整数都有相同的数位长度。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用哈希表统计每个数位上每个数字出现的次数，然后计算每对数位差的总和。

算法步骤:
1. 将每个数字转换为字符串形式，以便逐位处理。
2. 使用一个哈希表记录每个数位上每个数字出现的次数。
3. 遍历哈希表，计算每对数位差的总和。

关键点:
- 使用哈希表来统计每个数位上每个数字的出现次数。
- 通过遍历哈希表来计算每对数位差的总和。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n * d)，其中 n 是 nums 的长度，d 是每个数字的数位长度。
空间复杂度: O(d * 10)，因为每个数位上最多有 10 种不同的数字。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def sum_of_digit_differences(nums: List[int]) -> int:
    """
    函数式接口 - 计算所有整数对中数位差之和
    """
    # 将每个数字转换为字符串形式，以便逐位处理
    str_nums = [str(num) for num in nums]
    
    # 获取数位长度
    digit_length = len(str_nums[0])
    
    # 初始化哈希表，用于记录每个数位上每个数字出现的次数
    digit_count = [[0] * 10 for _ in range(digit_length)]
    
    # 统计每个数位上每个数字出现的次数
    for num in str_nums:
        for i, digit in enumerate(num):
            digit_count[i][int(digit)] += 1
    
    # 计算每对数位差的总和
    total_diff = 0
    for counts in digit_count:
        for count in counts:
            total_diff += count * (len(nums) - count)
    
    return total_diff // 2  # 每对数位差被计算了两次，所以需要除以 2


Solution = create_solution(sum_of_digit_differences)