# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2047
标题: Find a Peak Element II
难度: medium
链接: https://leetcode.cn/problems/find-a-peak-element-ii/
题目类型: 数组、二分查找、矩阵
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1901. 寻找峰值 II - 一个 2D 网格中的 峰值 是指那些 严格大于 其相邻格子(上、下、左、右)的元素。 给你一个 从 0 开始编号 的 m x n 矩阵 mat ，其中任意两个相邻格子的值都 不相同 。找出 任意一个 峰值 mat[i][j] 并 返回其位置 [i,j] 。 你可以假设整个矩阵周边环绕着一圈值为 -1 的格子。 要求必须写出时间复杂度为 O(m log(n)) 或 O(n log(m)) 的算法 示例 1: [https://assets.leetcode.com/uploads/2021/06/08/1.png] 输入: mat = [[1,4],[3,2]] 输出: [0,1] 解释: 3 和 4 都是峰值，所以[1,0]和[0,1]都是可接受的答案。 示例 2: [https://assets.leetcode.com/uploads/2021/06/07/3.png] 输入: mat = [[10,20,15],[21,30,14],[7,16,32]] 输出: [1,1] 解释: 30 和 32 都是峰值，所以[1,1]和[2,2]都是可接受的答案。 提示： * m == mat.length * n == mat[i].length * 1 <= m, n <= 500 * 1 <= mat[i][j] <= 105 * 任意两个相邻元素均不相等.
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用二分查找在列方向上找到峰值。

算法步骤:
1. 初始化左右边界 l 和 r。
2. 进行二分查找：
   - 计算中间列 mid。
   - 找到中间列的最大值及其行索引。
   - 比较最大值与其左边和右边的值：
     - 如果最大值大于左边和右边的值，则返回该位置。
     - 如果左边的值大于最大值，则将右边界移动到 mid - 1。
     - 否则，将左边界移动到 mid + 1。
3. 返回找到的峰值位置。

关键点:
- 利用二分查找在列方向上进行搜索，确保时间复杂度为 O(m log(n))。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m log(n))
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def findPeakGrid(mat: List[List[int]]) -> List[int]:
    """
    函数式接口 - 寻找峰值元素
    """
    m, n = len(mat), len(mat[0])
    l, r = 0, n - 1
    
    while l <= r:
        mid = (l + r) // 2
        max_row = 0
        for i in range(m):
            if mat[i][mid] > mat[max_row][mid]:
                max_row = i
        
        left_val = mat[max_row][mid - 1] if mid > 0 else float('-inf')
        right_val = mat[max_row][mid + 1] if mid < n - 1 else float('-inf')
        
        if mat[max_row][mid] > left_val and mat[max_row][mid] > right_val:
            return [max_row, mid]
        elif left_val > mat[max_row][mid]:
            r = mid - 1
        else:
            l = mid + 1

Solution = findPeakGrid