# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1884
标题: Minimum Changes To Make Alternating Binary String
难度: easy
链接: https://leetcode.cn/problems/minimum-changes-to-make-alternating-binary-string/
题目类型: 字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1758. 生成交替二进制字符串的最少操作数 - 给你一个仅由字符 '0' 和 '1' 组成的字符串 s 。一步操作中，你可以将任一 '0' 变成 '1' ，或者将 '1' 变成 '0' 。 交替字符串 定义为：如果字符串中不存在相邻两个字符相等的情况，那么该字符串就是交替字符串。例如，字符串 "010" 是交替字符串，而字符串 "0100" 不是。 返回使 s 变成 交替字符串 所需的 最少 操作数。 示例 1： 输入：s = "0100" 输出：1 解释：如果将最后一个字符变为 '1' ，s 就变成 "0101" ，即符合交替字符串定义。 示例 2： 输入：s = "10" 输出：0 解释：s 已经是交替字符串。 示例 3： 输入：s = "1111" 输出：2 解释：需要 2 步操作得到 "0101" 或 "1010" 。 提示： * 1 <= s.length <= 104 * s[i] 是 '0' 或 '1'
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 计算将字符串转换为两种可能的交替字符串所需的最小操作数。

算法步骤:
1. 初始化两个计数器 `count_start_with_0` 和 `count_start_with_1`，分别表示以 '0' 和 '1' 开头的交替字符串所需的操作数。
2. 遍历字符串，根据当前字符和期望字符（基于当前索引）更新计数器。
3. 返回两个计数器中的最小值。

关键点:
- 交替字符串有两种形式：以 '0' 开头和以 '1' 开头。
- 通过遍历一次字符串，可以同时计算出两种形式的交替字符串所需的操作数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串的长度，因为我们需要遍历整个字符串一次。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def min_operations_to_alternate(s: str) -> int:
    """
    函数式接口 - 计算将字符串转换为交替字符串所需的最少操作数
    """
    count_start_with_0 = 0
    count_start_with_1 = 0
    
    for i in range(len(s)):
        if i % 2 == 0:
            if s[i] != '0':
                count_start_with_0 += 1
            if s[i] != '1':
                count_start_with_1 += 1
        else:
            if s[i] != '1':
                count_start_with_0 += 1
            if s[i] != '0':
                count_start_with_1 += 1
    
    return min(count_start_with_0, count_start_with_1)


Solution = create_solution(min_operations_to_alternate)