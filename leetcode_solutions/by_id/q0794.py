# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 794
标题: Swim in Rising Water
难度: hard
链接: https://leetcode.cn/problems/swim-in-rising-water/
题目类型: 深度优先搜索、广度优先搜索、并查集、数组、二分查找、矩阵、堆（优先队列）
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
778. 水位上升的泳池中游泳 - 在一个 n x n 的整数矩阵 grid 中，每一个方格的值 grid[i][j] 表示位置 (i, j) 的平台高度。 当开始下雨时，在时间为 t 时，水池中的水位为 t 。你可以从一个平台游向四周相邻的任意一个平台，但是前提是此时水位必须同时淹没这两个平台。假定你可以瞬间移动无限距离，也就是默认在方格内部游动是不耗时的。当然，在你游泳的时候你必须待在坐标方格里面。 你从坐标方格的左上平台 (0，0) 出发。返回 你到达坐标方格的右下平台 (n-1, n-1) 所需的最少时间 。 示例 1: [https://assets.leetcode.com/uploads/2021/06/29/swim1-grid.jpg] 输入: grid = [[0,2],[1,3]] 输出: 3 解释: 时间为0时，你位于坐标方格的位置为 (0, 0)。 此时你不能游向任意方向，因为四个相邻方向平台的高度都大于当前时间为 0 时的水位。 等时间到达 3 时，你才可以游向平台 (1, 1). 因为此时的水位是 3，坐标方格中的平台没有比水位 3 更高的，所以你可以游向坐标方格中的任意位置 示例 2: [https://assets.leetcode.com/uploads/2021/06/29/swim2-grid-1.jpg] 输入: grid = [[0,1,2,3,4],[24,23,22,21,5],[12,13,14,15,16],[11,17,18,19,20],[10,9,8,7,6]] 输出: 16 解释: 最终的路线用加粗进行了标记。 我们必须等到时间为 16，此时才能保证平台 (0, 0) 和 (4, 4) 是连通的 提示: * n == grid.length * n == grid[i].length * 1 <= n <= 50 * 0 <= grid[i][j] < n2 * grid[i][j] 中每个值 均无重复
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用最小堆（优先队列）进行广度优先搜索（BFS），以找到从起点到终点的最短路径。

算法步骤:
1. 初始化一个最小堆，将起点 (0, 0) 和其高度加入堆。
2. 使用一个集合记录已经访问过的节点，避免重复访问。
3. 从堆中取出当前高度最小的节点，检查是否到达终点 (n-1, n-1)，如果是则返回当前高度。
4. 否则，将该节点的四个相邻节点（如果未访问过且在网格范围内）及其高度加入堆。
5. 重复上述过程，直到找到终点。

关键点:
- 使用最小堆确保每次处理的是当前高度最小的节点。
- 通过集合记录已访问节点，避免重复访问。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n^2 log n)，其中 n 是网格的边长。每个节点最多被处理一次，每次处理需要 O(log n) 的时间来维护堆。
空间复杂度: O(n^2)，用于存储访问过的节点和堆。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Tuple
import heapq

def swimInWater(grid: List[List[int]]) -> int:
    n = len(grid)
    visited = set()
    min_heap = [(grid[0][0], 0, 0)]  # (height, row, col)
    visited.add((0, 0))
    
    while min_heap:
        height, row, col = heapq.heappop(min_heap)
        if row == n - 1 and col == n - 1:
            return height
        
        for dr, dc in [(-1, 0), (1, 0), (0, -1), (0, 1)]:
            new_row, new_col = row + dr, col + dc
            if 0 <= new_row < n and 0 <= new_col < n and (new_row, new_col) not in visited:
                visited.add((new_row, new_col))
                new_height = max(height, grid[new_row][new_col])
                heapq.heappush(min_heap, (new_height, new_row, new_col))

Solution = create_solution(swimInWater)