# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2734
标题: Array Prototype Last
难度: easy
链接: https://leetcode.cn/problems/array-prototype-last/
题目类型: 其他
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2619. 数组原型对象的最后一个元素 - 请你编写一段代码实现一个数组方法，使任何数组都可以调用 array.last() 方法，这个方法将返回数组最后一个元素。如果数组中没有元素，则返回 -1 。 你可以假设数组是 JSON.parse 的输出结果。 示例 1 ： 输入：nums = [null, {}, 3] 输出：3 解释：调用 nums.last() 后返回最后一个元素： 3。 示例 2 ： 输入：nums = [] 输出：-1 解释：因为此数组没有元素，所以应该返回 -1。 提示： * arr 是一个有效的 JSON 数组 * 0 <= arr.length <= 1000
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 直接检查数组长度，如果为空则返回 -1，否则返回最后一个元素。

算法步骤:
1. 检查数组是否为空。
2. 如果数组为空，返回 -1。
3. 否则，返回数组的最后一个元素。

关键点:
- 使用数组的长度来判断数组是否为空。
- 直接访问数组的最后一个元素。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(1) - 只需要常数时间来检查数组长度并访问最后一个元素。
空间复杂度: O(1) - 不需要额外的空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(arr: List[Optional[object]]) -> Optional[object]:
    """
    函数式接口 - 实现数组的 last 方法
    """
    if not arr:
        return -1
    return arr[-1]


Solution = create_solution(solution_function_name)