# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3880
标题: Minimum Time to Visit All Houses
难度: medium
链接: https://leetcode.cn/problems/minimum-time-to-visit-all-houses/
题目类型: 数组、前缀和
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3540. 访问所有房屋的最短时间 - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 动态规划

算法步骤:
1. 定义 dp[i][j] 表示访问到第 i 个房子且最后一步从 j 房子来的最小时间。
2. 初始化 dp[0][0] = 0，其他 dp[0][j] = float('inf')。
3. 状态转移方程：dp[i][j] = min(dp[i-1][k] + cost[i-1][k][j]) for k in range(n)。
4. 最终结果是 min(dp[n-1][j]) for j in range(n)。

关键点:
- 使用动态规划来避免重复计算。
- 通过状态转移方程来更新 dp 数组。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n^3)，其中 n 是房子的数量。因为每次状态转移需要遍历 n 个房子。
空间复杂度: O(n^2)，用于存储 dp 数组。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def min_time_to_visit_all_houses(cost: List[List[int]]) -> int:
    """
    函数式接口 - 计算访问所有房屋的最短时间
    """
    n = len(cost)
    dp = [[float('inf')] * n for _ in range(n)]
    dp[0][0] = 0
    
    for i in range(1, n):
        for j in range(n):
            for k in range(n):
                if i > 0:
                    dp[i][j] = min(dp[i][j], dp[i-1][k] + cost[i-1][k][j])
    
    return min(dp[n-1])

Solution = create_solution(min_time_to_visit_all_houses)