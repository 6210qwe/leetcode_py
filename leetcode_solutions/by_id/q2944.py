# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2944
标题: Height of Special Binary Tree
难度: medium
链接: https://leetcode.cn/problems/height-of-special-binary-tree/
题目类型: 树、深度优先搜索、广度优先搜索、二叉树
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2773. 特殊二叉树的高度 - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用递归的深度优先搜索 (DFS) 来计算特殊二叉树的高度。

算法步骤:
1. 定义一个递归函数 `dfs`，用于计算当前节点的高度。
2. 如果当前节点为空，返回高度 0。
3. 递归计算左子树和右子树的高度。
4. 返回当前节点的高度，即左右子树高度的最大值加 1。

关键点:
- 递归终止条件是当前节点为空。
- 递归调用时，分别计算左子树和右子树的高度。
- 当前节点的高度是左右子树高度的最大值加 1。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是树中节点的数量。每个节点只会被访问一次。
空间复杂度: O(h)，其中 h 是树的高度。递归调用栈的深度最多为树的高度。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import Optional
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def height_of_special_binary_tree(root: Optional[TreeNode]) -> int:
    """
    计算特殊二叉树的高度
    """
    def dfs(node: Optional[TreeNode]) -> int:
        if not node:
            return 0
        left_height = dfs(node.left)
        right_height = dfs(node.right)
        return max(left_height, right_height) + 1

    return dfs(root)


Solution = create_solution(height_of_special_binary_tree)