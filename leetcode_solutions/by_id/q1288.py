# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1288
标题: Maximum Subarray Sum with One Deletion
难度: medium
链接: https://leetcode.cn/problems/maximum-subarray-sum-with-one-deletion/
题目类型: 数组、动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1186. 删除一次得到子数组最大和 - 给你一个整数数组，返回它的某个 非空 子数组（连续元素）在执行一次可选的删除操作后，所能得到的最大元素总和。换句话说，你可以从原数组中选出一个子数组，并可以决定要不要从中删除一个元素（只能删一次哦），（删除后）子数组中至少应当有一个元素，然后该子数组（剩下）的元素总和是所有子数组之中最大的。 注意，删除一个元素后，子数组 不能为空。 示例 1： 输入：arr = [1,-2,0,3] 输出：4 解释：我们可以选出 [1, -2, 0, 3]，然后删掉 -2，这样得到 [1, 0, 3]，和最大。 示例 2： 输入：arr = [1,-2,-2,3] 输出：3 解释：我们直接选出 [3]，这就是最大和。 示例 3： 输入：arr = [-1,-1,-1,-1] 输出：-1 解释：最后得到的子数组不能为空，所以我们不能选择 [-1] 并从中删去 -1 来得到 0。 我们应该直接选择 [-1]，或者选择 [-1, -1] 再从中删去一个 -1。 提示： * 1 <= arr.length <= 105 * -104 <= arr[i] <= 104
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划来解决这个问题。我们需要维护两个状态：
- `dp_keep` 表示不删除任何元素时的最大子数组和。
- `dp_delete` 表示删除一个元素后的最大子数组和。

算法步骤:
1. 初始化 `dp_keep` 和 `dp_delete` 为第一个元素的值。
2. 遍历数组，更新 `dp_keep` 和 `dp_delete`。
3. 最终结果是 `max(dp_keep, dp_delete)` 的最大值。

关键点:
- `dp_keep` 可以通过 `max(dp_keep + arr[i], arr[i])` 更新。
- `dp_delete` 可以通过 `max(dp_delete + arr[i], dp_keep)` 更新。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是数组的长度。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def max_subarray_sum_with_one_deletion(arr: List[int]) -> int:
    """
    函数式接口 - 返回数组的一个非空子数组在执行一次可选的删除操作后，所能得到的最大元素总和。
    """
    if not arr:
        return 0

    n = len(arr)
    dp_keep = arr[0]
    dp_delete = 0
    max_sum = arr[0]

    for i in range(1, n):
        # 更新 dp_keep 和 dp_delete
        dp_keep, dp_delete = max(dp_keep + arr[i], arr[i]), max(dp_delete + arr[i], dp_keep)
        # 更新最大和
        max_sum = max(max_sum, dp_keep, dp_delete)

    return max_sum


Solution = create_solution(max_subarray_sum_with_one_deletion)