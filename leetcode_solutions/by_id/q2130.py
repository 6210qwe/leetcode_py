# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2130
标题: Maximum Product of the Length of Two Palindromic Subsequences
难度: medium
链接: https://leetcode.cn/problems/maximum-product-of-the-length-of-two-palindromic-subsequences/
题目类型: 位运算、字符串、动态规划、回溯、状态压缩
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2002. 两个回文子序列长度的最大乘积 - 给你一个字符串 s ，请你找到 s 中两个 不相交回文子序列 ，使得它们长度的 乘积最大 。两个子序列在原字符串中如果没有任何相同下标的字符，则它们是 不相交 的。 请你返回两个回文子序列长度可以达到的 最大乘积 。 子序列 指的是从原字符串中删除若干个字符（可以一个也不删除）后，剩余字符不改变顺序而得到的结果。如果一个字符串从前往后读和从后往前读一模一样，那么这个字符串是一个 回文字符串 。 示例 1： example-1 [https://assets.leetcode.com/uploads/2021/08/24/two-palindromic-subsequences.png] 输入：s = "leetcodecom" 输出：9 解释：最优方案是选择 "ete" 作为第一个子序列，"cdc" 作为第二个子序列。 它们的乘积为 3 * 3 = 9 。 示例 2： 输入：s = "bb" 输出：1 解释：最优方案为选择 "b" （第一个字符）作为第一个子序列，"b" （第二个字符）作为第二个子序列。 它们的乘积为 1 * 1 = 1 。 示例 3： 输入：s = "accbcaxxcxx" 输出：25 解释：最优方案为选择 "accca" 作为第一个子序列，"xxcxx" 作为第二个子序列。 它们的乘积为 5 * 5 = 25 。 提示： * 2 <= s.length <= 12 * s 只含有小写英文字母。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用状态压缩和回溯来枚举所有可能的子序列，并检查它们是否为回文子序列。通过记录每个子序列的长度，计算最大乘积。

算法步骤:
1. 使用状态压缩表示子序列的选择情况。
2. 对于每个状态，检查其是否为回文子序列。
3. 记录每个回文子序列的长度。
4. 遍历所有状态组合，计算两个不相交回文子序列长度的最大乘积。

关键点:
- 使用状态压缩表示子序列的选择情况。
- 通过位运算快速判断两个子序列是否相交。
- 使用递归和回溯来生成所有可能的子序列。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(3^n) - 其中 n 是字符串的长度。因为每个字符有三种选择：属于第一个子序列、属于第二个子序列或不属于任何子序列。
空间复杂度: O(2^n) - 用于存储所有可能的子序列及其长度。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional

def is_palindrome(subseq: str) -> bool:
    return subseq == subseq[::-1]

def max_product(s: str) -> int:
    n = len(s)
    max_product = 0
    
    # 生成所有可能的子序列
    for mask in range(1, 1 << n):
        subseq1 = ""
        subseq2 = ""
        for i in range(n):
            if mask & (1 << i):
                subseq1 += s[i]
            else:
                subseq2 += s[i]
        
        if is_palindrome(subseq1) and is_palindrome(subseq2):
            max_product = max(max_product, len(subseq1) * len(subseq2))
    
    return max_product

Solution = create_solution(max_product)