# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3201
标题: Distribute Candies Among Children II
难度: medium
链接: https://leetcode.cn/problems/distribute-candies-among-children-ii/
题目类型: 数学、组合数学、枚举
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2929. 给小朋友们分糖果 II - 给你两个正整数 n 和 limit 。 请你将 n 颗糖果分给 3 位小朋友，确保没有任何小朋友得到超过 limit 颗糖果，请你返回满足此条件下的 总方案数 。 示例 1： 输入：n = 5, limit = 2 输出：3 解释：总共有 3 种方法分配 5 颗糖果，且每位小朋友的糖果数不超过 2 ：(1, 2, 2) ，(2, 1, 2) 和 (2, 2, 1) 。 示例 2： 输入：n = 3, limit = 3 输出：10 解释：总共有 10 种方法分配 3 颗糖果，且每位小朋友的糖果数不超过 3 ：(0, 0, 3) ，(0, 1, 2) ，(0, 2, 1) ，(0, 3, 0) ，(1, 0, 2) ，(1, 1, 1) ，(1, 2, 0) ，(2, 0, 1) ，(2, 1, 0) 和 (3, 0, 0) 。 提示： * 1 <= n <= 106 * 1 <= limit <= 106
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过枚举第一个和第二个小朋友的糖果数，计算第三个小朋友的糖果数是否在限制范围内。

算法步骤:
1. 初始化结果计数器 `count` 为 0。
2. 枚举第一个小朋友的糖果数 `a` 从 0 到 `min(n, limit)`。
3. 对于每个 `a`，枚举第二个小朋友的糖果数 `b` 从 0 到 `min(n - a, limit)`。
4. 计算第三个小朋友的糖果数 `c = n - a - b`，如果 `c` 在 0 到 `limit` 范围内，则增加计数器 `count`。

关键点:
- 通过枚举前两个小朋友的糖果数，可以确定第三个小朋友的糖果数。
- 确保每个小朋友的糖果数不超过 `limit`。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(min(n, limit)^2)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def distribute_candies(n: int, limit: int) -> int:
    """
    函数式接口 - 分配糖果
    """
    count = 0
    for a in range(min(n, limit) + 1):
        for b in range(min(n - a, limit) + 1):
            c = n - a - b
            if 0 <= c <= limit:
                count += 1
    return count


Solution = create_solution(distribute_candies)