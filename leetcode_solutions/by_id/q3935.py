# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3935
标题: Once Twice
难度: medium
链接: https://leetcode.cn/problems/once-twice/
题目类型: 位运算、数组
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3595. 一次或两次 - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用位运算来找到数组中只出现一次和出现两次的元素。

算法步骤:
1. 初始化两个变量 `one` 和 `two` 分别用于记录只出现一次和出现两次的元素。
2. 遍历数组中的每个元素，使用位运算更新 `one` 和 `two`。
3. 最后返回 `one` 和 `two`。

关键点:
- 使用位运算可以在线性时间内找到只出现一次和出现两次的元素。
- 通过 `one & ~two` 和 `~one & two` 来分别更新 `one` 和 `two`。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(nums: List[int]) -> List[int]:
    """
    函数式接口 - 找到数组中只出现一次和出现两次的元素
    """
    one, two = 0, 0
    for num in nums:
        one = (one ^ num) & ~two
        two = (two ^ num) & ~one
    return [one, two]


Solution = create_solution(solution_function_name)