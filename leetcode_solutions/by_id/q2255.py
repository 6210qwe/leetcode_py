# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2255
标题: Minimum Swaps to Group All 1's Together II
难度: medium
链接: https://leetcode.cn/problems/minimum-swaps-to-group-all-1s-together-ii/
题目类型: 数组、滑动窗口
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2134. 最少交换次数来组合所有的 1 II - 交换 定义为选中一个数组中的两个 互不相同 的位置并交换二者的值。 环形 数组是一个数组，可以认为 第一个 元素和 最后一个 元素 相邻 。 给你一个 二进制环形 数组 nums ，返回在 任意位置 将数组中的所有 1 聚集在一起需要的最少交换次数。 示例 1： 输入：nums = [0,1,0,1,1,0,0] 输出：1 解释：这里列出一些能够将所有 1 聚集在一起的方案： [0,0,1,1,1,0,0] 交换 1 次。 [0,1,1,1,0,0,0] 交换 1 次。 [1,1,0,0,0,0,1] 交换 2 次（利用数组的环形特性）。 无法在交换 0 次的情况下将数组中的所有 1 聚集在一起。 因此，需要的最少交换次数为 1 。 示例 2： 输入：nums = [0,1,1,1,0,0,1,1,0] 输出：2 解释：这里列出一些能够将所有 1 聚集在一起的方案： [1,1,1,0,0,0,0,1,1] 交换 2 次（利用数组的环形特性）。 [1,1,1,1,1,0,0,0,0] 交换 2 次。 无法在交换 0 次或 1 次的情况下将数组中的所有 1 聚集在一起。 因此，需要的最少交换次数为 2 。 示例 3： 输入：nums = [1,1,0,0,1] 输出：0 解释：得益于数组的环形特性，所有的 1 已经聚集在一起。 因此，需要的最少交换次数为 0 。 提示： * 1 <= nums.length <= 105 * nums[i] 为 0 或者 1
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用滑动窗口技术来找到包含所有 1 的最小窗口，并计算所需的最少交换次数。

算法步骤:
1. 计算数组中 1 的总数 `k`。
2. 初始化一个长度为 `k` 的滑动窗口，计算窗口内 0 的数量 `zero_count`。
3. 移动滑动窗口，更新 `zero_count`，并记录最小的 `zero_count`。
4. 返回最小的 `zero_count` 作为最少交换次数。

关键点:
- 使用滑动窗口技术来高效地找到包含所有 1 的最小窗口。
- 利用数组的环形特性，通过扩展数组来处理边界情况。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def min_swaps_to_group_ones(nums: List[int]) -> int:
    """
    返回在任意位置将数组中的所有 1 聚集在一起需要的最少交换次数。
    """
    n = len(nums)
    k = sum(nums)  # 计算数组中 1 的总数
    if k == 0 or k == n:
        return 0  # 如果没有 1 或者全是 1，不需要交换

    # 扩展数组以处理环形特性
    extended_nums = nums + nums
    zero_count = extended_nums[:k].count(0)  # 初始窗口内 0 的数量
    min_swaps = zero_count  # 初始化最少交换次数

    for i in range(k, n + k):
        # 更新窗口内的 0 的数量
        if extended_nums[i] == 0:
            zero_count += 1
        if extended_nums[i - k] == 0:
            zero_count -= 1
        min_swaps = min(min_swaps, zero_count)

    return min_swaps

Solution = create_solution(min_swaps_to_group_ones)