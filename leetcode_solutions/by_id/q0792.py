# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 792
标题: Binary Search
难度: easy
链接: https://leetcode.cn/problems/binary-search/
题目类型: 数组、二分查找
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
704. 二分查找 - 给定一个 n 个元素有序的（升序）整型数组 nums 和一个目标值 target ，写一个函数搜索 nums 中的 target，如果 target 存在返回下标，否则返回 -1。 你必须编写一个具有 O(log n) 时间复杂度的算法。 示例 1: 输入: nums = [-1,0,3,5,9,12], target = 9 输出: 4 解释: 9 出现在 nums 中并且下标为 4 示例 2: 输入: nums = [-1,0,3,5,9,12], target = 2 输出: -1 解释: 2 不存在 nums 中因此返回 -1 提示： 1. 你可以假设 nums 中的所有元素是不重复的。 2. n 将在 [1, 10000]之间。 3. nums 的每个元素都将在 [-9999, 9999]之间。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用二分查找算法来查找目标值。

算法步骤:
1. 初始化左右指针 left 和 right，分别指向数组的起始位置和结束位置。
2. 进入循环，条件是 left 不大于 right。
3. 计算中间位置 mid。
4. 如果 nums[mid] 等于 target，则返回 mid。
5. 如果 nums[mid] 小于 target，则将 left 移动到 mid + 1。
6. 如果 nums[mid] 大于 target，则将 right 移动到 mid - 1。
7. 如果循环结束仍未找到目标值，返回 -1。

关键点:
- 通过不断缩小搜索范围，可以在 O(log n) 时间内找到目标值。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(log n)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def binary_search(nums: List[int], target: int) -> int:
    """
    二分查找 - 在有序数组中查找目标值的索引。
    :param nums: 有序数组
    :param target: 目标值
    :return: 目标值的索引，如果不存在则返回 -1
    """
    left, right = 0, len(nums) - 1
    
    while left <= right:
        mid = (left + right) // 2
        if nums[mid] == target:
            return mid
        elif nums[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    
    return -1

Solution = create_solution(binary_search)