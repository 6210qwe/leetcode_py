# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1571
标题: Allocate Mailboxes
难度: hard
链接: https://leetcode.cn/problems/allocate-mailboxes/
题目类型: 数组、数学、动态规划、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1478. 安排邮筒 - 给你一个房屋数组houses 和一个整数 k ，其中 houses[i] 是第 i 栋房子在一条街上的位置，现需要在这条街上安排 k 个邮筒。 请你返回每栋房子与离它最近的邮筒之间的距离的 最小 总和。 答案保证在 32 位有符号整数范围以内。 示例 1： [https://assets.leetcode.cn/aliyun-lc-upload/uploads/2020/06/13/sample_11_1816.png] 输入：houses = [1,4,8,10,20], k = 3 输出：5 解释：将邮筒分别安放在位置 3， 9 和 20 处。 每个房子到最近邮筒的距离和为 |3-1| + |4-3| + |9-8| + |10-9| + |20-20| = 5 。 示例 2： [https://assets.leetcode.cn/aliyun-lc-upload/uploads/2020/06/13/sample_2_1816.png] 输入：houses = [2,3,5,12,18], k = 2 输出：9 解释：将邮筒分别安放在位置 3 和 14 处。 每个房子到最近邮筒距离和为 |2-3| + |3-3| + |5-3| + |12-14| + |18-14| = 9 。 示例 3： 输入：houses = [7,4,6,1], k = 1 输出：8 示例 4： 输入：houses = [3,6,14,10], k = 4 输出：0 提示： * n == houses.length * 1 <= n <= 100 * 1 <= houses[i] <= 10^4 * 1 <= k <= n * 数组 houses 中的整数互不相同。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 动态规划

算法步骤:
1. 计算所有房子对之间的最小距离和。
2. 使用动态规划来确定放置 k 个邮筒的最优位置。

关键点:
- 使用预处理来计算所有房子对之间的最小距离和。
- 动态规划的状态转移方程。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n^3)
空间复杂度: O(n^2)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def min_distance_sum(houses: List[int]) -> int:
    n = len(houses)
    cost = [[0] * n for _ in range(n)]
    for i in range(n):
        for j in range(i, n):
            mid = (i + j) // 2
            cost[i][j] = sum(abs(houses[k] - houses[mid]) for k in range(i, j + 1))
    return cost

def min_distance_to_mailboxes(houses: List[int], k: int) -> int:
    n = len(houses)
    houses.sort()
    cost = min_distance_sum(houses)
    
    dp = [[float('inf')] * (k + 1) for _ in range(n + 1)]
    dp[0][0] = 0
    
    for m in range(1, n + 1):
        for j in range(1, k + 1):
            for i in range(m):
                dp[m][j] = min(dp[m][j], dp[i][j - 1] + cost[i][m - 1])
    
    return dp[n][k]

Solution = create_solution(min_distance_to_mailboxes)