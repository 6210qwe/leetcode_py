# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2232
标题: Adding Spaces to a String
难度: medium
链接: https://leetcode.cn/problems/adding-spaces-to-a-string/
题目类型: 数组、双指针、字符串、模拟
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2109. 向字符串添加空格 - 给你一个下标从 0 开始的字符串 s ，以及一个下标从 0 开始的整数数组 spaces 。 数组 spaces 描述原字符串中需要添加空格的下标。每个空格都应该插入到给定索引处的字符值 之前 。 * 例如，s = "EnjoyYourCoffee" 且 spaces = [5, 9] ，那么我们需要在 'Y' 和 'C' 之前添加空格，这两个字符分别位于下标 5 和下标 9 。因此，最终得到 "Enjoy Your Coffee" 。 请你添加空格，并返回修改后的字符串。 示例 1： 输入：s = "LeetcodeHelpsMeLearn", spaces = [8,13,15] 输出："Leetcode Helps Me Learn" 解释： 下标 8、13 和 15 对应 "LeetcodeHelpsMeLearn" 中加粗斜体字符。 接着在这些字符前添加空格。 示例 2： 输入：s = "icodeinpython", spaces = [1,5,7,9] 输出："i code in py thon" 解释： 下标 1、5、7 和 9 对应 "icodeinpython" 中加粗斜体字符。 接着在这些字符前添加空格。 示例 3： 输入：s = "spacing", spaces = [0,1,2,3,4,5,6] 输出：" s p a c i n g" 解释： 字符串的第一个字符前可以添加空格。 提示： * 1 <= s.length <= 3 * 105 * s 仅由大小写英文字母组成 * 1 <= spaces.length <= 3 * 105 * 0 <= spaces[i] <= s.length - 1 * spaces 中的所有值 严格递增
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用双指针方法，一边遍历字符串一边在指定位置插入空格。

算法步骤:
1. 初始化一个空的结果字符串 `result`。
2. 使用两个指针 `i` 和 `j`，其中 `i` 用于遍历字符串 `s`，`j` 用于遍历 `spaces` 数组。
3. 遍历字符串 `s`：
   - 如果 `i` 等于 `spaces[j]`，则在 `result` 中添加一个空格，并将 `j` 增加 1。
   - 将当前字符 `s[i]` 添加到 `result` 中。
   - 将 `i` 增加 1。
4. 返回结果字符串 `result`。

关键点:
- 使用双指针方法可以在 O(n) 时间复杂度内完成任务。
- 在遍历过程中动态插入空格，避免多次字符串拼接操作。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串 s 的长度。
空间复杂度: O(n)，结果字符串 `result` 的空间开销。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def add_spaces(s: str, spaces: List[int]) -> str:
    """
    在字符串 s 的指定位置插入空格，并返回修改后的字符串。
    """
    result = []
    j = 0
    for i in range(len(s)):
        if j < len(spaces) and i == spaces[j]:
            result.append(' ')
            j += 1
        result.append(s[i])
    return ''.join(result)

Solution = create_solution(add_spaces)