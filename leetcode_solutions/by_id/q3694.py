# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3694
标题: Minimum Total Operations
难度: easy
链接: https://leetcode.cn/problems/minimum-total-operations/
题目类型: 数组
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3353. 最小总操作数 - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用贪心算法，每次选择当前最小的操作数进行合并。

算法步骤:
1. 初始化一个计数器数组 `count`，用于记录每个数字的出现次数。
2. 遍历输入数组 `nums`，更新 `count` 数组。
3. 计算需要的操作数，通过不断合并最小的两个非零元素，直到只剩下一个元素。

关键点:
- 使用贪心策略，每次选择当前最小的两个非零元素进行合并。
- 通过计数器数组来优化查找和合并过程。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n + k log k)，其中 n 是输入数组的长度，k 是不同数字的数量。
空间复杂度: O(k)，其中 k 是不同数字的数量。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List
from collections import Counter

def solution_function_name(nums: List[int]) -> int:
    """
    函数式接口 - 实现最优解法
    """
    # 统计每个数字的出现次数
    count = Counter(nums)
    
    # 将计数器中的值转换为列表
    counts = list(count.values())
    
    # 计算需要的操作数
    operations = 0
    while len(counts) > 1:
        # 按升序排序
        counts.sort()
        # 合并最小的两个非零元素
        min1 = counts.pop(0)
        min2 = counts.pop(0)
        operations += min1
        if min1 + min2 > 0:
            counts.append(min1 + min2)
    
    return operations

Solution = create_solution(solution_function_name)