# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1675
标题: Magnetic Force Between Two Balls
难度: medium
链接: https://leetcode.cn/problems/magnetic-force-between-two-balls/
题目类型: 数组、二分查找、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1552. 两球之间的磁力 - 在代号为 C-137 的地球上，Rick 发现如果他将两个球放在他新发明的篮子里，它们之间会形成特殊形式的磁力。Rick 有 n 个空的篮子，第 i 个篮子的位置在 position[i] ，Morty 想把 m 个球放到这些篮子里，使得任意两球间 最小磁力 最大。 已知两个球如果分别位于 x 和 y ，那么它们之间的磁力为 |x - y| 。 给你一个整数数组 position 和一个整数 m ，请你返回最大化的最小磁力。 示例 1： [https://assets.leetcode.cn/aliyun-lc-upload/uploads/2020/08/16/q3v1.jpg] 输入：position = [1,2,3,4,7], m = 3 输出：3 解释：将 3 个球分别放入位于 1，4 和 7 的三个篮子，两球间的磁力分别为 [3, 3, 6]。最小磁力为 3 。我们没办法让最小磁力大于 3 。 示例 2： 输入：position = [5,4,3,2,1,1000000000], m = 2 输出：999999999 解释：我们使用位于 1 和 1000000000 的篮子时最小磁力最大。 提示： * n == position.length * 2 <= n <= 10^5 * 1 <= position[i] <= 10^9 * 所有 position 中的整数 互不相同 。 * 2 <= m <= position.length
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用二分查找来确定最大化的最小磁力。

算法步骤:
1. 对位置数组进行排序。
2. 定义一个辅助函数 `can_place`，用于判断给定的磁力值是否可以放置 m 个球。
3. 使用二分查找来确定最大化的最小磁力：
   - 初始化左右边界 `left` 和 `right`，分别表示最小和最大的可能磁力。
   - 在每次迭代中，计算中间值 `mid`，并使用 `can_place` 函数判断是否可以放置 m 个球。
   - 如果可以放置，则更新左边界 `left`；否则，更新右边界 `right`。
4. 返回最终的 `left` 值作为结果。

关键点:
- 通过二分查找来高效地确定最大化的最小磁力。
- 辅助函数 `can_place` 用于验证当前磁力值是否可行。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log(max(position) - min(position)))
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def can_place(position: List[int], m: int, force: int) -> bool:
    count = 1
    last_position = position[0]
    for pos in position[1:]:
        if pos - last_position >= force:
            count += 1
            last_position = pos
            if count == m:
                return True
    return False

def solution_function_name(position: List[int], m: int) -> int:
    """
    函数式接口 - 实现最大化最小磁力
    """
    position.sort()
    
    left, right = 1, position[-1] - position[0]
    
    while left < right:
        mid = (left + right + 1) // 2
        if can_place(position, m, mid):
            left = mid
        else:
            right = mid - 1
    
    return left

Solution = create_solution(solution_function_name)