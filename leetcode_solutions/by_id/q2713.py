# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2713
标题: Find the Divisibility Array of a String
难度: medium
链接: https://leetcode.cn/problems/find-the-divisibility-array-of-a-string/
题目类型: 数组、数学、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2575. 找出字符串的可整除数组 - 给你一个下标从 0 开始的字符串 word ，长度为 n ，由从 0 到 9 的数字组成。另给你一个正整数 m 。 word 的 可整除数组 div 是一个长度为 n 的整数数组，并满足： * 如果 word[0,...,i] 所表示的 数值 能被 m 整除，div[i] = 1 * 否则，div[i] = 0 返回 word 的可整除数组。 示例 1： 输入：word = "998244353", m = 3 输出：[1,1,0,0,0,1,1,0,0] 解释：仅有 4 个前缀可以被 3 整除："9"、"99"、"998244" 和 "9982443" 。 示例 2： 输入：word = "1010", m = 10 输出：[0,1,0,1] 解释：仅有 2 个前缀可以被 10 整除："10" 和 "1010" 。 提示： * 1 <= n <= 105 * word.length == n * word 由数字 0 到 9 组成 * 1 <= m <= 109
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用模运算来避免大数溢出

算法步骤:
1. 初始化当前数值 current_value 为 0 和结果数组 result 为空列表。
2. 遍历字符串 word 的每个字符：
   - 将当前字符转换为整数并更新 current_value。
   - 更新 current_value 为 (current_value * 10 + int(char)) % m。
   - 如果 current_value 为 0，则在 result 中添加 1，否则添加 0。
3. 返回结果数组 result。

关键点:
- 使用模运算来避免大数溢出。
- 通过不断更新 current_value 来保持其在合理范围内。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串 word 的长度。
空间复杂度: O(1)，除了返回的结果数组外，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def find_divisibility_array(word: str, m: int) -> List[int]:
    """
    函数式接口 - 找出字符串的可整除数组
    """
    current_value = 0
    result = []
    
    for char in word:
        current_value = (current_value * 10 + int(char)) % m
        if current_value == 0:
            result.append(1)
        else:
            result.append(0)
    
    return result

Solution = create_solution(find_divisibility_array)