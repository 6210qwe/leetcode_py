# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2397
标题: Count Number of Ways to Place Houses
难度: medium
链接: https://leetcode.cn/problems/count-number-of-ways-to-place-houses/
题目类型: 动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2320. 统计放置房子的方式数 - 一条街道上共有 n * 2 个 地块 ，街道的两侧各有 n 个地块。每一边的地块都按从 1 到 n 编号。每个地块上都可以放置一所房子。 现要求街道同一侧不能存在两所房子相邻的情况，请你计算并返回放置房屋的方式数目。由于答案可能很大，需要对 109 + 7 取余后再返回。 注意，如果一所房子放置在这条街某一侧上的第 i 个地块，不影响在另一侧的第 i 个地块放置房子。 示例 1： 输入：n = 1 输出：4 解释： 可能的放置方式： 1. 所有地块都不放置房子。 2. 一所房子放在街道的某一侧。 3. 一所房子放在街道的另一侧。 4. 放置两所房子，街道两侧各放置一所。 示例 2： [https://assets.leetcode.com/uploads/2022/05/12/arrangements.png] 输入：n = 2 输出：9 解释：如上图所示，共有 9 种可能的放置方式。 提示： * 1 <= n <= 104
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划来解决这个问题。我们定义 dp[i][0] 表示前 i 个地块不放置房子的方法数，dp[i][1] 表示前 i 个地块放置房子的方法数。然后通过状态转移方程来更新这些值。

算法步骤:
1. 初始化 dp 数组，dp[0][0] = 1, dp[0][1] = 1。
2. 通过状态转移方程更新 dp 数组：
   - dp[i][0] = (dp[i-1][0] + dp[i-1][1]) % MOD
   - dp[i][1] = dp[i-1][0] % MOD
3. 最终结果为 (dp[n][0] + dp[n][1]) ** 2 % MOD。

关键点:
- 使用动态规划来避免重复计算。
- 通过对称性，可以将两边的地块分开考虑，最后将结果相乘。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1) 通过优化空间复杂度，只使用常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def countHousePlacements(n: int) -> int:
    """
    函数式接口 - 计算放置房屋的方式数目
    """
    MOD = 10**9 + 7
    
    # 初始化 dp 数组
    dp = [[0, 0] for _ in range(n + 1)]
    dp[0][0] = 1
    dp[0][1] = 1
    
    # 通过状态转移方程更新 dp 数组
    for i in range(1, n + 1):
        dp[i][0] = (dp[i - 1][0] + dp[i - 1][1]) % MOD
        dp[i][1] = dp[i - 1][0] % MOD
    
    # 最终结果
    result = (dp[n][0] + dp[n][1]) ** 2 % MOD
    return result


Solution = create_solution(countHousePlacements)