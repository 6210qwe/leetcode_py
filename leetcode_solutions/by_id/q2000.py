# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2000
标题: Minimum Speed to Arrive on Time
难度: medium
链接: https://leetcode.cn/problems/minimum-speed-to-arrive-on-time/
题目类型: 数组、二分查找
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1870. 准时到达的列车最小时速 - 给你一个浮点数 hour ，表示你到达办公室可用的总通勤时间。要到达办公室，你必须按给定次序乘坐 n 趟列车。另给你一个长度为 n 的整数数组 dist ，其中 dist[i] 表示第 i 趟列车的行驶距离（单位是千米）。 每趟列车均只能在整点发车，所以你可能需要在两趟列车之间等待一段时间。 * 例如，第 1 趟列车需要 1.5 小时，那你必须再等待 0.5 小时，搭乘在第 2 小时发车的第 2 趟列车。 返回能满足你在时限前到达办公室所要求全部列车的 最小正整数 时速（单位：千米每小时），如果无法准时到达，则返回 -1 。 生成的测试用例保证答案不超过 107 ，且 hour 的 小数点后最多存在两位数字 。 示例 1： 输入：dist = [1,3,2], hour = 6 输出：1 解释：速度为 1 时： - 第 1 趟列车运行需要 1/1 = 1 小时。 - 由于是在整数时间到达，可以立即换乘在第 1 小时发车的列车。第 2 趟列车运行需要 3/1 = 3 小时。 - 由于是在整数时间到达，可以立即换乘在第 4 小时发车的列车。第 3 趟列车运行需要 2/1 = 2 小时。 - 你将会恰好在第 6 小时到达。 示例 2： 输入：dist = [1,3,2], hour = 2.7 输出：3 解释：速度为 3 时： - 第 1 趟列车运行需要 1/3 = 0.33333 小时。 - 由于不是在整数时间到达，故需要等待至第 1 小时才能搭乘列车。第 2 趟列车运行需要 3/3 = 1 小时。 - 由于是在整数时间到达，可以立即换乘在第 2 小时发车的列车。第 3 趟列车运行需要 2/3 = 0.66667 小时。 - 你将会在第 2.66667 小时到达。 示例 3： 输入：dist = [1,3,2], hour = 1.9 输出：-1 解释：不可能准时到达，因为第 3 趟列车最早是在第 2 小时发车。 提示： * n == dist.length * 1 <= n <= 105 * 1 <= dist[i] <= 105 * 1 <= hour <= 109 * hours 中，小数点后最多存在两位数字
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用二分查找来确定最小的时速。

算法步骤:
1. 定义一个辅助函数 `can_arrive_on_time`，用于判断在给定时速下是否能在规定时间内到达。
2. 初始化二分查找的左右边界，左边界为 1，右边界为 10^7。
3. 在二分查找过程中，计算中间值 `mid`，并使用 `can_arrive_on_time` 判断是否可以在 `mid` 时速下准时到达。
4. 如果可以准时到达，则更新右边界；否则，更新左边界。
5. 最终返回左边界作为最小的时速。

关键点:
- 使用二分查找来优化搜索过程。
- 辅助函数 `can_arrive_on_time` 用于判断是否可以在给定时速下准时到达。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log(max_dist))，其中 n 是 dist 的长度，max_dist 是 dist 中的最大值。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def can_arrive_on_time(dist: List[int], speed: int, hour: float) -> bool:
    total_time = 0.0
    for i in range(len(dist) - 1):
        total_time += (dist[i] + speed - 1) // speed  # 向上取整
    total_time += dist[-1] / speed
    return total_time <= hour


def solution_function_name(dist: List[int], hour: float) -> int:
    """
    函数式接口 - 计算最小的时速以确保在规定时间内到达
    """
    left, right = 1, 10**7
    while left < right:
        mid = (left + right) // 2
        if can_arrive_on_time(dist, mid, hour):
            right = mid
        else:
            left = mid + 1
    return left if can_arrive_on_time(dist, left, hour) else -1


Solution = create_solution(solution_function_name)