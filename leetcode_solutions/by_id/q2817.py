# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2817
标题: Minimum Cost to Make All Characters Equal
难度: medium
链接: https://leetcode.cn/problems/minimum-cost-to-make-all-characters-equal/
题目类型: 贪心、字符串、动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2712. 使所有字符相等的最小成本 - 给你一个下标从 0 开始、长度为 n 的二进制字符串 s ，你可以对其执行两种操作： * 选中一个下标 i 并且反转从下标 0 到下标 i（包括下标 0 和下标 i ）的所有字符，成本为 i + 1 。 * 选中一个下标 i 并且反转从下标 i 到下标 n - 1（包括下标 i 和下标 n - 1 ）的所有字符，成本为 n - i 。 返回使字符串内所有字符 相等 需要的 最小成本 。 反转 字符意味着：如果原来的值是 '0' ，则反转后值变为 '1' ，反之亦然。 示例 1： 输入：s = "0011" 输出：2 解释：执行第二种操作，选中下标 i = 2 ，可以得到 s = "0000" ，成本为 2 。可以证明 2 是使所有字符相等的最小成本。 示例 2： 输入：s = "010101" 输出：9 解释：执行第一种操作，选中下标 i = 2 ，可以得到 s = "101101" ，成本为 3 。 执行第一种操作，选中下标 i = 1 ，可以得到 s = "011101" ，成本为 2 。 执行第一种操作，选中下标 i = 0 ，可以得到 s = "111101" ，成本为 1 。 执行第二种操作，选中下标 i = 4 ，可以得到 s = "111110" ，成本为 2 。 执行第二种操作，选中下标 i = 5 ，可以得到 s = "111111" ，成本为 1 。 使所有字符相等的总成本等于 9 。可以证明 9 是使所有字符相等的最小成本。 提示： * 1 <= s.length == n <= 105 * s[i] 为 '0' 或 '1'
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用贪心算法，通过前缀和计算每个位置的翻转成本，并选择最小的成本。

算法步骤:
1. 计算前缀和数组，表示从开头到当前索引的翻转成本。
2. 计算后缀和数组，表示从当前索引到结尾的翻转成本。
3. 遍历字符串，对于每个位置，计算将所有字符翻转为 '0' 或 '1' 的最小成本。
4. 返回最小成本。

关键点:
- 使用前缀和和后缀和来优化计算成本。
- 通过贪心选择最小的成本。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(n)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def minimum_cost_to_make_all_characters_equal(s: str) -> int:
    """
    函数式接口 - 计算使所有字符相等的最小成本
    """
    n = len(s)
    prefix_sum = [0] * (n + 1)
    suffix_sum = [0] * (n + 1)

    # 计算前缀和
    for i in range(1, n + 1):
        prefix_sum[i] = prefix_sum[i - 1] + (i if s[i - 1] == '1' else 0)

    # 计算后缀和
    for i in range(n - 1, -1, -1):
        suffix_sum[i] = suffix_sum[i + 1] + (n - i if s[i] == '0' else 0)

    min_cost = float('inf')

    # 计算最小成本
    for i in range(n + 1):
        cost = prefix_sum[i] + suffix_sum[i]
        min_cost = min(min_cost, cost)

    return min_cost


Solution = create_solution(minimum_cost_to_make_all_characters_equal)