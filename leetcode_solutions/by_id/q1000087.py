# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1000087
标题: 小张刷题计划
难度: medium
链接: https://leetcode.cn/problems/xiao-zhang-shua-ti-ji-hua/
题目类型: 数组、二分查找
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
LCP 12. 小张刷题计划 - 为了提高自己的代码能力，小张制定了 LeetCode 刷题计划，他选中了 LeetCode 题库中的 n 道题，编号从 0 到 n-1，并计划在 m 天内按照题目编号顺序刷完所有的题目（注意，小张不能用多天完成同一题）。 在小张刷题计划中，小张需要用 time[i] 的时间完成编号 i 的题目。此外，小张还可以使用场外求助功能，通过询问他的好朋友小杨题目的解法，可以省去该题的做题时间。为了防止“小张刷题计划”变成“小杨刷题计划”，小张每天最多使用一次求助。 我们定义 m 天中做题时间最多的一天耗时为 T（小杨完成的题目不计入做题总时间）。请你帮小张求出最小的 T是多少。 示例 1： > 输入：time = [1,2,3,3], m = 2 > > 输出：3 > > 解释：第一天小张完成前三题，其中第三题找小杨帮忙；第二天完成第四题，并且找小杨帮忙。这样做题时间最多的一天花费了 3 的时间，并且这个值是最小的。 示例 2： > 输入：time = [999,999,999], m = 4 > > 输出：0 > > 解释：在前三天中，小张每天求助小杨一次，这样他可以在三天内完成所有的题目并不花任何时间。 限制： * 1 <= time.length <= 10^5 * 1 <= time[i] <= 10000 * 1 <= m <= 1000
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用二分查找来确定最小的最大做题时间T。

算法步骤:
1. 定义一个辅助函数 `can_finish` 来判断在给定的最大做题时间T下，是否可以在m天内完成所有题目。
2. 初始化二分查找的左右边界，左边界为0，右边界为题目时间之和。
3. 进行二分查找，找到最小的T使得 `can_finish(T)` 为True。

关键点:
- 使用二分查找来优化查找过程，减少时间复杂度。
- 辅助函数 `can_finish` 通过贪心策略来判断是否可以在给定时间内完成任务。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log(sum(time)))
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def can_finish(time: List[int], max_time: int, m: int) -> bool:
    days = 1
    current_time = 0
    max_time_today = 0
    
    for t in time:
        if current_time + t - max_time_today > max_time:
            if days == m:
                return False
            days += 1
            current_time = 0
            max_time_today = 0
        current_time += t
        max_time_today = max(max_time_today, t)
    
    return True

def min_max_time(time: List[int], m: int) -> int:
    left, right = 0, sum(time)
    while left < right:
        mid = (left + right) // 2
        if can_finish(time, mid, m):
            right = mid
        else:
            left = mid + 1
    return left

Solution = create_solution(min_max_time)