# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1000290
标题: 组合总和 Ⅳ
难度: medium
链接: https://leetcode.cn/problems/D0F0SV/
题目类型: 数组、动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
LCR 104. 组合总和 Ⅳ - 给定一个由 不同 正整数组成的数组 nums ，和一个目标整数 target 。请从 nums 中找出并返回总和为 target 的元素组合的个数。数组中的数字可以在一次排列中出现任意次，但是顺序不同的序列被视作不同的组合。 题目数据保证答案符合 32 位整数范围。 示例 1： 输入：nums = [1,2,3], target = 4 输出：7 解释： 所有可能的组合为： (1, 1, 1, 1) (1, 1, 2) (1, 2, 1) (1, 3) (2, 1, 1) (2, 2) (3, 1) 请注意，顺序不同的序列被视作不同的组合。 示例 2： 输入：nums = [9], target = 3 输出：0 提示： * 1 <= nums.length <= 200 * 1 <= nums[i] <= 1000 * nums 中的所有元素 互不相同 * 1 <= target <= 1000 进阶：如果给定的数组中含有负数会发生什么？问题会产生何种变化？如果允许负数出现，需要向题目中添加哪些限制条件？ 注意：本题与主站 377 题相同：https://leetcode.cn/problems/combination-sum-iv/ [https://leetcode.cn/problems/combination-sum-iv/]
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划来解决这个问题。我们定义 dp[i] 表示总和为 i 的组合数。对于每个 i，我们可以通过将 nums 中的每个数加上 dp[i-num] 来更新 dp[i]。

算法步骤:
1. 初始化一个长度为 target + 1 的 dp 数组，dp[0] = 1，表示总和为 0 的组合数为 1。
2. 遍历从 1 到 target 的每个值 i，对于每个 i，遍历 nums 中的每个数 num，如果 i >= num，则 dp[i] += dp[i - num]。
3. 返回 dp[target]。

关键点:
- 动态规划的状态转移方程：dp[i] = sum(dp[i - num] for num in nums if i >= num)
- 初始化 dp[0] = 1
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n * target)，其中 n 是 nums 的长度，target 是目标值。
空间复杂度: O(target)，因为我们需要一个长度为 target + 1 的 dp 数组。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def combination_sum_iv(nums: List[int], target: int) -> int:
    """
    函数式接口 - 计算总和为 target 的组合数
    """
    # 初始化 dp 数组
    dp = [0] * (target + 1)
    dp[0] = 1  # 总和为 0 的组合数为 1
    
    # 动态规划计算 dp 数组
    for i in range(1, target + 1):
        for num in nums:
            if i >= num:
                dp[i] += dp[i - num]
    
    return dp[target]


Solution = create_solution(combination_sum_iv)