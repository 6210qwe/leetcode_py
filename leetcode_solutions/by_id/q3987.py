# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3987
标题: Maximum Weight in Two Bags
难度: medium
链接: https://leetcode.cn/problems/maximum-weight-in-two-bags/
题目类型: 数组、动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3647. 两个袋子中的最大重量 - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划来解决这个问题。我们需要找到两个不相交的子数组，使得它们的和最大。

算法步骤:
1. 初始化一个二维数组 dp，其中 dp[i][0] 表示以第 i 个元素结尾的最大子数组和，dp[i][1] 表示以第 i 个元素结尾的第二大子数组和。
2. 遍历数组，更新 dp 数组。
3. 最后遍历 dp 数组，找到两个不相交的子数组的最大和。

关键点:
- 使用 dp 数组来记录每个位置的最大子数组和和第二大的子数组和。
- 确保两个子数组不相交。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(n)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(nums: List[int]) -> int:
    """
    函数式接口 - 实现最优解法
    """
    n = len(nums)
    if n < 2:
        return 0

    # 初始化 dp 数组
    dp = [[0, 0] for _ in range(n)]
    dp[0][0] = nums[0]

    max_sum = float('-inf')

    for i in range(1, n):
        # 更新 dp[i][0] 和 dp[i][1]
        dp[i][0] = max(dp[i-1][0] + nums[i], nums[i])
        dp[i][1] = max(dp[i-1][1], dp[i-1][0])

        # 更新最大和
        if i > 1:
            max_sum = max(max_sum, dp[i][0] + dp[i-1][1])

    return max_sum if max_sum != float('-inf') else 0


Solution = create_solution(solution_function_name)