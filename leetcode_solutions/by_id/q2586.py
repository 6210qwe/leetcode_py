# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2586
标题: Longest Square Streak in an Array
难度: medium
链接: https://leetcode.cn/problems/longest-square-streak-in-an-array/
题目类型: 数组、哈希表、二分查找、动态规划、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2501. 数组中最长的方波 - 给你一个整数数组 nums 。如果 nums 的子序列满足下述条件，则认为该子序列是一个 方波 ： * 子序列的长度至少为 2 ，并且 * 将子序列从小到大排序 之后 ，除第一个元素外，每个元素都是前一个元素的 平方 。 返回 nums 中 最长方波 的长度，如果不存在 方波 则返回 -1 。 子序列 也是一个数组，可以由另一个数组删除一些或不删除元素且不改变剩余元素的顺序得到。 示例 1 ： 输入：nums = [4,3,6,16,8,2] 输出：3 解释：选出子序列 [4,16,2] 。排序后，得到 [2,4,16] 。 - 4 = 2 * 2. - 16 = 4 * 4. 因此，[4,16,2] 是一个方波. 可以证明长度为 4 的子序列都不是方波。 示例 2 ： 输入：nums = [2,3,5,6,7] 输出：-1 解释：nums 不存在方波，所以返回 -1 。 提示： * 2 <= nums.length <= 105 * 2 <= nums[i] <= 105
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用哈希表记录每个数字及其平方数的存在情况，并通过递归计算最长的方波。

算法步骤:
1. 将所有数字存入集合中，以便快速查找。
2. 遍历每个数字，尝试找到以该数字为起点的最长方波。
3. 对于每个数字，递归地检查其平方数是否存在，并更新最长方波的长度。

关键点:
- 使用集合存储数字，以便 O(1) 时间复杂度的查找。
- 递归地检查每个数字的平方数，直到找不到为止。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log n)，其中 n 是 nums 的长度。排序操作的时间复杂度为 O(n log n)，遍历和递归的时间复杂度为 O(n)。
空间复杂度: O(n)，使用集合存储数字，递归调用栈的空间复杂度为 O(log n)。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def longest_square_streak(nums: List[int]) -> int:
    """
    函数式接口 - 计算数组中最长的方波长度
    """
    # 将所有数字存入集合中
    num_set = set(nums)
    
    def find_longest_streak(num: int) -> int:
        if num * num not in num_set:
            return 1
        return 1 + find_longest_streak(num * num)
    
    max_streak = -1
    for num in nums:
        if num * num in num_set:
            max_streak = max(max_streak, find_longest_streak(num))
    
    return max_streak if max_streak >= 2 else -1


Solution = create_solution(longest_square_streak)