# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1666
标题: Make The String Great
难度: easy
链接: https://leetcode.cn/problems/make-the-string-great/
题目类型: 栈、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1544. 整理字符串 - 给你一个由大小写英文字母组成的字符串 s 。 一个整理好的字符串中，两个相邻字符 s[i] 和 s[i+1]，其中 0<= i <= s.length-2 ，要满足如下条件: * 若 s[i] 是小写字符，则 s[i+1] 不可以是相同的大写字符。 * 若 s[i] 是大写字符，则 s[i+1] 不可以是相同的小写字符。 请你将字符串整理好，每次你都可以从字符串中选出满足上述条件的 两个相邻 字符并删除，直到字符串整理好为止。 请返回整理好的 字符串 。题目保证在给出的约束条件下，测试样例对应的答案是唯一的。 注意：空字符串也属于整理好的字符串，尽管其中没有任何字符。 示例 1： 输入：s = "leEeetcode" 输出："leetcode" 解释：无论你第一次选的是 i = 1 还是 i = 2，都会使 "leEeetcode" 缩减为 "leetcode" 。 示例 2： 输入：s = "abBAcC" 输出："" 解释：存在多种不同情况，但所有的情况都会导致相同的结果。例如： "abBAcC" --> "aAcC" --> "cC" --> "" "abBAcC" --> "abBA" --> "aA" --> "" 示例 3： 输入：s = "s" 输出："s" 提示： * 1 <= s.length <= 100 * s 只包含小写和大写英文字母
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用栈来处理字符串中的字符，如果当前字符与栈顶字符是大小写关系，则弹出栈顶字符；否则将当前字符压入栈。

算法步骤:
1. 初始化一个空栈。
2. 遍历字符串中的每个字符。
3. 如果栈不为空且当前字符与栈顶字符是大小写关系，则弹出栈顶字符。
4. 否则，将当前字符压入栈。
5. 最后，将栈中的字符拼接成结果字符串。

关键点:
- 使用栈来处理字符的匹配和删除。
- 判断字符是否为大小写关系时，可以使用 `ord` 函数和 ASCII 值。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串的长度。每个字符最多只会被压入和弹出栈一次。
空间复杂度: O(n)，最坏情况下，栈中可能会存储所有的字符。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def make_good(s: str) -> str:
    """
    函数式接口 - 将字符串整理好，使其满足相邻字符不为相同字母的大小写形式。
    """
    stack = []
    
    for char in s:
        if stack and (stack[-1].lower() == char.lower() and stack[-1] != char):
            stack.pop()
        else:
            stack.append(char)
    
    return ''.join(stack)


Solution = create_solution(make_good)