# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1585
标题: The kth Factor of n
难度: medium
链接: https://leetcode.cn/problems/the-kth-factor-of-n/
题目类型: 数学、数论
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1492. n 的第 k 个因子 - 给你两个正整数 n 和 k 。 如果正整数 i 满足 n % i == 0 ，那么我们就说正整数 i 是整数 n 的因子。 考虑整数 n 的所有因子，将它们 升序排列 。请你返回第 k 个因子。如果 n 的因子数少于 k ，请你返回 -1 。 示例 1： 输入：n = 12, k = 3 输出：3 解释：因子列表包括 [1, 2, 3, 4, 6, 12]，第 3 个因子是 3 。 示例 2： 输入：n = 7, k = 2 输出：7 解释：因子列表包括 [1, 7] ，第 2 个因子是 7 。 示例 3： 输入：n = 4, k = 4 输出：-1 解释：因子列表包括 [1, 2, 4] ，只有 3 个因子，所以我们应该返回 -1 。 提示： * 1 <= k <= n <= 1000 进阶： 你可以设计时间复杂度小于 O(n) 的算法来解决此问题吗？
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过遍历从 1 到 sqrt(n) 的所有数，找到 n 的所有因子，并存储在两个列表中。一个列表存储较小的因子，另一个列表存储较大的因子。最后根据 k 的值返回第 k 个因子。

算法步骤:
1. 初始化两个列表 small_factors 和 large_factors。
2. 遍历从 1 到 sqrt(n) 的所有数，如果 i 是 n 的因子，则将 i 添加到 small_factors 中，将 n // i 添加到 large_factors 中。
3. 根据 k 的值，从 small_factors 或 large_factors 中返回第 k 个因子。

关键点:
- 只需要遍历到 sqrt(n)，因为超过 sqrt(n) 的因子可以通过 n // i 得到。
- 通过两个列表分别存储较小和较大的因子，可以避免重复计算。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(sqrt(n))
空间复杂度: O(sqrt(n))
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(n: int, k: int) -> int:
    """
    函数式接口 - 返回 n 的第 k 个因子
    """
    small_factors = []
    large_factors = []

    for i in range(1, int(n**0.5) + 1):
        if n % i == 0:
            small_factors.append(i)
            if i != n // i:
                large_factors.append(n // i)

    all_factors = small_factors + large_factors[::-1]

    if k > len(all_factors):
        return -1
    else:
        return all_factors[k - 1]


Solution = create_solution(solution_function_name)