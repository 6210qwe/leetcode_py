# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 632
标题: Smallest Range Covering Elements from K Lists
难度: hard
链接: https://leetcode.cn/problems/smallest-range-covering-elements-from-k-lists/
题目类型: 贪心、数组、哈希表、排序、滑动窗口、堆（优先队列）
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
632. 最小区间 - 你有 k 个 非递减排列 的整数列表。找到一个 最小 区间，使得 k 个列表中的每个列表至少有一个数包含在其中。 我们定义如果 b-a < d-c 或者在 b-a == d-c 时 a < c，则区间 [a,b] 比 [c,d] 小。 示例 1： 输入：nums = [[4,10,15,24,26], [0,9,12,20], [5,18,22,30]] 输出：[20,24] 解释： 列表 1：[4, 10, 15, 24, 26]，24 在区间 [20,24] 中。 列表 2：[0, 9, 12, 20]，20 在区间 [20,24] 中。 列表 3：[5, 18, 22, 30]，22 在区间 [20,24] 中。 示例 2： 输入：nums = [[1,2,3],[1,2,3],[1,2,3]] 输出：[1,1] 提示： * nums.length == k * 1 <= k <= 3500 * 1 <= nums[i].length <= 50 * -105 <= nums[i][j] <= 105 * nums[i] 按非递减顺序排列
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用最小堆来维护当前区间的最小值，并使用一个变量来记录当前区间的最大值。通过不断更新最小堆和最大值，找到最小区间。

算法步骤:
1. 初始化最小堆，将每个列表的第一个元素加入堆中，并记录当前的最大值。
2. 不断从堆中取出最小值，更新当前区间的最小值和最大值，计算当前区间的长度。
3. 如果当前区间的长度小于已知的最小区间长度，则更新最小区间。
4. 将取出元素的下一个元素加入堆中，继续上述过程，直到某个列表的所有元素都被处理完。

关键点:
- 使用最小堆来高效地获取当前区间的最小值。
- 通过不断更新最小值和最大值来找到最小区间。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n * log k)，其中 n 是所有列表中元素的总数，k 是列表的数量。
空间复杂度: O(k)，因为最小堆中最多有 k 个元素。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List
import heapq

def smallestRange(nums: List[List[int]]) -> List[int]:
    """
    函数式接口 - 找到覆盖 k 个列表中每个列表至少一个数的最小区间
    """
    # 初始化最小堆和最大值
    min_heap = []
    max_val = float('-inf')
    
    # 将每个列表的第一个元素加入堆中
    for i, lst in enumerate(nums):
        heapq.heappush(min_heap, (lst[0], i, 0))
        max_val = max(max_val, lst[0])
    
    # 初始化最小区间
    min_range = [float('-inf'), float('inf')]
    min_range_length = float('inf')
    
    while min_heap:
        min_val, list_idx, element_idx = heapq.heappop(min_heap)
        
        # 更新最小区间
        if max_val - min_val < min_range_length:
            min_range = [min_val, max_val]
            min_range_length = max_val - min_val
        
        # 如果当前列表还有更多元素，将其加入堆中
        if element_idx + 1 < len(nums[list_idx]):
            next_val = nums[list_idx][element_idx + 1]
            heapq.heappush(min_heap, (next_val, list_idx, element_idx + 1))
            max_val = max(max_val, next_val)
        else:
            # 如果某个列表的所有元素都被处理完，结束循环
            break
    
    return min_range

Solution = create_solution(smallestRange)