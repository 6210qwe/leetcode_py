# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1398
标题: Number of Ways to Stay in the Same Place After Some Steps
难度: hard
链接: https://leetcode.cn/problems/number-of-ways-to-stay-in-the-same-place-after-some-steps/
题目类型: 动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1269. 停在原地的方案数 - 有一个长度为 arrLen 的数组，开始有一个指针在索引 0 处。 每一步操作中，你可以将指针向左或向右移动 1 步，或者停在原地（指针不能被移动到数组范围外）。 给你两个整数 steps 和 arrLen ，请你计算并返回：在恰好执行 steps 次操作以后，指针仍然指向索引 0 处的方案数。 由于答案可能会很大，请返回方案数 模 10^9 + 7 后的结果。 示例 1： 输入：steps = 3, arrLen = 2 输出：4 解释：3 步后，总共有 4 种不同的方法可以停在索引 0 处。 向右，向左，不动 不动，向右，向左 向右，不动，向左 不动，不动，不动 示例 2： 输入：steps = 2, arrLen = 4 输出：2 解释：2 步后，总共有 2 种不同的方法可以停在索引 0 处。 向右，向左 不动，不动 示例 3： 输入：steps = 4, arrLen = 2 输出：8 提示： * 1 <= steps <= 500 * 1 <= arrLen <= 106
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划来解决这个问题。我们定义 dp[i][j] 表示在第 i 步时指针位于 j 位置的方案数。

算法步骤:
1. 初始化一个二维数组 dp，其中 dp[i][j] 表示在第 i 步时指针位于 j 位置的方案数。
2. 设置边界条件：dp[0][0] = 1，表示初始时指针在位置 0。
3. 对于每个步数 i，更新 dp[i][j] 的值，考虑三种情况：从 j-1 移动到 j，从 j+1 移动到 j，以及停留在 j。
4. 最终结果是 dp[steps][0]，即在 steps 步后指针仍然在位置 0 的方案数。

关键点:
- 为了优化空间复杂度，我们可以使用滚动数组，只保留当前行和上一行的状态。
- 为了避免越界，我们需要确保 j 在 [0, min(arrLen-1, steps)] 范围内。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(steps * min(arrLen, steps)) - 我们需要遍历每个步数和每个可能的位置。
空间复杂度: O(min(arrLen, steps)) - 使用滚动数组优化后的空间复杂度。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def num_ways(steps: int, arr_len: int) -> int:
    """
    计算在恰好执行 steps 次操作以后，指针仍然指向索引 0 处的方案数。
    """
    MOD = 10**9 + 7
    max_pos = min(arr_len - 1, steps)
    dp = [0] * (max_pos + 1)
    dp[0] = 1

    for i in range(1, steps + 1):
        next_dp = [0] * (max_pos + 1)
        for j in range(max_pos + 1):
            next_dp[j] = dp[j]
            if j > 0:
                next_dp[j] = (next_dp[j] + dp[j - 1]) % MOD
            if j < max_pos:
                next_dp[j] = (next_dp[j] + dp[j + 1]) % MOD
        dp = next_dp

    return dp[0]


Solution = create_solution(num_ways)