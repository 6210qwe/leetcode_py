# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2367
标题: Minimum Lines to Represent a Line Chart
难度: medium
链接: https://leetcode.cn/problems/minimum-lines-to-represent-a-line-chart/
题目类型: 几何、数组、数学、数论、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2280. 表示一个折线图的最少线段数 - 给你一个二维整数数组 stockPrices ，其中 stockPrices[i] = [dayi, pricei] 表示股票在 dayi 的价格为 pricei 。折线图 是一个二维平面上的若干个点组成的图，横坐标表示日期，纵坐标表示价格，折线图由相邻的点连接而成。比方说下图是一个例子： [https://assets.leetcode.com/uploads/2022/03/30/1920px-pushkin_population_historysvg.png] 请你返回要表示一个折线图所需要的 最少线段数 。 示例 1： [https://assets.leetcode.com/uploads/2022/03/30/ex0.png] 输入：stockPrices = [[1,7],[2,6],[3,5],[4,4],[5,4],[6,3],[7,2],[8,1]] 输出：3 解释： 上图为输入对应的图，横坐标表示日期，纵坐标表示价格。 以下 3 个线段可以表示折线图： - 线段 1 （红色）从 (1,7) 到 (4,4) ，经过 (1,7) ，(2,6) ，(3,5) 和 (4,4) 。 - 线段 2 （蓝色）从 (4,4) 到 (5,4) 。 - 线段 3 （绿色）从 (5,4) 到 (8,1) ，经过 (5,4) ，(6,3) ，(7,2) 和 (8,1) 。 可以证明，无法用少于 3 条线段表示这个折线图。 示例 2： [https://assets.leetcode.com/uploads/2022/03/30/ex1.png] 输入：stockPrices = [[3,4],[1,2],[7,8],[2,3]] 输出：1 解释： 如上图所示，折线图可以用一条线段表示。 提示： * 1 <= stockPrices.length <= 105 * stockPrices[i].length == 2 * 1 <= dayi, pricei <= 109 * 所有 dayi 互不相同 。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用斜率来判断相邻点是否在同一直线上。

算法步骤:
1. 对 stockPrices 按日期排序。
2. 初始化线段数为 1（至少有一条线段）。
3. 遍历排序后的点，计算每对相邻点的斜率。
4. 如果当前斜率与前一个斜率不同，则增加线段数。
5. 返回线段数。

关键点:
- 计算斜率时使用最大公约数来避免浮点误差。
- 使用 Python 的 math.gcd 函数来简化斜率的计算。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log n)，其中 n 是 stockPrices 的长度。排序操作的时间复杂度为 O(n log n)，遍历操作的时间复杂度为 O(n)。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List
import math


def minimum_lines(stock_prices: List[List[int]]) -> int:
    """
    返回表示折线图所需的最少线段数。
    """
    if len(stock_prices) < 2:
        return 0

    # 按日期排序
    stock_prices.sort(key=lambda x: x[0])

    # 初始化线段数
    lines = 1
    prev_slope = None

    for i in range(1, len(stock_prices)):
        dx = stock_prices[i][0] - stock_prices[i - 1][0]
        dy = stock_prices[i][1] - stock_prices[i - 1][1]
        gcd = math.gcd(dx, dy)
        slope = (dx // gcd, dy // gcd)

        if slope != prev_slope:
            lines += 1
            prev_slope = slope

    return lines


Solution = create_solution(minimum_lines)