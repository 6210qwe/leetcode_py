# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3298
标题: Maximize Consecutive Elements in an Array After Modification
难度: hard
链接: https://leetcode.cn/problems/maximize-consecutive-elements-in-an-array-after-modification/
题目类型: 数组、动态规划、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3041. 修改数组后最大化数组中的连续元素数目 - 给你一个下标从 0 开始只包含 正 整数的数组 nums 。 一开始，你可以将数组中 任意数量 元素增加 至多 1 。 修改后，你可以从最终数组中选择 一个或者更多 元素，并确保这些元素升序排序后是 连续 的。比方说，[3, 4, 5] 是连续的，但是 [3, 4, 6] 和 [1, 1, 2, 3] 不是连续的。 请你返回 最多 可以选出的元素数目。 示例 1： 输入：nums = [2,1,5,1,1] 输出：3 解释：我们将下标 0 和 3 处的元素增加 1 ，得到结果数组 nums = [3,1,5,2,1] 。 我们选择元素 [3,1,5,2,1] 并将它们排序得到 [1,2,3] ，是连续元素。 最多可以得到 3 个连续元素。 示例 2： 输入：nums = [1,4,7,10] 输出：1 解释：我们可以选择的最多元素数目是 1 。 提示： * 1 <= nums.length <= 105 * 1 <= nums[i] <= 106
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划来记录每个元素作为连续子序列结尾的最大长度。

算法步骤:
1. 对数组进行排序。
2. 使用动态规划数组 dp，其中 dp[i] 表示以 nums[i] 结尾的最长连续子序列的长度。
3. 初始化 dp 数组，dp[0] = 1。
4. 遍历数组，对于每个元素 nums[i]，检查它是否可以接在前面的某个元素后面形成连续子序列。
5. 更新 dp[i] 的值。
6. 返回 dp 数组中的最大值。

关键点:
- 排序后的数组可以帮助我们更容易地找到连续子序列。
- 动态规划数组 dp 用于记录每个位置的最大连续子序列长度。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log n)，其中 n 是 nums 的长度，因为排序的时间复杂度是 O(n log n)。
空间复杂度: O(n)，因为我们使用了一个额外的 dp 数组。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def solution_function_name(nums: List[int]) -> int:
    """
    函数式接口 - 实现最优解法
    """
    if not nums:
        return 0

    # 对数组进行排序
    nums.sort()

    n = len(nums)
    dp = [1] * n  # 初始化 dp 数组

    for i in range(1, n):
        for j in range(i):
            if nums[i] == nums[j] + 1 or (nums[i] == nums[j] and nums[i] != 1):
                dp[i] = max(dp[i], dp[j] + 1)

    return max(dp)

Solution = create_solution(solution_function_name)