# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1000273
标题: 有效的字母异位词
难度: easy
链接: https://leetcode.cn/problems/dKk3P7/
题目类型: 哈希表、字符串、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
LCR 032. 有效的字母异位词 - 给定两个字符串 s 和 t ，编写一个函数来判断它们是不是一组变位词（字母异位词）。 注意：若 s 和 t 中每个字符出现的次数都相同且字符顺序不完全相同，则称 s 和 t 互为变位词（字母异位词）。 示例 1： 输入：s = "anagram", t = "nagaram" 输出：true 示例 2： 输入：s = "rat", t = "car" 输出：false 示例 3： 输入：s = "a", t = "a" 输出：false 提示： * 1 <= s.length, t.length <= 5 * 104 * s 和 t 仅包含小写字母 进阶: 如果输入字符串包含 unicode 字符怎么办？你能否调整你的解法来应对这种情况？ 注意：本题与主站 242 题相似（字母异位词定义不同）：https://leetcode.cn/problems/valid-anagram/ [https://leetcode.cn/problems/valid-anagram/]
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用哈希表记录字符出现的次数，然后比较两个字符串的字符计数是否相同。同时检查两个字符串是否完全相同。

算法步骤:
1. 检查两个字符串是否完全相同，如果相同则返回 False。
2. 使用哈希表记录第一个字符串中每个字符的出现次数。
3. 遍历第二个字符串，减少哈希表中对应字符的计数。
4. 最后检查哈希表中所有字符的计数是否都为零。

关键点:
- 使用哈希表记录字符出现的次数。
- 检查两个字符串是否完全相同。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串的长度。我们需要遍历两个字符串各一次。
空间复杂度: O(1)，因为哈希表的大小最多为 26（小写字母的数量）。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def is_valid_anagram(s: str, t: str) -> bool:
    """
    判断两个字符串是否是有效的字母异位词。
    """
    if s == t:
        return False

    # 使用哈希表记录字符出现的次数
    char_count = {}
    for char in s:
        char_count[char] = char_count.get(char, 0) + 1

    for char in t:
        if char not in char_count:
            return False
        char_count[char] -= 1
        if char_count[char] < 0:
            return False

    # 检查哈希表中所有字符的计数是否都为零
    for count in char_count.values():
        if count != 0:
            return False

    return True


Solution = create_solution(is_valid_anagram)