# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2584
标题: Number of Nodes With Value One
难度: medium
链接: https://leetcode.cn/problems/number-of-nodes-with-value-one/
题目类型: 树、深度优先搜索、广度优先搜索、数组、二叉树
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2445. 值为 1 的节点数 - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用深度优先搜索（DFS）遍历整棵树，统计值为 1 的节点数。

算法步骤:
1. 定义一个递归函数 `dfs`，用于遍历树的每个节点。
2. 在 `dfs` 函数中，如果当前节点为空，则返回 0。
3. 如果当前节点的值为 1，则计数加 1，并继续递归遍历其左子节点和右子节点。
4. 返回当前节点及其子节点中值为 1 的节点总数。

关键点:
- 使用递归进行深度优先搜索，确保每个节点都被访问到。
- 通过递归调用累加每个子树中值为 1 的节点数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是树中节点的数量。每个节点都被访问一次。
空间复杂度: O(h)，其中 h 是树的高度。递归调用栈的深度最多为树的高度。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import Optional
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution

def count_nodes_with_value_one(root: Optional[TreeNode]) -> int:
    """
    统计二叉树中值为 1 的节点数。
    
    :param root: 二叉树的根节点
    :return: 值为 1 的节点数
    """
    def dfs(node: Optional[TreeNode]) -> int:
        if not node:
            return 0
        count = 0
        if node.val == 1:
            count += 1
        count += dfs(node.left)
        count += dfs(node.right)
        return count

    return dfs(root)

Solution = create_solution(count_nodes_with_value_one)