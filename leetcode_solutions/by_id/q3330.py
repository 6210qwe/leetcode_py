# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3330
标题: Modify the Matrix
难度: easy
链接: https://leetcode.cn/problems/modify-the-matrix/
题目类型: 数组、矩阵
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3033. 修改矩阵 - 给你一个下标从 0 开始、大小为 m x n 的整数矩阵 matrix ，新建一个下标从 0 开始、名为 answer 的矩阵。使 answer 与 matrix 相等，接着将其中每个值为 -1 的元素替换为所在列的 最大 元素。 返回矩阵 answer 。 示例 1： [https://assets.leetcode.com/uploads/2023/12/24/matrix1.png] 输入：matrix = [[1,2,-1],[4,-1,6],[7,8,9]] 输出：[[1,2,9],[4,8,6],[7,8,9]] 解释：上图显示了发生替换的元素（蓝色区域）。 - 将单元格 [1][1] 中的值替换为列 1 中的最大值 8 。 - 将单元格 [0][2] 中的值替换为列 2 中的最大值 9 。 示例 2： [https://assets.leetcode.com/uploads/2023/12/24/matrix2.png] 输入：matrix = [[3,-1],[5,2]] 输出：[[3,2],[5,2]] 解释：上图显示了发生替换的元素（蓝色区域）。 提示： * m == matrix.length * n == matrix[i].length * 2 <= m, n <= 50 * -1 <= matrix[i][j] <= 100 * 测试用例中生成的输入满足每列至少包含一个非负整数。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 
- 首先计算每一列的最大值。
- 然后遍历矩阵，将所有值为 -1 的元素替换为其所在列的最大值。

算法步骤:
1. 初始化一个数组 col_maxes 来存储每一列的最大值。
2. 遍历矩阵的每一列，计算并存储每一列的最大值。
3. 再次遍历矩阵，将值为 -1 的元素替换为其所在列的最大值。

关键点:
- 通过两次遍历矩阵来实现最优的时间复杂度。
- 使用额外的空间来存储每一列的最大值。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n)
- 需要两次遍历矩阵，每次遍历的时间复杂度为 O(m * n)。

空间复杂度: O(n)
- 需要一个长度为 n 的数组来存储每一列的最大值。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def modify_matrix(matrix: List[List[int]]) -> List[List[int]]:
    """
    函数式接口 - 修改矩阵
    """
    if not matrix or not matrix[0]:
        return matrix

    m, n = len(matrix), len(matrix[0])
    col_maxes = [max(col) for col in zip(*matrix)]  # 计算每一列的最大值

    for i in range(m):
        for j in range(n):
            if matrix[i][j] == -1:
                matrix[i][j] = col_maxes[j]  # 替换为所在列的最大值

    return matrix


Solution = create_solution(modify_matrix)