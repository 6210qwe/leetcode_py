# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2425
标题: Number of People That Can Be Seen in a Grid
难度: medium
链接: https://leetcode.cn/problems/number-of-people-that-can-be-seen-in-a-grid/
题目类型: 栈、数组、矩阵、单调栈
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2282. 在一个网格中可以看到的人数 - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用单调栈来解决这个问题。对于每一行，从左到右遍历，使用单调递减栈来记录可以被看到的人。

算法步骤:
1. 初始化结果变量 `res` 为 0。
2. 对于每一行：
   - 初始化一个单调递减栈 `stack`。
   - 从左到右遍历该行的每个元素：
     - 如果当前元素大于栈顶元素，则将栈顶元素弹出，并增加结果计数。
     - 将当前元素压入栈中。
3. 返回结果 `res`。

关键点:
- 使用单调递减栈来记录可以被看到的人。
- 每次遇到比栈顶元素大的元素时，栈顶元素会被弹出，表示该位置的人被遮挡。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n)，其中 m 是网格的行数，n 是网格的列数。每个元素最多只会被压入和弹出栈一次。
空间复杂度: O(n)，栈的最大长度为网格的一行长度。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(grid: List[List[int]]) -> int:
    """
    函数式接口 - 计算在一个网格中可以看到的人数
    """
    res = 0
    for row in grid:
        stack = []
        for height in row:
            while stack and stack[-1] <= height:
                stack.pop()
            if not stack:
                res += 1
            stack.append(height)
    return res


Solution = create_solution(solution_function_name)