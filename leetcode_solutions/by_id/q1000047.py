# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1000047
标题: T9 LCCI
难度: medium
链接: https://leetcode.cn/problems/t9-lcci/
题目类型: 数组、哈希表、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
面试题 16.20. T9键盘 - 在老式手机上，用户通过数字键盘输入，手机将提供与这些数字相匹配的单词列表。每个数字映射到0至4个字母。给定一个数字序列，实现一个算法来返回匹配单词的列表。你会得到一张含有有效单词的列表。映射如下图所示： [https://assets.leetcode.cn/aliyun-lc-upload/original_images/17_telephone_keypad.png] 示例 1： 输入：num = "8733", words = ["tree", "used"] 输出：["tree", "used"] 示例 2： 输入：num = "2", words = ["a", "b", "c", "d"] 输出：["a", "b", "c"] 提示： * num.length <= 1000 * words.length <= 500 * words[i].length == num.length * num中不会出现 0, 1 这两个数字
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用字典存储数字和字母的映射关系，然后遍历单词列表，检查每个单词是否与输入的数字序列匹配。

算法步骤:
1. 创建一个字典，存储每个数字对应的字母。
2. 遍历单词列表，对于每个单词，检查其每个字符是否与输入的数字序列中的对应字符匹配。
3. 如果匹配，则将该单词加入结果列表。

关键点:
- 使用字典快速查找每个数字对应的字母。
- 逐字符比较单词和数字序列，确保每个字符都匹配。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n * m)，其中 n 是 words 的长度，m 是每个单词的长度。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution

def t9_keyboard(num: str, words: List[str]) -> List[str]:
    """
    返回与输入数字序列匹配的单词列表。
    """
    # 数字到字母的映射
    digit_to_letters = {
        '2': 'abc', '3': 'def', '4': 'ghi', '5': 'jkl',
        '6': 'mno', '7': 'pqrs', '8': 'tuv', '9': 'wxyz'
    }
    
    # 结果列表
    result = []
    
    # 遍历单词列表
    for word in words:
        if len(word) != len(num):
            continue
        
        # 检查每个字符是否匹配
        for i, char in enumerate(word):
            if char not in digit_to_letters[num[i]]:
                break
        else:
            result.append(word)
    
    return result

Solution = create_solution(t9_keyboard)