# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 626
标题: Exchange Seats
难度: medium
链接: https://leetcode.cn/problems/exchange-seats/
题目类型: 数据库
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
626. 换座位 - 表: Seat +-------------+---------+ | Column Name | Type | +-------------+---------+ | id | int | | student | varchar | +-------------+---------+ id 是该表的主键（唯一值）列。 该表的每一行都表示学生的姓名和 ID。 ID 序列始终从 1 开始并连续增加。 编写解决方案来交换每两个连续的学生的座位号。如果学生的数量是奇数，则最后一个学生的id不交换。 按 id 升序 返回结果表。 查询结果格式如下所示。 示例 1: 输入: Seat 表: +----+---------+ | id | student | +----+---------+ | 1 | Abbot | | 2 | Doris | | 3 | Emerson | | 4 | Green | | 5 | Jeames | +----+---------+ 输出: +----+---------+ | id | student | +----+---------+ | 1 | Doris | | 2 | Abbot | | 3 | Green | | 4 | Emerson | | 5 | Jeames | +----+---------+ 解释: 请注意，如果学生人数为奇数，则不需要更换最后一名学生的座位。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用 SQL 语句来实现座位交换。

算法步骤:
1. 使用 CASE WHEN 语句来判断每个学生的 id 是奇数还是偶数。
2. 如果 id 是奇数且不是最后一个学生，则将其 id 增加 1。
3. 如果 id 是偶数，则将其 id 减少 1。
4. 如果 id 是奇数且是最后一个学生，则保持不变。
5. 按新的 id 排序并返回结果。

关键点:
- 使用 CASE WHEN 语句来处理不同的 id 情况。
- 确保最后一个学生的 id 不被交换。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是学生的数量。我们需要遍历所有学生来进行交换。
空间复杂度: O(1)，我们只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def exchange_seats(seat: List[List[str]]) -> List[List[str]]:
    """
    交换每两个连续的学生的座位号。如果学生的数量是奇数，则最后一个学生的id不交换。
    按 id 升序 返回结果表。
    """
    # 将输入列表转换为字典，方便后续操作
    seat_dict = {int(id): student for id, student in seat}
    
    # 获取学生总数
    n = len(seat)
    
    # 用于存储结果的列表
    result = []
    
    for i in range(1, n + 1):
        if i % 2 == 0:
            # 如果 id 是偶数，将其 id 减少 1
            new_id = i - 1
        elif i < n:
            # 如果 id 是奇数且不是最后一个学生，将其 id 增加 1
            new_id = i + 1
        else:
            # 如果 id 是奇数且是最后一个学生，保持不变
            new_id = i
        
        # 将新 id 和对应的学生添加到结果列表中
        result.append([new_id, seat_dict[i]])
    
    # 按新的 id 排序
    result.sort(key=lambda x: x[0])
    
    return result


Solution = create_solution(exchange_seats)