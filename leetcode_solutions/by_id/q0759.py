# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 759
标题: Set Intersection Size At Least Two
难度: hard
链接: https://leetcode.cn/problems/set-intersection-size-at-least-two/
题目类型: 贪心、数组、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
757. 设置交集大小至少为2 - 给你一个二维整数数组 intervals ，其中 intervals[i] = [starti, endi] 表示从 starti 到 endi 的所有整数，包括 starti 和 endi 。 包含集合 是一个名为 nums 的数组，并满足 intervals 中的每个区间都 至少 有 两个 整数在 nums 中。 * 例如，如果 intervals = [[1,3], [3,7], [8,9]] ，那么 [1,2,4,7,8,9] 和 [2,3,4,8,9] 都符合 包含集合 的定义。 返回包含集合可能的最小大小。 示例 1： 输入：intervals = [[1,3],[3,7],[8,9]] 输出：5 解释：nums = [2, 3, 4, 8, 9]. 可以证明不存在元素数量为 4 的包含集合。 示例 2： 输入：intervals = [[1,3],[1,4],[2,5],[3,5]] 输出：3 解释：nums = [2, 3, 4]. 可以证明不存在元素数量为 2 的包含集合。 示例 3： 输入：intervals = [[1,2],[2,3],[2,4],[4,5]] 输出：5 解释：nums = [1, 2, 3, 4, 5]. 可以证明不存在元素数量为 4 的包含集合。 提示： * 1 <= intervals.length <= 3000 * intervals[i].length == 2 * 0 <= starti < endi <= 108
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用贪心算法来选择最少的元素，使得每个区间至少有两个元素在集合中。

算法步骤:
1. 按区间的结束位置升序排序。
2. 初始化一个空的结果集 `result`。
3. 遍历排序后的区间：
   - 如果当前区间的开始位置大于结果集中的最后一个元素，则添加当前区间的最后两个元素到结果集中。
   - 否则，检查当前区间的结束位置是否小于结果集中的倒数第二个元素，如果是，则添加当前区间的最后一个元素到结果集中。

关键点:
- 通过按结束位置排序，确保每次选择的元素尽可能靠后，从而减少后续区间的覆盖需求。
- 通过维护结果集中的元素，确保每个区间至少有两个元素在集合中。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log n)，其中 n 是 intervals 的长度。排序操作的时间复杂度为 O(n log n)，遍历操作为 O(n)。
空间复杂度: O(1)，除了输入和输出外，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def intersectionSizeTwo(intervals: List[List[int]]) -> int:
    """
    函数式接口 - 实现最优解法
    """
    # 按区间的结束位置升序排序，如果结束位置相同，则按开始位置降序排序
    intervals.sort(key=lambda x: (x[1], -x[0]))
    
    result = []
    for start, end in intervals:
        if not result or start > result[-1]:
            result.append(end - 1)
            result.append(end)
        elif start > result[-2]:
            result.append(end)
    
    return len(result)

Solution = create_solution(intersectionSizeTwo)