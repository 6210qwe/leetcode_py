# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2170
标题: Count Number of Maximum Bitwise-OR Subsets
难度: medium
链接: https://leetcode.cn/problems/count-number-of-maximum-bitwise-or-subsets/
题目类型: 位运算、数组、回溯、枚举
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2044. 统计按位或能得到最大值的子集数目 - 给你一个整数数组 nums ，请你找出 nums 子集 按位或 可能得到的 最大值 ，并返回按位或能得到最大值的 不同非空子集的数目 。 如果数组 a 可以由数组 b 删除一些元素（或不删除）得到，则认为数组 a 是数组 b 的一个 子集 。如果选中的元素下标位置不一样，则认为两个子集 不同 。 对数组 a 执行 按位或 ，结果等于 a[0] OR a[1] OR ... OR a[a.length - 1]（下标从 0 开始）。 示例 1： 输入：nums = [3,1] 输出：2 解释：子集按位或能得到的最大值是 3 。有 2 个子集按位或可以得到 3 ： - [3] - [3,1] 示例 2： 输入：nums = [2,2,2] 输出：7 解释：[2,2,2] 的所有非空子集的按位或都可以得到 2 。总共有 23 - 1 = 7 个子集。 示例 3： 输入：nums = [3,2,1,5] 输出：6 解释：子集按位或可能的最大值是 7 。有 6 个子集按位或可以得到 7 ： - [3,5] - [3,1,5] - [3,2,5] - [3,2,1,5] - [2,5] - [2,1,5] 提示： * 1 <= nums.length <= 16 * 1 <= nums[i] <= 105
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用位掩码来生成所有可能的子集，并计算每个子集的按位或结果，统计最大按位或结果的子集数量。

算法步骤:
1. 计算所有元素的按位或结果，得到最大值。
2. 使用位掩码生成所有可能的子集。
3. 对于每个子集，计算其按位或结果，如果等于最大值，则计数器加一。

关键点:
- 使用位掩码生成子集，时间复杂度为 O(2^n)。
- 计算按位或结果时，使用位运算优化。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(2^n)，其中 n 是数组的长度。因为我们需要生成所有可能的子集。
空间复杂度: O(1)，除了输入和输出外，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def count_max_or_subsets(nums: List[int]) -> int:
    """
    函数式接口 - 统计按位或能得到最大值的子集数目
    """
    max_or = 0
    for num in nums:
        max_or |= num  # 计算所有元素的按位或结果

    n = len(nums)
    count = 0
    for mask in range(1, 1 << n):  # 生成所有可能的子集
        current_or = 0
        for i in range(n):
            if mask & (1 << i):
                current_or |= nums[i]
        if current_or == max_or:
            count += 1

    return count

Solution = create_solution(count_max_or_subsets)