# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2199
标题: Two Furthest Houses With Different Colors
难度: easy
链接: https://leetcode.cn/problems/two-furthest-houses-with-different-colors/
题目类型: 贪心、数组
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2078. 两栋颜色不同且距离最远的房子 - 街上有 n 栋房子整齐地排成一列，每栋房子都粉刷上了漂亮的颜色。给你一个下标从 0 开始且长度为 n 的整数数组 colors ，其中 colors[i] 表示第 i 栋房子的颜色。 返回 两栋 颜色 不同 房子之间的 最大 距离。 第 i 栋房子和第 j 栋房子之间的距离是 abs(i - j) ，其中 abs(x) 是 x 的绝对值。 示例 1： [https://assets.leetcode.com/uploads/2021/10/31/eg1.png] 输入：colors = [1,1,1,6,1,1,1] 输出：3 解释：上图中，颜色 1 标识成蓝色，颜色 6 标识成红色。 两栋颜色不同且距离最远的房子是房子 0 和房子 3 。 房子 0 的颜色是颜色 1 ，房子 3 的颜色是颜色 6 。两栋房子之间的距离是 abs(0 - 3) = 3 。 注意，房子 3 和房子 6 也可以产生最佳答案。 示例 2： [https://assets.leetcode.com/uploads/2021/10/31/eg2.png] 输入：colors = [1,8,3,8,3] 输出：4 解释：上图中，颜色 1 标识成蓝色，颜色 8 标识成黄色，颜色 3 标识成绿色。 两栋颜色不同且距离最远的房子是房子 0 和房子 4 。 房子 0 的颜色是颜色 1 ，房子 4 的颜色是颜色 3 。两栋房子之间的距离是 abs(0 - 4) = 4 。 示例 3： 输入：colors = [0,1] 输出：1 解释：两栋颜色不同且距离最远的房子是房子 0 和房子 1 。 房子 0 的颜色是颜色 0 ，房子 1 的颜色是颜色 1 。两栋房子之间的距离是 abs(0 - 1) = 1 。 提示： * n == colors.length * 2 <= n <= 100 * 0 <= colors[i] <= 100 * 生成的测试数据满足 至少 存在 2 栋颜色不同的房子
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过双指针法找到最远的不同颜色的房子对。

算法步骤:
1. 初始化两个指针，分别指向数组的首尾。
2. 如果首尾指针所指的房子颜色不同，则返回它们的距离。
3. 否则，移动指针，直到找到不同颜色的房子对。
4. 比较所有可能的最大距离，返回最大值。

关键点:
- 使用双指针法可以在线性时间内找到最远的不同颜色的房子对。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(colors: List[int]) -> int:
    """
    函数式接口 - 找到两栋颜色不同且距离最远的房子
    """
    n = len(colors)
    left, right = 0, n - 1
    
    # 双指针法
    while left < right:
        if colors[left] != colors[right]:
            return right - left
        # 移动指针
        if colors[left] == colors[left + 1]:
            left += 1
        if colors[right] == colors[right - 1]:
            right -= 1
        if colors[left] != colors[left + 1] and colors[right] != colors[right - 1]:
            left += 1
            right -= 1
    
    return 0


Solution = create_solution(solution_function_name)