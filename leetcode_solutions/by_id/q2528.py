# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2528
标题: Number of Valid Clock Times
难度: easy
链接: https://leetcode.cn/problems/number-of-valid-clock-times/
题目类型: 字符串、枚举
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2437. 有效时间的数目 - 给你一个长度为 5 的字符串 time ，表示一个电子时钟当前的时间，格式为 "hh:mm" 。最早 可能的时间是 "00:00" ，最晚 可能的时间是 "23:59" 。 在字符串 time 中，被字符 ? 替换掉的数位是 未知的 ，被替换的数字可能是 0 到 9 中的任何一个。 请你返回一个整数 answer ，将每一个 ? 都用 0 到 9 中一个数字替换后，可以得到的有效时间的数目。 示例 1： 输入：time = "?5:00" 输出：2 解释：我们可以将 ? 替换成 0 或 1 ，得到 "05:00" 或者 "15:00" 。注意我们不能替换成 2 ，因为时间 "25:00" 是无效时间。所以我们有两个选择。 示例 2： 输入：time = "0?:0?" 输出：100 解释：两个 ? 都可以被 0 到 9 之间的任意数字替换，所以我们总共有 100 种选择。 示例 3： 输入：time = "??:??" 输出：1440 解释：小时总共有 24 种选择，分钟总共有 60 种选择。所以总共有 24 * 60 = 1440 种选择。 提示： * time 是一个长度为 5 的有效字符串，格式为 "hh:mm" 。 * "00" <= hh <= "23" * "00" <= mm <= "59" * 字符串中有的数位是 '?' ，需要用 0 到 9 之间的数字替换。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 枚举所有可能的小时和分钟组合，并检查其有效性。

算法步骤:
1. 分别处理小时和分钟部分，根据问号的位置确定可能的取值范围。
2. 计算所有有效的时间组合数量。

关键点:
- 小时部分的取值范围需要根据第一个字符是否为问号来确定。
- 分钟部分的取值范围相对固定。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(1)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def count_valid_clock_times(time: str) -> int:
    """
    函数式接口 - 计算有效时间的数目
    """
    # 处理小时部分
    if time[0] == '?' and time[1] == '?':
        hour_options = 24
    elif time[0] == '?':
        hour_options = 3 if time[1] < '4' else 2
    elif time[1] == '?':
        hour_options = 10 if time[0] < '2' else 4
    else:
        hour_options = 1

    # 处理分钟部分
    if time[3] == '?' and time[4] == '?':
        minute_options = 60
    elif time[3] == '?':
        minute_options = 6
    elif time[4] == '?':
        minute_options = 10
    else:
        minute_options = 1

    return hour_options * minute_options


Solution = create_solution(count_valid_clock_times)