# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 446
标题: Arithmetic Slices II - Subsequence
难度: hard
链接: https://leetcode.cn/problems/arithmetic-slices-ii-subsequence/
题目类型: 数组、动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
446. 等差数列划分 II - 子序列 - 给你一个整数数组 nums ，返回 nums 中所有 等差子序列 的数目。 如果一个序列中 至少有三个元素 ，并且任意两个相邻元素之差相同，则称该序列为等差序列。 * 例如，[1, 3, 5, 7, 9]、[7, 7, 7, 7] 和 [3, -1, -5, -9] 都是等差序列。 * 再例如，[1, 1, 2, 5, 7] 不是等差序列。 数组中的子序列是从数组中删除一些元素（也可能不删除）得到的一个序列。 * 例如，[2,5,10] 是 [1,2,1,2,4,1,5,10] 的一个子序列。 题目数据保证答案是一个 32-bit 整数。 示例 1： 输入：nums = [2,4,6,8,10] 输出：7 解释：所有的等差子序列为： [2,4,6] [4,6,8] [6,8,10] [2,4,6,8] [4,6,8,10] [2,4,6,8,10] [2,6,10] 示例 2： 输入：nums = [7,7,7,7,7] 输出：16 解释：数组中的任意子序列都是等差子序列。 提示： * 1 <= nums.length <= 1000 * -231 <= nums[i] <= 231 - 1
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划来记录每个差值的子序列数量

算法步骤:
1. 初始化一个字典 `dp`，其中 `dp[i]` 是一个字典，记录以 `i` 结尾的等差子序列的数量。
2. 遍历数组 `nums`，对于每一对 `(i, j)`，计算差值 `diff = nums[j] - nums[i]`。
3. 更新 `dp[j][diff]`，将 `dp[i][diff]` 的值加到 `dp[j][diff]` 上，并将结果累加到总和 `total` 中。

关键点:
- 使用字典来存储每个差值的子序列数量，避免重复计算。
- 动态规划的状态转移方程为 `dp[j][diff] += dp[i][diff] + 1`。
- 注意边界条件，初始时 `dp[i]` 为空字典。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n^2) - 需要遍历每一对 (i, j)
空间复杂度: O(n^2) - `dp` 字典的大小最多为 n^2
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def arithmetic_slices_ii_subsequence(nums: List[int]) -> int:
    """
    函数式接口 - 计算等差子序列的数量
    
    实现思路:
    使用动态规划来记录每个差值的子序列数量。
    
    Args:
        nums: 整数数组
        
    Returns:
        等差子序列的数量
        
    Example:
        >>> arithmetic_slices_ii_subsequence([2,4,6,8,10])
        7
    """
    n = len(nums)
    if n < 3:
        return 0

    total = 0
    dp = [{} for _ in range(n)]

    for i in range(n):
        for j in range(i):
            diff = nums[i] - nums[j]
            count = dp[j].get(diff, 0)
            dp[i][diff] = dp[i].get(diff, 0) + count + 1
            total += count

    return total


# 自动生成Solution类（无需手动编写）
Solution = create_solution(arithmetic_slices_ii_subsequence)