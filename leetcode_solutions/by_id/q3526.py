# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3526
标题: Minimum Number of Flips to Make Binary Grid Palindromic I
难度: medium
链接: https://leetcode.cn/problems/minimum-number-of-flips-to-make-binary-grid-palindromic-i/
题目类型: 数组、双指针、矩阵
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3239. 最少翻转次数使二进制矩阵回文 I - 给你一个 m x n 的二进制矩阵 grid 。 如果矩阵中一行或者一列从前往后与从后往前读是一样的，那么我们称这一行或者这一列是 回文 的。 你可以将 grid 中任意格子的值 翻转 ，也就是将格子里的值从 0 变成 1 ，或者从 1 变成 0 。 请你返回 最少 翻转次数，使得矩阵 要么 所有行是 回文的 ，要么所有列是 回文的 。 示例 1： 输入：grid = [[1,0,0],[0,0,0],[0,0,1]] 输出：2 解释： [https://assets.leetcode.com/uploads/2024/07/07/screenshot-from-2024-07-08-00-20-10.png] 将高亮的格子翻转，得到所有行都是回文的。 示例 2： 输入：grid = [[0,1],[0,1],[0,0]] 输出：1 解释： [https://assets.leetcode.com/uploads/2024/07/07/screenshot-from-2024-07-08-00-31-23.png] 将高亮的格子翻转，得到所有列都是回文的。 示例 3： 输入：grid = [[1],[0]] 输出：0 解释： 所有行已经是回文的。 提示： * m == grid.length * n == grid[i].length * 1 <= m * n <= 2 * 105 * 0 <= grid[i][j] <= 1
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 计算每一行和每一列变成回文所需的最小翻转次数，然后取两者中的最小值。

算法步骤:
1. 定义一个辅助函数 `count_flips_to_palindrome`，用于计算将一个数组变成回文所需的最小翻转次数。
2. 分别计算所有行变成回文所需的总翻转次数和所有列变成回文所需的总翻转次数。
3. 返回两者中的最小值。

关键点:
- 使用双指针方法来计算将一个数组变成回文所需的最小翻转次数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n)，其中 m 是矩阵的行数，n 是矩阵的列数。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def count_flips_to_palindrome(arr: List[int]) -> int:
    """
    计算将一个数组变成回文所需的最小翻转次数。
    """
    flips = 0
    left, right = 0, len(arr) - 1
    while left < right:
        if arr[left] != arr[right]:
            flips += 1
        left += 1
        right -= 1
    return flips

def min_flips_to_make_grid_palindromic(grid: List[List[int]]) -> int:
    """
    计算最少翻转次数使二进制矩阵回文。
    """
    m, n = len(grid), len(grid[0])
    
    # 计算所有行变成回文所需的总翻转次数
    row_flips = sum(count_flips_to_palindrome(row) for row in grid)
    
    # 计算所有列变成回文所需的总翻转次数
    col_flips = sum(count_flips_to_palindrome([grid[i][j] for i in range(m)]) for j in range(n))
    
    # 返回两者中的最小值
    return min(row_flips, col_flips)

Solution = create_solution(min_flips_to_make_grid_palindromic)