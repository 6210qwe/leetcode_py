# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3195
标题: Separate Black and White Balls
难度: medium
链接: https://leetcode.cn/problems/separate-black-and-white-balls/
题目类型: 贪心、双指针、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2938. 区分黑球与白球 - 桌子上有 n 个球，每个球的颜色不是黑色，就是白色。 给你一个长度为 n 、下标从 0 开始的二进制字符串 s，其中 1 和 0 分别代表黑色和白色的球。 在每一步中，你可以选择两个相邻的球并交换它们。 返回「将所有黑色球都移到右侧，所有白色球都移到左侧所需的 最小步数」。 示例 1： 输入：s = "101" 输出：1 解释：我们可以按以下方式将所有黑色球移到右侧： - 交换 s[0] 和 s[1]，s = "011"。 最开始，1 没有都在右侧，需要至少 1 步将其移到右侧。 示例 2： 输入：s = "100" 输出：2 解释：我们可以按以下方式将所有黑色球移到右侧： - 交换 s[0] 和 s[1]，s = "010"。 - 交换 s[1] 和 s[2]，s = "001"。 可以证明所需的最小步数为 2 。 示例 3： 输入：s = "0111" 输出：0 解释：所有黑色球都已经在右侧。 提示： * 1 <= n == s.length <= 105 * s[i] 不是 '0'，就是 '1'。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用双指针方法，一个指针指向当前处理的白色球，另一个指针遍历字符串，计算需要的交换次数。

算法步骤:
1. 初始化两个指针 `i` 和 `j`，分别指向字符串的开头和第一个黑色球的位置。
2. 遍历字符串，当遇到白色球时，计算需要的交换次数，并更新指针 `i`。
3. 更新指针 `j`，使其始终指向下一个黑色球的位置。
4. 返回总交换次数。

关键点:
- 使用双指针可以高效地计算出最小交换次数。
- 通过一次遍历即可完成计算，时间复杂度为 O(n)。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(s: str) -> int:
    """
    函数式接口 - 计算将所有黑色球移到右侧，所有白色球移到左侧所需的最小步数
    """
    n = len(s)
    i, j = 0, 0
    swaps = 0
    
    while j < n:
        if s[j] == '0':
            swaps += j - i
            i += 1
        j += 1
    
    return swaps


Solution = create_solution(solution_function_name)