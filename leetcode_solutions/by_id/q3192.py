# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3192
标题: Maximum Xor Product
难度: medium
链接: https://leetcode.cn/problems/maximum-xor-product/
题目类型: 贪心、位运算、数学
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2939. 最大异或乘积 - 给你三个整数 a ，b 和 n ，请你返回 (a XOR x) * (b XOR x) 的 最大值 且 x 需要满足 0 <= x < 2n。 由于答案可能会很大，返回它对 109 + 7 取余 后的结果。 注意，XOR 是按位异或操作。 示例 1： 输入：a = 12, b = 5, n = 4 输出：98 解释：当 x = 2 时，(a XOR x) = 14 且 (b XOR x) = 7 。所以，(a XOR x) * (b XOR x) = 98 。 98 是所有满足 0 <= x < 2n 中 (a XOR x) * (b XOR x) 的最大值。 示例 2： 输入：a = 6, b = 7 , n = 5 输出：930 解释：当 x = 25 时，(a XOR x) = 31 且 (b XOR x) = 30 。所以，(a XOR x) * (b XOR x) = 930 。 930 是所有满足 0 <= x < 2n 中 (a XOR x) * (b XOR x) 的最大值。 示例 3： 输入：a = 1, b = 6, n = 3 输出：12 解释： 当 x = 5 时，(a XOR x) = 4 且 (b XOR x) = 3 。所以，(a XOR x) * (b XOR x) = 12 。 12 是所有满足 0 <= x < 2n 中 (a XOR x) * (b XOR x) 的最大值。 提示： * 0 <= a, b < 250 * 0 <= n <= 50
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过贪心算法和位运算来最大化 (a XOR x) * (b XOR x)。

算法步骤:
1. 初始化结果变量 res 为 0。
2. 从最高位到最低位遍历每一位。
3. 对于每一位，尝试将该位设置为 1，并计算 (a XOR x) * (b XOR x) 的值。
4. 如果新值大于当前的最大值，则更新最大值并设置该位为 1。
5. 返回最终结果并对 10^9 + 7 取模。

关键点:
- 通过位运算逐位优化 x 的值。
- 保证每次更新 x 时都能最大化 (a XOR x) * (b XOR x)。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution

MOD = 10**9 + 7

def solution_function_name(a: int, b: int, n: int) -> int:
    """
    函数式接口 - 返回 (a XOR x) * (b XOR x) 的最大值，x 满足 0 <= x < 2^n。
    """
    res = 0
    for i in range(n - 1, -1, -1):
        # 尝试将第 i 位设置为 1
        candidate = res | (1 << i)
        if (a ^ candidate) * (b ^ candidate) > (a ^ res) * (b ^ res):
            res = candidate
    return (a ^ res) * (b ^ res) % MOD

Solution = create_solution(solution_function_name)