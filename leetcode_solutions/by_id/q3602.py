# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3602
标题: Sum of Consecutive Subarrays
难度: medium
链接: https://leetcode.cn/problems/sum-of-consecutive-subarrays/
题目类型: 数组、双指针、动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3284. 连续子数组的和 - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用前缀和来计算连续子数组的和，并使用双指针优化计算过程。

算法步骤:
1. 计算前缀和数组。
2. 使用双指针遍历前缀和数组，计算每个子数组的和。
3. 将所有子数组的和累加起来。

关键点:
- 前缀和数组可以快速计算任意子数组的和。
- 双指针可以避免重复计算，提高效率。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n^2)
空间复杂度: O(n)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(nums: List[int]) -> int:
    """
    函数式接口 - 计算所有连续子数组的和
    """
    n = len(nums)
    if n == 0:
        return 0

    # 计算前缀和数组
    prefix_sum = [0] * (n + 1)
    for i in range(1, n + 1):
        prefix_sum[i] = prefix_sum[i - 1] + nums[i - 1]

    total_sum = 0

    # 使用双指针遍历前缀和数组，计算每个子数组的和
    for i in range(n):
        for j in range(i + 1, n + 1):
            total_sum += prefix_sum[j] - prefix_sum[i]

    return total_sum


Solution = create_solution(solution_function_name)