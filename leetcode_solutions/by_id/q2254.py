# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2254
标题: Check if Every Row and Column Contains All Numbers
难度: easy
链接: https://leetcode.cn/problems/check-if-every-row-and-column-contains-all-numbers/
题目类型: 数组、哈希表、矩阵
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2133. 检查是否每一行每一列都包含全部整数 - 对一个大小为 n x n 的矩阵而言，如果其每一行和每一列都包含从 1 到 n 的 全部 整数（含 1 和 n），则认为该矩阵是一个 有效 矩阵。 给你一个大小为 n x n 的整数矩阵 matrix ，请你判断矩阵是否为一个有效矩阵：如果是，返回 true ；否则，返回 false 。 示例 1： [https://assets.leetcode.com/uploads/2021/12/21/example1drawio.png] 输入：matrix = [[1,2,3],[3,1,2],[2,3,1]] 输出：true 解释：在此例中，n = 3 ，每一行和每一列都包含数字 1、2、3 。 因此，返回 true 。 示例 2： [https://assets.leetcode.com/uploads/2021/12/21/example2drawio.png] 输入：matrix = [[1,1,1],[1,2,3],[1,2,3]] 输出：false 解释：在此例中，n = 3 ，但第一行和第一列不包含数字 2 和 3 。 因此，返回 false 。 提示： * n == matrix.length == matrix[i].length * 1 <= n <= 100 * 1 <= matrix[i][j] <= n
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用集合来检查每一行和每一列是否包含从 1 到 n 的所有整数。

算法步骤:
1. 初始化两个列表，分别用于存储每一行和每一列的集合。
2. 遍历矩阵，将每一行和每一列的元素添加到对应的集合中。
3. 检查每一行和每一列的集合是否包含从 1 到 n 的所有整数。

关键点:
- 使用集合来高效地检查每一行和每一列是否包含从 1 到 n 的所有整数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n^2)
空间复杂度: O(n)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def check_valid_matrix(matrix: List[List[int]]) -> bool:
    """
    检查矩阵是否每一行每一列都包含从 1 到 n 的所有整数。
    
    :param matrix: 大小为 n x n 的整数矩阵
    :return: 如果矩阵是有效的，返回 True；否则，返回 False
    """
    n = len(matrix)
    required_set = set(range(1, n + 1))
    
    for i in range(n):
        row_set = set()
        col_set = set()
        for j in range(n):
            row_set.add(matrix[i][j])
            col_set.add(matrix[j][i])
        
        if row_set != required_set or col_set != required_set:
            return False
    
    return True

Solution = create_solution(check_valid_matrix)