# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 916
标题: Decoded String at Index
难度: medium
链接: https://leetcode.cn/problems/decoded-string-at-index/
题目类型: 栈、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
880. 索引处的解码字符串 - 给定一个编码字符串 s 。请你找出 解码字符串 并将其写入磁带。解码时，从编码字符串中 每次读取一个字符 ，并采取以下步骤： * 如果所读的字符是字母，则将该字母写在磁带上。 * 如果所读的字符是数字（例如 d），则整个当前磁带总共会被重复写 d-1 次。 现在，对于给定的编码字符串 s 和索引 k，查找并返回解码字符串中的第 k 个字母。 示例 1： 输入：s = "leet2code3", k = 10 输出："o" 解释： 解码后的字符串为 "leetleetcodeleetleetcodeleetleetcode"。 字符串中的第 10 个字母是 "o"。 示例 2： 输入：s = "ha22", k = 5 输出："h" 解释： 解码后的字符串为 "hahahaha"。第 5 个字母是 "h"。 示例 3： 输入：s = "a2345678999999999999999", k = 1 输出："a" 解释： 解码后的字符串为 "a" 重复 8301530446056247680 次。第 1 个字母是 "a"。 提示： * 2 <= s.length <= 100 * s 只包含小写字母与数字 2 到 9 。 * s 以字母开头。 * 1 <= k <= 109 * 题目保证 k 小于或等于解码字符串的长度。 * 解码后的字符串保证少于 263 个字母。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过逆向遍历编码字符串，逐步缩小解码字符串的长度，直到找到第 k 个字符。

算法步骤:
1. 计算解码字符串的总长度。
2. 逆向遍历编码字符串，逐步缩小解码字符串的长度，直到找到第 k 个字符。

关键点:
- 通过逆向遍历避免生成完整的解码字符串，从而优化时间和空间复杂度。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是编码字符串的长度。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def decoded_string_at_index(s: str, k: int) -> str:
    """
    函数式接口 - 找到解码字符串中的第 k 个字符
    """
    size = 0
    # 计算解码字符串的总长度
    for char in s:
        if char.isdigit():
            size *= int(char)
        else:
            size += 1
    
    # 逆向遍历编码字符串
    for char in reversed(s):
        k %= size
        if k == 0 and char.isalpha():
            return char
        
        if char.isdigit():
            size //= int(char)
        else:
            size -= 1

Solution = create_solution(decoded_string_at_index)