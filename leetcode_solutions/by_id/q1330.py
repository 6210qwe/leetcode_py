# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1330
标题: Longest Arithmetic Subsequence of Given Difference
难度: medium
链接: https://leetcode.cn/problems/longest-arithmetic-subsequence-of-given-difference/
题目类型: 数组、哈希表、动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1218. 最长定差子序列 - 给你一个整数数组 arr 和一个整数 difference，请你找出并返回 arr 中最长等差子序列的长度，该子序列中相邻元素之间的差等于 difference 。 子序列 是指在不改变其余元素顺序的情况下，通过删除一些元素或不删除任何元素而从 arr 派生出来的序列。 示例 1： 输入：arr = [1,2,3,4], difference = 1 输出：4 解释：最长的等差子序列是 [1,2,3,4]。 示例 2： 输入：arr = [1,3,5,7], difference = 1 输出：1 解释：最长的等差子序列是任意单个元素。 示例 3： 输入：arr = [1,5,7,8,5,3,4,2,1], difference = -2 输出：4 解释：最长的等差子序列是 [7,5,3,1]。 提示： * 1 <= arr.length <= 105 * -104 <= arr[i], difference <= 104
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用哈希表记录每个元素作为结尾时的最长等差子序列长度。

算法步骤:
1. 初始化一个字典 `dp`，键为数组中的元素，值为以该元素结尾的最长等差子序列的长度。
2. 遍历数组中的每个元素 `num`，检查 `num - difference` 是否在 `dp` 中。
3. 如果 `num - difference` 在 `dp` 中，则更新 `dp[num]` 为 `dp[num - difference] + 1`。
4. 如果 `num - difference` 不在 `dp` 中，则将 `dp[num]` 设为 1。
5. 记录遍历过程中 `dp` 中的最大值，即为最长等差子序列的长度。

关键点:
- 使用哈希表来存储每个元素作为结尾时的最长等差子序列长度，从而实现 O(1) 时间复杂度的查找和更新。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是数组的长度。我们只需要遍历数组一次。
空间复杂度: O(n)，最坏情况下，哈希表需要存储数组中的所有元素。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def longest_subsequence(arr: List[int], difference: int) -> int:
    """
    函数式接口 - 找出并返回 arr 中最长等差子序列的长度，该子序列中相邻元素之间的差等于 difference。
    """
    dp = {}
    max_length = 0
    
    for num in arr:
        if num - difference in dp:
            dp[num] = dp[num - difference] + 1
        else:
            dp[num] = 1
        max_length = max(max_length, dp[num])
    
    return max_length

Solution = create_solution(longest_subsequence)