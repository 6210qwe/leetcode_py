# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1103
标题: Moving Stones Until Consecutive
难度: medium
链接: https://leetcode.cn/problems/moving-stones-until-consecutive/
题目类型: 脑筋急转弯、数学
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1033. 移动石子直到连续 - 三枚石子放置在数轴上，位置分别为 a，b，c。 每一回合，你可以从两端之一拿起一枚石子（位置最大或最小），并将其放入两端之间的任一空闲位置。形式上，假设这三枚石子当前分别位于位置 x, y, z 且 x < y < z。那么就可以从位置 x 或者是位置 z 拿起一枚石子，并将该石子移动到某一整数位置 k 处，其中 x < k < z 且 k != y。 当你无法进行任何移动时，即，这些石子的位置连续时，游戏结束。 要使游戏结束，你可以执行的最小和最大移动次数分别是多少？ 以长度为 2 的数组形式返回答案：answer = [minimum_moves, maximum_moves] 示例 1： 输入：a = 1, b = 2, c = 5 输出：[1, 2] 解释：将石子从 5 移动到 4 再移动到 3，或者我们可以直接将石子移动到 3。 示例 2： 输入：a = 4, b = 3, c = 2 输出：[0, 0] 解释：我们无法进行任何移动。 提示： 1. 1 <= a <= 100 2. 1 <= b <= 100 3. 1 <= c <= 100 4. a != b, b != c, c != a
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过排序和计算间隔来确定最小和最大移动次数。

算法步骤:
1. 对输入的三个位置进行排序。
2. 计算最小移动次数：
   - 如果两个石子之间的间隔小于等于2，则最小移动次数为1。
   - 否则，检查是否存在一个石子可以一步移动到其他两个石子之间。
3. 计算最大移动次数：
   - 最大移动次数为两个最远石子之间的间隔减去1。

关键点:
- 通过排序简化了位置关系的处理。
- 通过计算间隔来确定最小和最大移动次数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(1) - 因为只涉及常数次操作。
空间复杂度: O(1) - 只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def moving_stones_until_consecutive(a: int, b: int, c: int) -> List[int]:
    """
    函数式接口 - 计算使石子连续所需的最小和最大移动次数
    """
    # 对输入的三个位置进行排序
    stones = sorted([a, b, c])
    
    # 计算最小移动次数
    if stones[1] - stones[0] == 1 and stones[2] - stones[1] == 1:
        min_moves = 0
    elif stones[1] - stones[0] <= 2 or stones[2] - stones[1] <= 2:
        min_moves = 1
    else:
        min_moves = 2
    
    # 计算最大移动次数
    max_moves = stones[2] - stones[0] - 2
    
    return [min_moves, max_moves]


Solution = create_solution(moving_stones_until_consecutive)