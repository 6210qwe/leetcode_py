# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1933
标题: Number of Different Integers in a String
难度: easy
链接: https://leetcode.cn/problems/number-of-different-integers-in-a-string/
题目类型: 哈希表、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1805. 字符串中不同整数的数目 - 给你一个字符串 word ，该字符串由数字和小写英文字母组成。 请你用空格替换每个不是数字的字符。例如，"a123bc34d8ef34" 将会变成 " 123 34 8 34" 。注意，剩下的这些整数为（相邻彼此至少有一个空格隔开）："123"、"34"、"8" 和 "34" 。 返回对 word 完成替换后形成的 不同 整数的数目。 只有当两个整数的 不含前导零 的十进制表示不同， 才认为这两个整数也不同。 示例 1： 输入：word = "a123bc34d8ef34" 输出：3 解释：不同的整数有 "123"、"34" 和 "8" 。注意，"34" 只计数一次。 示例 2： 输入：word = "leet1234code234" 输出：2 示例 3： 输入：word = "a1b01c001" 输出：1 解释："1"、"01" 和 "001" 视为同一个整数的十进制表示，因为在比较十进制值时会忽略前导零的存在。 提示： * 1 <= word.length <= 1000 * word 由数字和小写英文字母组成
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用正则表达式提取所有数字，并使用集合去重。

算法步骤:
1. 使用正则表达式匹配所有连续的数字。
2. 对每个匹配到的数字，去掉前导零并转换为整数。
3. 使用集合去重，返回集合的大小。

关键点:
- 使用正则表达式提取数字。
- 去掉前导零以确保唯一性。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串的长度。正则表达式匹配和遍历字符串的时间复杂度都是 O(n)。
空间复杂度: O(m)，其中 m 是不同整数的数量。集合的空间复杂度是 O(m)。
"""

# ============================================================================
# 代码实现
# ============================================================================

import re
from typing import Set

def num_different_integers(word: str) -> int:
    """
    函数式接口 - 返回字符串中不同整数的数目
    """
    # 使用正则表达式匹配所有连续的数字
    numbers = re.findall(r'\d+', word)
    
    # 去掉前导零并转换为整数
    unique_numbers = {int(num) for num in numbers}
    
    # 返回不同整数的数量
    return len(unique_numbers)

Solution = create_solution(num_different_integers)