# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2498
标题: Smallest Subarrays With Maximum Bitwise OR
难度: medium
链接: https://leetcode.cn/problems/smallest-subarrays-with-maximum-bitwise-or/
题目类型: 位运算、数组、二分查找、滑动窗口
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2411. 按位或最大的最小子数组长度 - 给你一个长度为 n 下标从 0 开始的数组 nums ，数组中所有数字均为非负整数。对于 0 到 n - 1 之间的每一个下标 i ，你需要找出 nums 中一个 最小 非空子数组，它的起始位置为 i （包含这个位置），同时有 最大 的 按位或运算值 。 * 换言之，令 Bij 表示子数组 nums[i...j] 的按位或运算的结果，你需要找到一个起始位置为 i 的最小子数组，这个子数组的按位或运算的结果等于 max(Bik) ，其中 i <= k <= n - 1 。 一个数组的按位或运算值是这个数组里所有数字按位或运算的结果。 请你返回一个大小为 n 的整数数组 answer，其中 answer[i]是开始位置为 i ，按位或运算结果最大，且 最短 子数组的长度。 子数组 是数组里一段连续非空元素组成的序列。 示例 1： 输入：nums = [1,0,2,1,3] 输出：[3,3,2,2,1] 解释： 任何位置开始，最大按位或运算的结果都是 3 。 - 下标 0 处，能得到结果 3 的最短子数组是 [1,0,2] 。 - 下标 1 处，能得到结果 3 的最短子数组是 [0,2,1] 。 - 下标 2 处，能得到结果 3 的最短子数组是 [2,1] 。 - 下标 3 处，能得到结果 3 的最短子数组是 [1,3] 。 - 下标 4 处，能得到结果 3 的最短子数组是 [3] 。 所以我们返回 [3,3,2,2,1] 。 示例 2： 输入：nums = [1,2] 输出：[2,1] 解释： 下标 0 处，能得到最大按位或运算值的最短子数组长度为 2 。 下标 1 处，能得到最大按位或运算值的最短子数组长度为 1 。 所以我们返回 [2,1] 。 提示： * n == nums.length * 1 <= n <= 105 * 0 <= nums[i] <= 109
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用倒序遍历和位运算来找到每个位置的最短子数组。

算法步骤:
1. 从右到左遍历数组，维护一个当前的最大按位或值。
2. 对于每个位置 i，更新当前的最大按位或值，并计算从 i 开始的最短子数组长度。
3. 使用一个字典来记录每个位最后出现的位置，以便快速跳过不需要的位。

关键点:
- 倒序遍历可以确保我们总是知道从当前位置到数组末尾的最大按位或值。
- 使用字典记录每个位最后出现的位置，可以快速跳过不需要的位。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1)（不考虑输出数组的空间）
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def smallest_subarrays(nums: List[int]) -> List[int]:
    n = len(nums)
    last_occurrence = [0] * 32  # 记录每个位最后出现的位置
    result = [1] * n  # 初始化结果数组

    for i in range(n - 1, -1, -1):
        for j in range(32):
            if nums[i] & (1 << j):
                last_occurrence[j] = i  # 更新位 j 最后出现的位置
        max_index = max(last_occurrence)  # 当前位置 i 的最短子数组结束位置
        result[i] = max_index - i + 1  # 计算最短子数组长度

    return result

Solution = create_solution(smallest_subarrays)