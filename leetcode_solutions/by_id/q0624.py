# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 624
标题: Maximum Distance in Arrays
难度: medium
链接: https://leetcode.cn/problems/maximum-distance-in-arrays/
题目类型: 贪心、数组
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
624. 数组列表中的最大距离 - 给定 m 个数组，每个数组都已经按照升序排好序了。 现在你需要从两个不同的数组中选择两个整数（每个数组选一个）并且计算它们的距离。两个整数 a 和 b 之间的距离定义为它们差的绝对值 |a-b| 。 返回最大距离。 示例 1： 输入：[[1,2,3],[4,5],[1,2,3]] 输出：4 解释： 一种得到答案 4 的方法是从第一个数组或者第三个数组中选择 1，同时从第二个数组中选择 5 。 示例 2： 输入：arrays = [[1],[1]] 输出：0 提示： * m == arrays.length * 2 <= m <= 105 * 1 <= arrays[i].length <= 500 * -104 <= arrays[i][j] <= 104 * arrays[i] 以 升序 排序。 * 所有数组中最多有 105 个整数。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过遍历每个数组的最小值和最大值来找到最大距离。

算法步骤:
1. 初始化全局最小值和最大值及其对应的数组索引。
2. 遍历每个数组，更新全局最小值和最大值。
3. 计算当前数组的最大值与全局最小值的差值，以及当前数组的最小值与全局最大值的差值，取两者中的最大值作为当前最大距离。
4. 更新全局最小值和最大值。
5. 返回最大距离。

关键点:
- 通过一次遍历即可找到最大距离，避免了两两比较的时间复杂度。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m)，其中 m 是数组的数量。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def max_distance(arrays: List[List[int]]) -> int:
    """
    函数式接口 - 实现数组列表中的最大距离
    """
    if not arrays or len(arrays) < 2:
        return 0

    # 初始化全局最小值和最大值及其对应的数组索引
    min_val, max_val = arrays[0][0], arrays[0][-1]
    max_distance = 0

    # 遍历每个数组
    for i in range(1, len(arrays)):
        current_min, current_max = arrays[i][0], arrays[i][-1]

        # 计算当前数组的最大值与全局最小值的差值，以及当前数组的最小值与全局最大值的差值
        max_distance = max(max_distance, abs(current_max - min_val), abs(max_val - current_min))

        # 更新全局最小值和最大值
        min_val = min(min_val, current_min)
        max_val = max(max_val, current_max)

    return max_distance


Solution = create_solution(max_distance)