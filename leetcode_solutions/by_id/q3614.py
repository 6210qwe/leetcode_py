# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3614
标题: Find the Number of K-Even Arrays
难度: medium
链接: https://leetcode.cn/problems/find-the-number-of-k-even-arrays/
题目类型: 动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3339. 查找 K 偶数数组的数量 - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划来解决这个问题。我们定义 dp[i][j] 表示前 i 个元素中，有 j 个偶数的方案数。

算法步骤:
1. 初始化一个二维数组 dp，其中 dp[i][j] 表示前 i 个元素中，有 j 个偶数的方案数。
2. 遍历每个元素，根据当前元素是奇数还是偶数，更新 dp 数组。
3. 最后返回 dp[n][k]，即前 n 个元素中有 k 个偶数的方案数。

关键点:
- 使用动态规划来避免重复计算。
- 通过状态转移方程来更新 dp 数组。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n * k)，其中 n 是数组的长度，k 是偶数的数量。
空间复杂度: O(n * k)，用于存储 dp 数组。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def solution_function_name(nums: List[int], k: int) -> int:
    """
    函数式接口 - 计算数组中恰好有 k 个偶数的子数组数量
    """
    n = len(nums)
    if n == 0 or k < 0 or k > n:
        return 0
    
    # 初始化 dp 数组
    dp = [[0] * (k + 1) for _ in range(n + 1)]
    dp[0][0] = 1  # 空数组有 0 个偶数的方案数为 1
    
    for i in range(1, n + 1):
        for j in range(k + 1):
            if nums[i - 1] % 2 == 0:
                if j > 0:
                    dp[i][j] = dp[i - 1][j - 1] + dp[i - 1][j]
                else:
                    dp[i][j] = dp[i - 1][j]
            else:
                dp[i][j] = dp[i - 1][j]
    
    return dp[n][k]

Solution = create_solution(solution_function_name)