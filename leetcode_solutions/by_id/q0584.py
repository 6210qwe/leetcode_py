# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 584
标题: Find Customer Referee
难度: easy
链接: https://leetcode.cn/problems/find-customer-referee/
题目类型: 数据库
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
584. 寻找用户推荐人 - 表: Customer +-------------+---------+ | Column Name | Type | +-------------+---------+ | id | int | | name | varchar | | referee_id | int | +-------------+---------+ 在 SQL 中，id 是该表的主键列。 该表的每一行表示一个客户的 id、姓名以及推荐他们的客户的 id。 找出以下客户的姓名： 1. 被任何 id != 2 的用户推荐。 2. 没有被 任何用户推荐。 以 任意顺序 返回结果表。 结果格式如下所示。 示例 1： 输入： Customer 表: +----+------+------------+ | id | name | referee_id | +----+------+------------+ | 1 | Will | null | | 2 | Jane | null | | 3 | Alex | 2 | | 4 | Bill | null | | 5 | Zack | 1 | | 6 | Mark | 2 | +----+------+------------+ 输出： +------+ | name | +------+ | Will | | Jane | | Bill | | Zack | +------+
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用 Pandas 库来处理数据，筛选出符合条件的客户姓名。

算法步骤:
1. 读取输入的 DataFrame。
2. 筛选出 `referee_id` 不等于 2 或者为 `null` 的记录。
3. 提取这些记录中的 `name` 列并返回。

关键点:
- 使用 Pandas 的条件筛选功能来实现高效的查询。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是 Customer 表的行数。因为我们需要遍历整个表来筛选符合条件的记录。
空间复杂度: O(1)，除了输入和输出外，我们只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

import pandas as pd


def find_customer_referee(customers: pd.DataFrame) -> pd.DataFrame:
    """
    函数式接口 - 使用 Pandas 处理数据，筛选出符合条件的客户姓名。
    """
    # 筛选出 referee_id 不等于 2 或者为 null 的记录
    filtered_customers = customers[(customers['referee_id'] != 2) | (customers['referee_id'].isnull())]
    
    # 提取这些记录中的 name 列并返回
    result = filtered_customers[['name']]
    
    return result


Solution = create_solution(find_customer_referee)