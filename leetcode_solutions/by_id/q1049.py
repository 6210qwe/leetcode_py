# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1049
标题: Minimum Domino Rotations For Equal Row
难度: medium
链接: https://leetcode.cn/problems/minimum-domino-rotations-for-equal-row/
题目类型: 贪心、数组
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1007. 行相等的最少多米诺旋转 - 在一排多米诺骨牌中，tops[i] 和 bottoms[i] 分别代表第 i 个多米诺骨牌的上半部分和下半部分。（一个多米诺是两个从 1 到 6 的数字同列平铺形成的 —— 该平铺的每一半上都有一个数字。） 我们可以旋转第 i 张多米诺，使得 tops[i] 和 bottoms[i] 的值交换。 返回能使 tops 中所有值或者 bottoms 中所有值都相同的最小旋转次数。 如果无法做到，返回 -1. 示例 1： [https://assets.leetcode.com/uploads/2021/05/14/domino.png] 输入：tops = [2,1,2,4,2,2], bottoms = [5,2,6,2,3,2] 输出：2 解释： 图一表示：在我们旋转之前， tops 和 bottoms 给出的多米诺牌。 如果我们旋转第二个和第四个多米诺骨牌，我们可以使上面一行中的每个值都等于 2，如图二所示。 示例 2： 输入：tops = [3,5,1,2,3], bottoms = [3,6,3,3,4] 输出：-1 解释： 在这种情况下，不可能旋转多米诺牌使一行的值相等。 提示： * 2 <= tops.length <= 2 * 104 * bottoms.length == tops.length * 1 <= tops[i], bottoms[i] <= 6
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过贪心算法找到可以使所有多米诺骨牌上半部分或下半部分相等的最小旋转次数。

算法步骤:
1. 检查第一个多米诺骨牌的上下两面的值（A[0] 和 B[0]），这两个值是我们可能的目标值。
2. 对于每个目标值，计算将其作为所有多米诺骨牌的上半部分或下半部分所需的最小旋转次数。
3. 如果某个目标值可以使所有多米诺骨牌的上半部分或下半部分相等，则返回最小旋转次数；否则返回 -1。

关键点:
- 通过计数器来记录每个多米诺骨牌的上半部分和下半部分的值出现次数。
- 使用贪心策略，选择最小的旋转次数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def min_domino_rotations(tops: List[int], bottoms: List[int]) -> int:
    def check(x: int) -> int:
        rotations_a = rotations_b = 0
        for i in range(len(tops)):
            if tops[i] != x and bottoms[i] != x:
                return -1
            elif tops[i] != x:
                rotations_a += 1
            elif bottoms[i] != x:
                rotations_b += 1
        return min(rotations_a, rotations_b)

    rotations = check(tops[0])
    if rotations != -1 or tops[0] == bottoms[0]:
        return rotations
    else:
        return check(bottoms[0])

Solution = create_solution(min_domino_rotations)