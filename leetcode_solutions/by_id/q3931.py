# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3931
标题: Process String with Special Operations I
难度: medium
链接: https://leetcode.cn/problems/process-string-with-special-operations-i/
题目类型: 字符串、模拟
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3612. 用特殊操作处理字符串 I - 给你一个字符串 s，它由小写英文字母和特殊字符：*、# 和 % 组成。 请根据以下规则从左到右处理 s 中的字符，构造一个新的字符串 result： * 如果字符是 小写 英文字母，则将其添加到 result 中。 * 字符 '*' 会 删除 result 中的最后一个字符（如果存在）。 * 字符 '#' 会 复制 当前的 result 并 追加 到其自身后面。 * 字符 '%' 会 反转 当前的 result。 在处理完 s 中的所有字符后，返回最终的字符串 result。 示例 1： 输入： s = "a#b%*" 输出： "ba" 解释： i s[i] 操作 当前 result 0 'a' 添加 'a' "a" 1 '#' 复制 result "aa" 2 'b' 添加 'b' "aab" 3 '%' 反转 result "baa" 4 '*' 删除最后一个字符 "ba" 因此，最终的 result 是 "ba"。 示例 2： 输入： s = "z*#" 输出： "" 解释： i s[i] 操作 当前 result 0 'z' 添加 'z' "z" 1 '*' 删除最后一个字符 "" 2 '#' 复制字符串 "" 因此，最终的 result 是 ""。 提示: * 1 <= s.length <= 20 * s 只包含小写英文字母和特殊字符 *、# 和 %。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用栈来处理字符串中的特殊字符，并根据规则更新结果字符串。

算法步骤:
1. 初始化一个空的结果字符串 `result`。
2. 遍历输入字符串 `s` 的每个字符：
   - 如果字符是小写字母，将其添加到 `result` 中。
   - 如果字符是 `*`，删除 `result` 的最后一个字符（如果存在）。
   - 如果字符是 `#`，将当前 `result` 复制并追加到其自身后面。
   - 如果字符是 `%`，反转当前 `result`。
3. 返回最终的 `result`。

关键点:
- 使用栈的思想来处理字符串中的特殊字符。
- 根据不同的特殊字符执行相应的操作。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串 s 的长度。我们只需要遍历一次字符串。
空间复杂度: O(n)，最坏情况下，结果字符串 `result` 的长度可能与输入字符串 `s` 的长度相同。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def process_string(s: str) -> str:
    """
    函数式接口 - 处理包含特殊操作的字符串
    """
    result = []
    
    for char in s:
        if char.islower():
            result.append(char)
        elif char == '*':
            if result:
                result.pop()
        elif char == '#':
            result.extend(result)
        elif char == '%':
            result.reverse()
    
    return ''.join(result)


Solution = create_solution(process_string)