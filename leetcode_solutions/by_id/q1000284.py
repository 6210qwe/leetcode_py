# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1000284
标题: 最大矩形
难度: hard
链接: https://leetcode.cn/problems/PLYXKQ/
题目类型: 栈、数组、动态规划、矩阵、单调栈
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
LCR 040. 最大矩形 - 给定一个由 0 和 1 组成的矩阵 matrix ，找出只包含 1 的最大矩形，并返回其面积。 注意：此题 matrix 输入格式为一维 01 字符串数组。 示例 1： [https://assets.leetcode.com/uploads/2020/09/14/maximal.jpg] 输入：matrix = ["10100","10111","11111","10010"] 输出：6 解释：最大矩形如上图所示。 示例 2： 输入：matrix = [] 输出：0 示例 3： 输入：matrix = ["0"] 输出：0 示例 4： 输入：matrix = ["1"] 输出：1 示例 5： 输入：matrix = ["00"] 输出：0 提示： * rows == matrix.length * cols == matrix[0].length * 0 <= row, cols <= 200 * matrix[i][j] 为 '0' 或 '1' 注意：本题与主站 85 题相同（输入参数格式不同）： https://leetcode.cn/problems/maximal-rectangle/ [https://leetcode.cn/problems/maximal-rectangle/]
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 将每一行看作直方图的底，通过计算每一行的直方图的最大矩形面积来求解整个矩阵的最大矩形面积。

算法步骤:
1. 初始化一个高度数组 `heights`，用于存储每一列的高度。
2. 遍历每一行，更新 `heights` 数组。
3. 对于每一行，使用单调栈计算当前直方图的最大矩形面积。
4. 更新最大矩形面积。

关键点:
- 使用单调栈来计算直方图的最大矩形面积。
- 每一行的高度数组可以通过前一行的高度数组和当前行的值来更新。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n)，其中 m 是矩阵的行数，n 是矩阵的列数。
空间复杂度: O(n)，需要一个额外的高度数组来存储每一列的高度。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def maximal_rectangle(matrix: List[str]) -> int:
    if not matrix or not matrix[0]:
        return 0

    m, n = len(matrix), len(matrix[0])
    heights = [0] * (n + 1)
    max_area = 0

    for row in matrix:
        for i in range(n):
            heights[i] = heights[i] + 1 if row[i] == '1' else 0

        stack = [-1]
        for i in range(n + 1):
            while heights[i] < heights[stack[-1]]:
                h = heights[stack.pop()]
                w = i - 1 - stack[-1]
                max_area = max(max_area, h * w)
            stack.append(i)

    return max_area

Solution = create_solution(maximal_rectangle)