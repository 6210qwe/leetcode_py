# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3792
标题: Find Invalid IP Addresses
难度: hard
链接: https://leetcode.cn/problems/find-invalid-ip-addresses/
题目类型: 数据库
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3451. 查找无效的 IP 地址 - 表：logs +-------------+---------+ | Column Name | Type | +-------------+---------+ | log_id | int | | ip | varchar | | status_code | int | +-------------+---------+ log_id 是这张表的唯一主键。 每一行包含服务器访问日志信息，包括 IP 地址和 HTTP 状态码。 编写一个解决方案来查找 无效的 IP 地址。一个 IPv4 地址如果满足以下任何条件之一，则无效： * 任何 8 位字节中包含大于 255 的数字 * 任何 8 位字节中含有 前导零（如 01.02.03.04） * 少于或多于 4 个 8 位字节 返回结果表分别以 invalid_count，ip 降序 排序。 结果格式如下所示。 示例： 输入： logs 表： +--------+---------------+-------------+ | log_id | ip | status_code | +--------+---------------+-------------+ | 1 | 192.168.1.1 | 200 | | 2 | 256.1.2.3 | 404 | | 3 | 192.168.001.1 | 200 | | 4 | 192.168.1.1 | 200 | | 5 | 192.168.1 | 500 | | 6 | 256.1.2.3 | 404 | | 7 | 192.168.001.1 | 200 | +--------+---------------+-------------+ 输出： +---------------+--------------+ | ip | invalid_count| +---------------+--------------+ | 256.1.2.3 | 2 | | 192.168.001.1 | 2 | | 192.168.1 | 1 | +---------------+--------------+ 解释： * 256.1.2.3 是无效的，因为 256 > 255 * 192.168.001.1 是无效的，因为有前导零 * 192.168.1 是非法的，因为只有 3 个 8 位字节 输出表分别以 invalid_count，ip 降序排序。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想:
- 使用正则表达式来匹配无效的 IP 地址。
- 对每个 IP 地址进行验证，检查是否符合无效的条件。
- 统计每个无效 IP 地址的出现次数，并按要求排序。

算法步骤:
1. 使用正则表达式匹配无效的 IP 地址。
2. 对每个 IP 地址进行验证，检查是否符合无效的条件。
3. 统计每个无效 IP 地址的出现次数。
4. 按照 invalid_count 和 ip 降序排序。

关键点:
- 使用正则表达式来高效地匹配无效的 IP 地址。
- 使用 Pandas 库来处理数据和统计。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n * m)，其中 n 是 logs 表的行数，m 是 IP 地址的平均长度。
空间复杂度: O(n)，用于存储无效 IP 地址的计数。
"""

# ============================================================================
# 代码实现
# ============================================================================

import pandas as pd
import re

def solution_function_name(logs: pd.DataFrame) -> pd.DataFrame:
    """
    函数式接口 - 查找无效的 IP 地址
    """
    # 定义正则表达式来匹配无效的 IP 地址
    invalid_ip_pattern = re.compile(r'^(?:(?!0)(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?!0)(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$')
    
    # 过滤出无效的 IP 地址
    invalid_ips = logs[~logs['ip'].str.match(invalid_ip_pattern)]
    
    # 统计每个无效 IP 地址的出现次数
    invalid_count = invalid_ips['ip'].value_counts().reset_index()
    invalid_count.columns = ['ip', 'invalid_count']
    
    # 按照 invalid_count 和 ip 降序排序
    result = invalid_count.sort_values(by=['invalid_count', 'ip'], ascending=[False, False])
    
    return result

Solution = create_solution(solution_function_name)