# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 4126
标题: Minimum Number of Flips to Reverse Binary String
难度: easy
链接: https://leetcode.cn/problems/minimum-number-of-flips-to-reverse-binary-string/
题目类型: 位运算、数学、双指针、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3750. 最少反转次数得到翻转二进制字符串 - 给你一个 正 整数 n。 令 s 为 n 的 二进制表示（不含前导零）。 二进制字符串 s 的 反转 是指将 s 中的字符按相反顺序排列得到的字符串。 你可以翻转 s 中的任意一位（将 0 → 1 或 1 → 0）。每次翻转 仅 影响一位。 请返回使 s 等于其原始形式的反转所需的 最少 翻转次数。 示例 1： 输入： n = 7 输出： 0 解释： 7 的二进制表示为 "111"。其反转也是 "111"，两者相同。因此，不需要翻转。 示例 2： 输入： n = 10 输出： 4 解释： 10 的二进制表示为 "1010"。其反转为 "0101"。必须翻转所有四位才能使它们相等。因此，所需的最少翻转次数为 4。 提示： * 1 <= n <= 109
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过比较原二进制字符串和其反转后的字符串，计算出需要翻转的位数。

算法步骤:
1. 将整数 n 转换为二进制字符串，并去除前导零。
2. 反转该二进制字符串。
3. 比较原二进制字符串和反转后的字符串，统计不同位的数量。

关键点:
- 使用 Python 的内置函数 bin() 和 [::-1] 来处理二进制字符串的转换和反转。
- 通过逐位比较来计算需要翻转的位数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(log n) - 二进制字符串的长度与 log n 成正比。
空间复杂度: O(log n) - 存储二进制字符串所需的空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def min_flips_to_reverse_binary_string(n: int) -> int:
    """
    函数式接口 - 计算使二进制字符串等于其反转所需的最少翻转次数
    """
    # 将整数 n 转换为二进制字符串，并去除前导零
    binary_str = bin(n)[2:]
    
    # 反转该二进制字符串
    reversed_str = binary_str[::-1]
    
    # 比较原二进制字符串和反转后的字符串，统计不同位的数量
    flips = sum(1 for a, b in zip(binary_str, reversed_str) if a != b)
    
    return flips


Solution = create_solution(min_flips_to_reverse_binary_string)