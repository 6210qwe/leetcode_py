# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2882
标题: Ways to Express an Integer as Sum of Powers
难度: medium
链接: https://leetcode.cn/problems/ways-to-express-an-integer-as-sum-of-powers/
题目类型: 动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2787. 将一个数字表示成幂的和的方案数 - 给你两个 正 整数 n 和 x 。 请你返回将 n 表示成一些 互不相同 正整数的 x 次幂之和的方案数。换句话说，你需要返回互不相同整数 [n1, n2, ..., nk] 的集合数目，满足 n = n1x + n2x + ... + nkx 。 由于答案可能非常大，请你将它对 109 + 7 取余后返回。 比方说，n = 160 且 x = 3 ，一个表示 n 的方法是 n = 23 + 33 + 53 。 示例 1： 输入：n = 10, x = 2 输出：1 解释：我们可以将 n 表示为：n = 32 + 12 = 10 。 这是唯一将 10 表达成不同整数 2 次方之和的方案。 示例 2： 输入：n = 4, x = 1 输出：2 解释：我们可以将 n 按以下方案表示： - n = 41 = 4 。 - n = 31 + 11 = 4 。 提示： * 1 <= n <= 300 * 1 <= x <= 5
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划来解决这个问题。我们定义 dp[i] 为将 i 表示成一些互不相同的正整数的 x 次幂之和的方案数。

算法步骤:
1. 初始化一个长度为 n+1 的数组 dp，其中 dp[0] = 1，表示 0 只有一种表示方法（即不使用任何数）。
2. 遍历所有可能的底数 i，从 1 到 n^(1/x)。
3. 对于每个底数 i，计算其 x 次幂 power。
4. 从 n 开始向前遍历，更新 dp[j] 为 dp[j] + dp[j - power]，表示将 j 表示成 power 加上其他数的方案数。
5. 最终返回 dp[n] % (10^9 + 7)。

关键点:
- 使用动态规划来避免重复计算。
- 通过从 n 向前遍历来确保每个数只被使用一次。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n * sqrt(x))
空间复杂度: O(n)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def waysToExpressAsSumOfPowers(n: int, x: int) -> int:
    """
    函数式接口 - 返回将 n 表示成一些互不相同的正整数的 x 次幂之和的方案数。
    """
    MOD = 10**9 + 7
    dp = [0] * (n + 1)
    dp[0] = 1  # 0 只有一种表示方法，即不使用任何数

    for i in range(1, int(n ** (1 / x)) + 1):
        power = i ** x
        for j in range(n, power - 1, -1):
            dp[j] = (dp[j] + dp[j - power]) % MOD

    return dp[n]


Solution = create_solution(waysToExpressAsSumOfPowers)