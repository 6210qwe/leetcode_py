# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1400
标题: Find Winner on a Tic Tac Toe Game
难度: easy
链接: https://leetcode.cn/problems/find-winner-on-a-tic-tac-toe-game/
题目类型: 数组、哈希表、矩阵、模拟
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1275. 找出井字棋的获胜者 - 井字棋 是由两个玩家 A 和 B 在 3 x 3 的棋盘上进行的游戏。井字棋游戏的规则如下： * 玩家轮流将棋子放在空方格 (' ') 上。 * 第一个玩家 A 总是用 'X' 作为棋子，而第二个玩家 B 总是用 'O' 作为棋子。 * 'X' 和 'O' 只能放在空方格中，而不能放在已经被占用的方格上。 * 只要有 3 个相同的（非空）棋子排成一条直线（行、列、对角线）时，游戏结束。 * 如果所有方块都放满棋子（不为空），游戏也会结束。 * 游戏结束后，棋子无法再进行任何移动。 给你一个数组 moves，其中 moves[i] = [rowi, coli] 表示第 i 次移动在 grid[rowi][coli]。如果游戏存在获胜者（A 或 B），就返回该游戏的获胜者；如果游戏以平局结束，则返回 "Draw"；如果仍会有行动（游戏未结束），则返回 "Pending"。 你可以假设 moves 都 有效（遵循 井字棋 规则），网格最初是空的，A 将先行动。 示例 1： [https://assets.leetcode.com/uploads/2021/09/22/xo1-grid.jpg] 输入：moves = [[0,0],[2,0],[1,1],[2,1],[2,2]] 输出："A" 解释："A" 获胜，他总是先走。 示例 2： [https://assets.leetcode.com/uploads/2021/09/22/xo2-grid.jpg] 输入：moves = [[0,0],[1,1],[0,1],[0,2],[1,0],[2,0]] 输出："B" 解释："B" 获胜。 示例 3： [https://assets.leetcode.com/uploads/2021/09/22/xo3-grid.jpg] 输入：moves = [[0,0],[1,1],[2,0],[1,0],[1,2],[2,1],[0,1],[0,2],[2,2]] 输出："Draw" 解释：由于没有办法再行动，游戏以平局结束。 提示： * 1 <= moves.length <= 9 * moves[i].length == 2 * 0 <= moves[i][j] <= 2 * moves 里没有重复的元素。 * moves 遵循井字棋的规则。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过记录每个玩家在每行、每列和对角线上的棋子数量来判断是否有玩家获胜。

算法步骤:
1. 初始化一个 3x3 的棋盘，并记录每个玩家在每行、每列和对角线上的棋子数量。
2. 遍历 moves 数组，根据当前步数确定是哪个玩家的回合，并更新棋盘和计数器。
3. 每次移动后检查是否有玩家在某一行、某一列或对角线上有三个棋子。
4. 如果有玩家获胜，返回该玩家；如果所有位置都被填满且没有玩家获胜，返回 "Draw"；否则返回 "Pending"。

关键点:
- 使用计数器来跟踪每个玩家在每行、每列和对角线上的棋子数量。
- 每次移动后立即检查是否有玩家获胜，以尽早结束游戏。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(1)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def tictactoe(moves: List[List[int]]) -> str:
    """
    函数式接口 - 判断井字棋游戏的获胜者
    """
    # 初始化棋盘和计数器
    board = [[''] * 3 for _ in range(3)]
    rows, cols = [0] * 3, [0] * 3
    diag, anti_diag = 0, 0
    
    # 遍历 moves 数组
    for i, (row, col) in enumerate(moves):
        player = 'A' if i % 2 == 0 else 'B'
        board[row][col] = player
        
        # 更新计数器
        if player == 'A':
            rows[row] += 1
            cols[col] += 1
            if row == col:
                diag += 1
            if row + col == 2:
                anti_diag += 1
        else:
            rows[row] -= 1
            cols[col] -= 1
            if row == col:
                diag -= 1
            if row + col == 2:
                anti_diag -= 1
        
        # 检查是否有玩家获胜
        if abs(rows[row]) == 3 or abs(cols[col]) == 3 or abs(diag) == 3 or abs(anti_diag) == 3:
            return player
    
    # 检查是否平局
    if len(moves) == 9:
        return "Draw"
    
    return "Pending"


Solution = create_solution(tictactoe)