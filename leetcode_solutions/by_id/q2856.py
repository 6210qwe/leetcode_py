# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2856
标题: Count Complete Subarrays in an Array
难度: medium
链接: https://leetcode.cn/problems/count-complete-subarrays-in-an-array/
题目类型: 数组、哈希表、滑动窗口
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2799. 统计完全子数组的数目 - 给你一个由 正 整数组成的数组 nums 。 如果数组中的某个子数组满足下述条件，则称之为 完全子数组 ： * 子数组中 不同 元素的数目等于整个数组不同元素的数目。 返回数组中 完全子数组 的数目。 子数组 是数组中的一个连续非空序列。 示例 1： 输入：nums = [1,3,1,2,2] 输出：4 解释：完全子数组有：[1,3,1,2]、[1,3,1,2,2]、[3,1,2] 和 [3,1,2,2] 。 示例 2： 输入：nums = [5,5,5,5] 输出：10 解释：数组仅由整数 5 组成，所以任意子数组都满足完全子数组的条件。子数组的总数为 10 。 提示： * 1 <= nums.length <= 1000 * 1 <= nums[i] <= 2000
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用滑动窗口来找到所有包含数组中所有不同元素的子数组。

算法步骤:
1. 计算整个数组的不同元素数目。
2. 使用滑动窗口遍历数组，维护当前窗口内的不同元素数目。
3. 当窗口内的不同元素数目等于整个数组的不同元素数目时，记录符合条件的子数组数量。
4. 移动窗口右边界，直到窗口内的不同元素数目不再满足条件，然后移动左边界缩小窗口。

关键点:
- 使用两个指针（left 和 right）来表示滑动窗口的左右边界。
- 使用一个字典来记录当前窗口内的元素及其出现次数。
- 每次右边界扩展时，更新字典并检查是否满足条件。
- 每次左边界收缩时，更新字典并继续检查。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(k)，其中 k 是数组中不同元素的数目。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def count_complete_subarrays(nums: List[int]) -> int:
    """
    函数式接口 - 统计完全子数组的数目
    """
    # 计算整个数组的不同元素数目
    total_unique_elements = len(set(nums))
    
    # 初始化滑动窗口的左右指针和结果计数器
    left, right = 0, 0
    current_unique_count = 0
    result = 0
    element_count = {}
    
    while right < len(nums):
        # 扩展右边界
        if nums[right] not in element_count:
            element_count[nums[right]] = 0
            current_unique_count += 1
        element_count[nums[right]] += 1
        
        # 当窗口内的不同元素数目等于整个数组的不同元素数目时
        while current_unique_count == total_unique_elements:
            # 记录符合条件的子数组数量
            result += len(nums) - right
            
            # 收缩左边界
            element_count[nums[left]] -= 1
            if element_count[nums[left]] == 0:
                del element_count[nums[left]]
                current_unique_count -= 1
            left += 1
        
        right += 1
    
    return result

Solution = create_solution(count_complete_subarrays)