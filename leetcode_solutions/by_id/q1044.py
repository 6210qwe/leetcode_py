# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1044
标题: Find Common Characters
难度: easy
链接: https://leetcode.cn/problems/find-common-characters/
题目类型: 数组、哈希表、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1002. 查找共用字符 - 给你一个字符串数组 words ，请你找出所有在 words 的每个字符串中都出现的共用字符（包括重复字符），并以数组形式返回。你可以按 任意顺序 返回答案。 示例 1： 输入：words = ["bella","label","roller"] 输出：["e","l","l"] 示例 2： 输入：words = ["cool","lock","cook"] 输出：["c","o"] 提示： * 1 <= words.length <= 100 * 1 <= words[i].length <= 100 * words[i] 由小写英文字母组成
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用哈希表记录每个字符在所有字符串中的最小出现次数。

算法步骤:
1. 初始化一个字典，记录第一个字符串中每个字符的出现次数。
2. 遍历剩余的字符串，更新字典中每个字符的最小出现次数。
3. 根据字典中的最小出现次数构建结果列表。

关键点:
- 使用 `collections.Counter` 来简化字符计数。
- 通过遍历多个字符串来更新最小出现次数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n * m)，其中 n 是字符串数组的长度，m 是每个字符串的平均长度。
空间复杂度: O(1)，因为字母表的大小是固定的（26个字母）。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List
from collections import Counter

def solution_function_name(words: List[str]) -> List[str]:
    """
    函数式接口 - 查找共用字符
    """
    if not words:
        return []

    # 初始化计数器，记录第一个字符串中每个字符的出现次数
    char_count = Counter(words[0])

    # 遍历剩余的字符串，更新每个字符的最小出现次数
    for word in words[1:]:
        current_count = Counter(word)
        for char in char_count:
            char_count[char] = min(char_count[char], current_count[char])

    # 构建结果列表
    result = []
    for char, count in char_count.items():
        result.extend([char] * count)

    return result

Solution = create_solution(solution_function_name)