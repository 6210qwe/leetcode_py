# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2324
标题: Find Triangular Sum of an Array
难度: medium
链接: https://leetcode.cn/problems/find-triangular-sum-of-an-array/
题目类型: 数组、数学、组合数学、模拟
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2221. 数组的三角和 - 给你一个下标从 0 开始的整数数组 nums ，其中 nums[i] 是 0 到 9 之间（两者都包含）的一个数字。 nums 的 三角和 是执行以下操作以后最后剩下元素的值： 1. nums 初始包含 n 个元素。如果 n == 1 ，终止 操作。否则，创建 一个新的下标从 0 开始的长度为 n - 1 的整数数组 newNums 。 2. 对于满足 0 <= i < n - 1 的下标 i ，newNums[i] 赋值 为 (nums[i] + nums[i+1]) % 10 ，% 表示取余运算。 3. 将 newNums 替换 数组 nums 。 4. 从步骤 1 开始 重复 整个过程。 请你返回 nums 的三角和。 示例 1： [https://assets.leetcode.com/uploads/2022/02/22/ex1drawio.png] 输入：nums = [1,2,3,4,5] 输出：8 解释： 上图展示了得到数组三角和的过程。 示例 2： 输入：nums = [5] 输出：5 解释： 由于 nums 中只有一个元素，数组的三角和为这个元素自己。 提示： * 1 <= nums.length <= 1000 * 0 <= nums[i] <= 9
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划的思想，逐步计算每一步的结果，直到只剩下一个元素。

算法步骤:
1. 初始化一个变量 `n` 为数组的长度。
2. 使用一个循环，每次减少数组的长度，直到长度为 1。
3. 在每次循环中，更新数组的每个元素为其与下一个元素之和的模 10 值。
4. 返回最终剩下的单个元素。

关键点:
- 通过原地修改数组来减少空间复杂度。
- 每次循环只处理当前有效的部分，避免不必要的计算。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n^2)，其中 n 是数组的长度。每次循环的时间复杂度是 O(n)，总共需要进行 n-1 次循环。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def triangular_sum(nums: List[int]) -> int:
    """
    函数式接口 - 计算数组的三角和
    """
    n = len(nums)
    while n > 1:
        for i in range(n - 1):
            nums[i] = (nums[i] + nums[i + 1]) % 10
        n -= 1
    return nums[0]


Solution = create_solution(triangular_sum)