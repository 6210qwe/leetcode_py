# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1552
标题: Build an Array With Stack Operations
难度: medium
链接: https://leetcode.cn/problems/build-an-array-with-stack-operations/
题目类型: 栈、数组、模拟
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1441. 用栈操作构建数组 - 给你一个数组 target 和一个整数 n。 给你一个空栈和两种操作： * "Push"：将一个整数加到栈顶。 * "Pop"：从栈顶删除一个整数。 同时给定一个范围 [1, n] 中的整数流。 使用两个栈操作使栈中的数字（从底部到顶部）等于 target。你应该遵循以下规则： * 如果整数流不为空，从流中选取下一个整数并将其推送到栈顶。 * 如果栈不为空，弹出栈顶的整数。 * 如果，在任何时刻，栈中的元素（从底部到顶部）等于 target，则不要从流中读取新的整数，也不要对栈进行更多操作。 请返回遵循上述规则构建 target 所用的操作序列。如果存在多个合法答案，返回 任一 即可。 示例 1： 输入：target = [1,3], n = 3 输出：["Push","Push","Pop","Push"] 解释：一开始栈为空。最后一个元素是栈顶。 从流中读取 1 并推入数组。s = [1]。 从流中读取 2 并推入数组。s = [1,2]。 从栈顶删除整数。s = [1]。 从流中读取 3 并推入数组。s = [1,3]。 示例 2： 输入：target = [1,2,3], n = 3 输出：["Push","Push","Push"] 解释：一开始栈为空。最后一个元素是栈顶。 从流中读取 1 并推入数组。s = [1]。 从流中读取 2 并推入数组。s = [1,2]。 从流中读取 3 并推入数组。s = [1,2,3]。 示例 3： 输入：target = [1,2], n = 4 输出：["Push","Push"] 解释：一开始栈为空。最后一个元素是栈顶。 从流中读取 1 并推入数组。s = [1]。 从流中读取 2 并推入数组。s = [1,2]。 由于栈（从底部到顶部）等于 target，我们停止栈操作。 从流中读取整数 3 的答案不被接受。 提示： * 1 <= target.length <= 100 * 1 <= n <= 100 * 1 <= target[i] <= n * target 严格递增
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过遍历整数流 [1, n]，在每次遇到 target 中的元素时执行 "Push" 操作，否则执行 "Push" 和 "Pop" 操作。

算法步骤:
1. 初始化一个空的操作列表 `operations`。
2. 遍历整数流 [1, n]：
   - 如果当前整数在 target 中，执行 "Push" 操作。
   - 如果当前整数不在 target 中，执行 "Push" 和 "Pop" 操作。
3. 返回操作列表 `operations`。

关键点:
- 通过遍历整数流 [1, n]，确保所有需要的 "Push" 和 "Pop" 操作都被正确记录。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是整数流的长度。
空间复杂度: O(m)，其中 m 是操作列表的长度。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def build_array_with_stack_operations(target: List[int], n: int) -> List[str]:
    """
    函数式接口 - 用栈操作构建数组
    """
    operations = []
    current = 1
    target_index = 0
    
    while target_index < len(target):
        if current == target[target_index]:
            operations.append("Push")
            target_index += 1
        else:
            operations.append("Push")
            operations.append("Pop")
        current += 1
    
    return operations


Solution = create_solution(build_array_with_stack_operations)