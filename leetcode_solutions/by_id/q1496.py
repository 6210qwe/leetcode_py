# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1496
标题: Lucky Numbers in a Matrix
难度: easy
链接: https://leetcode.cn/problems/lucky-numbers-in-a-matrix/
题目类型: 数组、矩阵
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1380. 矩阵中的幸运数 - 给你一个 m x n 的矩阵，矩阵中的数字 各不相同 。请你按 任意 顺序返回矩阵中的所有幸运数。 幸运数 是指矩阵中满足同时下列两个条件的元素： * 在同一行的所有元素中最小 * 在同一列的所有元素中最大 示例 1： 输入：matrix = [[3,7,8],[9,11,13],[15,16,17]] 输出：[15] 解释：15 是唯一的幸运数，因为它是其所在行中的最小值，也是所在列中的最大值。 示例 2： 输入：matrix = [[1,10,4,2],[9,3,8,7],[15,16,17,12]] 输出：[12] 解释：12 是唯一的幸运数，因为它是其所在行中的最小值，也是所在列中的最大值。 示例 3： 输入：matrix = [[7,8],[1,2]] 输出：[7] 解释：7 是唯一的幸运数字，因为它是行中的最小值，列中的最大值。 提示： * m == mat.length * n == mat[i].length * 1 <= n, m <= 50 * 1 <= matrix[i][j] <= 105 * 矩阵中的所有元素都是不同的
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过两次遍历矩阵，第一次找到每行的最小值及其索引，第二次找到每列的最大值及其索引。然后检查这些最小值和最大值是否在同一位置。

算法步骤:
1. 初始化一个列表来存储每行的最小值及其索引。
2. 遍历矩阵，找到每行的最小值及其索引，并存储在列表中。
3. 初始化一个列表来存储每列的最大值及其索引。
4. 遍历矩阵，找到每列的最大值及其索引，并存储在列表中。
5. 检查每行的最小值和每列的最大值是否在同一位置，如果是，则该值为幸运数。

关键点:
- 通过两次遍历矩阵，分别找到每行的最小值和每列的最大值。
- 检查这些值是否在同一位置。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n)，其中 m 是矩阵的行数，n 是矩阵的列数。我们需要遍历矩阵两次。
空间复杂度: O(m + n)，用于存储每行的最小值及其索引和每列的最大值及其索引。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def find_lucky_numbers(matrix: List[List[int]]) -> List[int]:
    """
    函数式接口 - 找到矩阵中的幸运数
    """
    if not matrix or not matrix[0]:
        return []

    m, n = len(matrix), len(matrix[0])
    
    # 找到每行的最小值及其索引
    row_min = [(min(row), row.index(min(row))) for row in matrix]
    
    # 找到每列的最大值及其索引
    col_max = [(max(col), col.index(max(col))) for col in zip(*matrix)]
    
    # 检查每行的最小值和每列的最大值是否在同一位置
    lucky_numbers = [matrix[row][col] for (val, col), (row, val) in zip(row_min, col_max) if row == col]
    
    return lucky_numbers


Solution = create_solution(find_lucky_numbers)