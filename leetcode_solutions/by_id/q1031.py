# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1031
标题: Add to Array-Form of Integer
难度: easy
链接: https://leetcode.cn/problems/add-to-array-form-of-integer/
题目类型: 数组、数学
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
989. 数组形式的整数加法 - 整数的 数组形式 num 是按照从左到右的顺序表示其数字的数组。 * 例如，对于 num = 1321 ，数组形式是 [1,3,2,1] 。 给定 num ，整数的 数组形式 ，和整数 k ，返回 整数 num + k 的 数组形式 。 示例 1： 输入：num = [1,2,0,0], k = 34 输出：[1,2,3,4] 解释：1200 + 34 = 1234 示例 2： 输入：num = [2,7,4], k = 181 输出：[4,5,5] 解释：274 + 181 = 455 示例 3： 输入：num = [2,1,5], k = 806 输出：[1,0,2,1] 解释：215 + 806 = 1021 提示： * 1 <= num.length <= 104 * 0 <= num[i] <= 9 * num 不包含任何前导零，除了零本身 * 1 <= k <= 104
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 从数组的末尾开始逐位相加，并处理进位。

算法步骤:
1. 初始化一个空列表 `result` 用于存储结果。
2. 从数组的末尾开始遍历，同时处理整数 `k` 的每一位。
3. 计算当前位的和，并处理进位。
4. 将当前位的结果添加到 `result` 中。
5. 如果 `k` 还有剩余的高位，继续处理。
6. 最后将 `result` 反转并返回。

关键点:
- 从数组末尾开始处理，可以方便地处理进位。
- 使用 `divmod` 函数来处理进位和当前位的结果。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是数组 A 的长度。我们需要遍历整个数组。
空间复杂度: O(1)，不考虑输出的空间占用，我们只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List


def add_to_array_form(num: List[int], k: int) -> List[int]:
    result = []
    i = len(num) - 1
    
    while i >= 0 or k > 0:
        if i >= 0:
            k += num[i]
        k, digit = divmod(k, 10)
        result.append(digit)
        i -= 1
    
    return result[::-1]


Solution = create_solution(add_to_array_form)