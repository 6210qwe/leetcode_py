# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3747
标题: Maximum Difference Between Adjacent Elements in a Circular Array
难度: easy
链接: https://leetcode.cn/problems/maximum-difference-between-adjacent-elements-in-a-circular-array/
题目类型: 数组
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3423. 循环数组中相邻元素的最大差值 - 给你一个 循环 数组 nums ，请你找出相邻元素之间的 最大 绝对差值。 注意：一个循环数组中，第一个元素和最后一个元素是相邻的。 示例 1： 输入：nums = [1,2,4] 输出：3 解释： 由于 nums 是循环的，nums[0] 和 nums[2] 是相邻的，它们之间的绝对差值是最大值 |4 - 1| = 3 。 示例 2： 输入：nums = [-5,-10,-5] 输出：5 解释： 相邻元素 nums[0] 和 nums[1] 之间的绝对差值为最大值 |-5 - (-10)| = 5 。 提示： * 2 <= nums.length <= 100 * -100 <= nums[i] <= 100
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 计算相邻元素之间的最大绝对差值，包括首尾元素。

算法步骤:
1. 初始化最大差值为0。
2. 遍历数组，计算每对相邻元素之间的绝对差值，并更新最大差值。
3. 特别处理首尾元素之间的绝对差值，并更新最大差值。

关键点:
- 使用一次遍历即可完成计算。
- 处理首尾元素时，需要特别考虑。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def max_adjacent_difference(nums: List[int]) -> int:
    """
    计算循环数组中相邻元素之间的最大绝对差值。
    """
    n = len(nums)
    if n < 2:
        raise ValueError("数组长度必须大于等于2")

    max_diff = 0

    # 遍历数组，计算每对相邻元素之间的绝对差值
    for i in range(1, n):
        max_diff = max(max_diff, abs(nums[i] - nums[i - 1]))

    # 特别处理首尾元素之间的绝对差值
    max_diff = max(max_diff, abs(nums[0] - nums[n - 1]))

    return max_diff


Solution = create_solution(max_adjacent_difference)