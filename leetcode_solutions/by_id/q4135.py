# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 4135
标题: Concatenate Non-Zero Digits and Multiply by Sum I
难度: easy
链接: https://leetcode.cn/problems/concatenate-non-zero-digits-and-multiply-by-sum-i/
题目类型: 数学
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3754. 连接非零数字并乘以其数字和 I - 给你一个整数 n。 将 n 中所有的 非零数字 按照它们的原始顺序连接起来，形成一个新的整数 x。如果不存在 非零数字 ，则 x = 0。 sum 为 x 中所有数字的 数字和 。 返回一个整数，表示 x * sum 的值。 示例 1： 输入： n = 10203004 输出： 12340 解释： * 非零数字是 1、2、3 和 4。因此，x = 1234。 * 数字和为 sum = 1 + 2 + 3 + 4 = 10。 * 因此，答案是 x * sum = 1234 * 10 = 12340。 示例 2： 输入： n = 1000 输出： 1 解释： * 非零数字是 1，因此 x = 1 且 sum = 1。 * 因此，答案是 x * sum = 1 * 1 = 1。 提示： * 0 <= n <= 109
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过遍历 n 的每一位数字，提取出非零数字并计算其和，然后将这些非零数字连接成一个新的整数 x，最后返回 x 与数字和的乘积。

算法步骤:
1. 初始化两个变量：`non_zero_digits` 用于存储非零数字的字符串形式，`digit_sum` 用于存储非零数字的和。
2. 遍历 n 的每一位数字，如果是非零数字，则将其添加到 `non_zero_digits` 中，并累加到 `digit_sum` 中。
3. 如果 `non_zero_digits` 为空，则返回 0。
4. 将 `non_zero_digits` 转换为整数 x。
5. 返回 x 与 `digit_sum` 的乘积。

关键点:
- 通过字符串操作来连接非零数字。
- 通过遍历每一位数字来提取非零数字并计算其和。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(d)，其中 d 是 n 的位数。我们需要遍历 n 的每一位数字。
空间复杂度: O(d)，其中 d 是 n 的位数。我们使用了一个字符串来存储非零数字。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def concatenate_non_zero_digits_and_multiply_by_sum(n: int) -> int:
    """
    函数式接口 - 实现连接非零数字并乘以其数字和
    """
    non_zero_digits = ""
    digit_sum = 0

    # 遍历 n 的每一位数字
    while n > 0:
        digit = n % 10
        if digit != 0:
            non_zero_digits += str(digit)
            digit_sum += digit
        n //= 10

    # 如果没有非零数字，返回 0
    if not non_zero_digits:
        return 0

    # 将非零数字字符串转换为整数
    x = int(non_zero_digits)

    # 返回 x 与数字和的乘积
    return x * digit_sum


Solution = create_solution(concatenate_non_zero_digits_and_multiply_by_sum)