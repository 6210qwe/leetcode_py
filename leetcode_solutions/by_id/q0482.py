# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 482
标题: License Key Formatting
难度: easy
链接: https://leetcode.cn/problems/license-key-formatting/
题目类型: 字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
482. 密钥格式化 - 给定一个许可密钥字符串 s，仅由字母、数字字符和破折号组成。字符串由 n 个破折号分成 n + 1 组。你也会得到一个整数 k 。 我们想要重新格式化字符串 s，使每一组包含 k 个字符，除了第一组，它可以比 k 短，但仍然必须包含至少一个字符。此外，两组之间必须插入破折号，并且应该将所有小写字母转换为大写字母。 返回 重新格式化的许可密钥 。 示例 1： 输入：S = "5F3Z-2e-9-w", k = 4 输出："5F3Z-2E9W" 解释：字符串 S 被分成了两个部分，每部分 4 个字符； 注意，两个额外的破折号需要删掉。 示例 2： 输入：S = "2-5g-3-J", k = 2 输出："2-5G-3J" 解释：字符串 S 被分成了 3 个部分，按照前面的规则描述，第一部分的字符可以少于给定的数量，其余部分皆为 2 个字符。 提示: * 1 <= s.length <= 105 * s 只包含字母、数字和破折号 '-'. * 1 <= k <= 104
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 将字符串中的破折号去掉，然后将所有字符转换为大写。接着从后向前遍历字符串，每 k 个字符插入一个破折号。

算法步骤:
1. 去掉字符串中的破折号，并将所有字符转换为大写。
2. 计算新字符串的长度，并确定第一组的长度。
3. 从后向前遍历字符串，每 k 个字符插入一个破折号。
4. 返回重新格式化的字符串。

关键点:
- 注意边界条件，确保第一组的长度至少为 1。
- 优化时间和空间复杂度，尽量减少不必要的操作。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n) - 其中 n 是字符串 s 的长度，因为我们需要遍历字符串一次。
空间复杂度: O(n) - 我们需要存储处理后的字符串。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def license_key_formatting(s: str, k: int) -> str:
    """
    函数式接口 - 根据题目要求重新格式化许可密钥字符串
    
    实现思路:
    1. 去掉字符串中的破折号，并将所有字符转换为大写。
    2. 计算新字符串的长度，并确定第一组的长度。
    3. 从后向前遍历字符串，每 k 个字符插入一个破折号。
    4. 返回重新格式化的字符串。
    
    Args:
        s: 输入的许可密钥字符串
        k: 每组的字符数
        
    Returns:
        重新格式化的许可密钥字符串
        
    Example:
        >>> license_key_formatting("5F3Z-2e-9-w", 4)
        "5F3Z-2E9W"
        >>> license_key_formatting("2-5g-3-J", 2)
        "2-5G-3J"
    """
    # 去掉破折号并将所有字符转换为大写
    s = s.replace('-', '').upper()
    
    # 计算新字符串的长度
    n = len(s)
    
    # 确定第一组的长度
    first_group_length = n % k if n % k != 0 else k
    
    # 初始化结果字符串
    result = s[:first_group_length]
    
    # 从后向前遍历字符串，每 k 个字符插入一个破折号
    for i in range(first_group_length, n, k):
        result += '-' + s[i:i+k]
    
    return result


# 自动生成Solution类（无需手动编写）
Solution = create_solution(license_key_formatting)