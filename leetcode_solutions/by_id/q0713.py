# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 713
标题: Subarray Product Less Than K
难度: medium
链接: https://leetcode.cn/problems/subarray-product-less-than-k/
题目类型: 数组、二分查找、前缀和、滑动窗口
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
713. 乘积小于 K 的子数组 - 给你一个整数数组 nums 和一个整数 k ，请你返回子数组内所有元素的乘积严格小于 k 的连续子数组的数目。 示例 1： 输入：nums = [10,5,2,6], k = 100 输出：8 解释：8 个乘积小于 100 的子数组分别为：[10]、[5]、[2]、[6]、[10,5]、[5,2]、[2,6]、[5,2,6]。 需要注意的是 [10,5,2] 并不是乘积小于 100 的子数组。 示例 2： 输入：nums = [1,2,3], k = 0 输出：0 提示: * 1 <= nums.length <= 3 * 104 * 1 <= nums[i] <= 1000 * 0 <= k <= 106
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用滑动窗口来找到所有乘积小于 k 的子数组。

算法步骤:
1. 初始化两个指针 left 和 right，以及当前窗口内的乘积 product。
2. 移动右指针 right，将新元素加入乘积中。
3. 如果当前窗口内的乘积大于等于 k，则移动左指针 left，直到乘积小于 k。
4. 计算以 right 为结尾的所有子数组的数量，并累加到结果中。

关键点:
- 使用滑动窗口来维护当前子数组的乘积。
- 每次移动右指针时，更新乘积并检查是否需要移动左指针。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是数组的长度。每个指针最多移动 n 次。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def solution_function_name(nums: List[int], k: int) -> int:
    """
    函数式接口 - 返回乘积小于 k 的子数组数量
    """
    if k <= 1:
        return 0

    count = 0
    product = 1
    left = 0

    for right in range(len(nums)):
        product *= nums[right]
        while product >= k:
            product /= nums[left]
            left += 1
        count += right - left + 1

    return count

Solution = create_solution(solution_function_name)