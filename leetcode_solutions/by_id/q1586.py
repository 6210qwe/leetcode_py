# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1586
标题: Longest Subarray of 1's After Deleting One Element
难度: medium
链接: https://leetcode.cn/problems/longest-subarray-of-1s-after-deleting-one-element/
题目类型: 数组、动态规划、滑动窗口
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1493. 删掉一个元素以后全为 1 的最长子数组 - 给你一个二进制数组 nums ，你需要从中删掉一个元素。 请你在删掉元素的结果数组中，返回最长的且只包含 1 的非空子数组的长度。 如果不存在这样的子数组，请返回 0 。 提示 1： 输入：nums = [1,1,0,1] 输出：3 解释：删掉位置 2 的数后，[1,1,1] 包含 3 个 1 。 示例 2： 输入：nums = [0,1,1,1,0,1,1,0,1] 输出：5 解释：删掉位置 4 的数字后，[0,1,1,1,1,1,0,1] 的最长全 1 子数组为 [1,1,1,1,1] 。 示例 3： 输入：nums = [1,1,1] 输出：2 解释：你必须要删除一个元素。 提示： * 1 <= nums.length <= 105 * nums[i] 要么是 0 要么是 1 。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用滑动窗口来找到最长的包含最多一个 0 的子数组。

算法步骤:
1. 初始化两个指针 left 和 right，以及一个计数器 zero_count 来记录当前窗口内的 0 的数量。
2. 移动右指针扩展窗口，如果遇到 0，则增加 zero_count。
3. 如果 zero_count 大于 1，则移动左指针收缩窗口，直到 zero_count 小于等于 1。
4. 记录窗口的最大长度，即为所求结果减去 1（因为需要删除一个元素）。

关键点:
- 使用滑动窗口来维护一个最多包含一个 0 的子数组。
- 窗口的最大长度即为所求结果减去 1。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是数组的长度。每个元素最多被访问两次（一次由右指针，一次由左指针）。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def longest_subarray(nums: List[int]) -> int:
    """
    函数式接口 - 返回删掉一个元素后的最长全 1 子数组的长度
    """
    left, right, zero_count, max_length = 0, 0, 0, 0
    
    while right < len(nums):
        if nums[right] == 0:
            zero_count += 1
        
        while zero_count > 1:
            if nums[left] == 0:
                zero_count -= 1
            left += 1
        
        max_length = max(max_length, right - left)
        right += 1
    
    return max_length

Solution = create_solution(longest_subarray)