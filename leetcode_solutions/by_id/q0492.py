# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 492
标题: Construct the Rectangle
难度: easy
链接: https://leetcode.cn/problems/construct-the-rectangle/
题目类型: 数学
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
492. 构造矩形 - 作为一位web开发者， 懂得怎样去规划一个页面的尺寸是很重要的。 所以，现给定一个具体的矩形页面面积，你的任务是设计一个长度为 L 和宽度为 W 且满足以下要求的矩形的页面。要求： 1. 你设计的矩形页面必须等于给定的目标面积。 2. 宽度 W 不应大于长度 L ，换言之，要求 L >= W 。 3. 长度 L 和宽度 W 之间的差距应当尽可能小。 返回一个 数组 [L, W]，其中 L 和 W 是你按照顺序设计的网页的长度和宽度。 示例1： 输入: 4 输出: [2, 2] 解释: 目标面积是 4， 所有可能的构造方案有 [1,4], [2,2], [4,1]。 但是根据要求2，[1,4] 不符合要求; 根据要求3，[2,2] 比 [4,1] 更能符合要求. 所以输出长度 L 为 2， 宽度 W 为 2。 示例 2: 输入: area = 37 输出: [37,1] 示例 3: 输入: area = 122122 输出: [427,286] 提示: * 1 <= area <= 107
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过从平方根开始向下寻找最接近的因数来找到最优解

算法步骤:
1. 从平方根开始向下遍历，找到第一个可以整除面积的数作为宽度。
2. 计算对应的长度。
3. 返回 [L, W]。

关键点:
- 从平方根开始向下遍历可以确保找到的因数对中，长度和宽度的差值最小。
- 优化时间和空间复杂度。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(√n) - 从平方根开始向下遍历，最多需要 √n 次操作
空间复杂度: O(1) - 只使用了常数级的额外空间
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def construct_the_rectangle(area: int) -> List[int]:
    """
    函数式接口 - 构造矩形
    
    实现思路:
    从平方根开始向下遍历，找到第一个可以整除面积的数作为宽度，计算对应的长度，返回 [L, W]。
    
    Args:
        area: 矩形页面的面积
        
    Returns:
        [L, W] - 矩形页面的长度和宽度
        
    Example:
        >>> construct_the_rectangle(4)
        [2, 2]
    """
    # 从平方根开始向下遍历
    for w in range(int(area ** 0.5), 0, -1):
        if area % w == 0:
            l = area // w
            return [l, w]

# 自动生成Solution类（无需手动编写）
Solution = create_solution(construct_the_rectangle)