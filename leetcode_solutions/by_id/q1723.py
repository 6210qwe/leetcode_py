# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1723
标题: Maximum Number of Achievable Transfer Requests
难度: hard
链接: https://leetcode.cn/problems/maximum-number-of-achievable-transfer-requests/
题目类型: 位运算、数组、回溯、枚举
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1601. 最多可达成的换楼请求数目 - 我们有 n 栋楼，编号从 0 到 n - 1 。每栋楼有若干员工。由于现在是换楼的季节，部分员工想要换一栋楼居住。 给你一个数组 requests ，其中 requests[i] = [fromi, toi] ，表示一个员工请求从编号为 fromi 的楼搬到编号为 toi 的楼。 一开始 所有楼都是满的，所以从请求列表中选出的若干个请求是可行的需要满足 每栋楼员工净变化为 0 。意思是每栋楼 离开 的员工数目 等于 该楼 搬入 的员工数数目。比方说 n = 3 且两个员工要离开楼 0 ，一个员工要离开楼 1 ，一个员工要离开楼 2 ，如果该请求列表可行，应该要有两个员工搬入楼 0 ，一个员工搬入楼 1 ，一个员工搬入楼 2 。 请你从原请求列表中选出若干个请求，使得它们是一个可行的请求列表，并返回所有可行列表中最大请求数目。 示例 1： [https://assets.leetcode.cn/aliyun-lc-upload/uploads/2020/09/26/move1.jpg] 输入：n = 5, requests = [[0,1],[1,0],[0,1],[1,2],[2,0],[3,4]] 输出：5 解释：请求列表如下： 从楼 0 离开的员工为 x 和 y ，且他们都想要搬到楼 1 。 从楼 1 离开的员工为 a 和 b ，且他们分别想要搬到楼 2 和 0 。 从楼 2 离开的员工为 z ，且他想要搬到楼 0 。 从楼 3 离开的员工为 c ，且他想要搬到楼 4 。 没有员工从楼 4 离开。 我们可以让 x 和 b 交换他们的楼，以满足他们的请求。 我们可以让 y，a 和 z 三人在三栋楼间交换位置，满足他们的要求。 所以最多可以满足 5 个请求。 示例 2： [https://assets.leetcode.cn/aliyun-lc-upload/uploads/2020/09/26/move2.jpg] 输入：n = 3, requests = [[0,0],[1,2],[2,1]] 输出：3 解释：请求列表如下： 从楼 0 离开的员工为 x ，且他想要回到原来的楼 0 。 从楼 1 离开的员工为 y ，且他想要搬到楼 2 。 从楼 2 离开的员工为 z ，且他想要搬到楼 1 。 我们可以满足所有的请求。 示例 3： 输入：n = 4, requests = [[0,3],[3,1],[1,2],[2,0]] 输出：4 提示： * 1 <= n <= 20 * 1 <= requests.length <= 16 * requests[i].length == 2 * 0 <= fromi, toi < n
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用回溯法来枚举所有可能的选择组合，并检查每个组合是否满足条件。

算法步骤:
1. 初始化一个递归函数来进行回溯。
2. 在递归函数中，使用一个计数器来记录当前选择的请求数量。
3. 对于每个请求，选择或不选择它，并递归处理下一个请求。
4. 在每次递归结束后，检查当前选择的请求是否满足每栋楼的净变化为0。
5. 如果满足条件，更新最大请求数量。
6. 返回最大请求数量。

关键点:
- 使用回溯法枚举所有可能的选择组合。
- 使用计数器记录当前选择的请求数量。
- 检查每个组合是否满足每栋楼的净变化为0。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(2^m)，其中 m 是 requests 的长度。因为每个请求都有两种选择（选或不选），总共有 2^m 种组合。
空间复杂度: O(n + m)，其中 n 是楼的数量，m 是 requests 的长度。需要额外的空间来存储楼的变化和递归调用栈。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def maxAchievableRequests(n: int, requests: List[List[int]]) -> int:
    def backtrack(index: int, count: int, changes: List[int]) -> None:
        if index == len(requests):
            if all(change == 0 for change in changes):
                nonlocal max_requests
                max_requests = max(max_requests, count)
            return
        
        # 不选择当前请求
        backtrack(index + 1, count, changes)
        
        # 选择当前请求
        from_i, to_i = requests[index]
        changes[from_i] -= 1
        changes[to_i] += 1
        backtrack(index + 1, count + 1, changes)
        
        # 回溯
        changes[from_i] += 1
        changes[to_i] -= 1
    
    max_requests = 0
    changes = [0] * n
    backtrack(0, 0, changes)
    return max_requests

Solution = create_solution(maxAchievableRequests)