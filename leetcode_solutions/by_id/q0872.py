# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 872
标题: Split Array into Fibonacci Sequence
难度: medium
链接: https://leetcode.cn/problems/split-array-into-fibonacci-sequence/
题目类型: 字符串、回溯
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
842. 将数组拆分成斐波那契序列 - 给定一个数字字符串 num，比如 "123456579"，我们可以将它分成「斐波那契式」的序列 [123, 456, 579]。 形式上，斐波那契式 序列是一个非负整数列表 f，且满足： * 0 <= f[i] < 231 ，（也就是说，每个整数都符合 32 位 有符号整数类型） * f.length >= 3 * 对于所有的0 <= i < f.length - 2，都有 f[i] + f[i + 1] = f[i + 2] 另外，请注意，将字符串拆分成小块时，每个块的数字一定不要以零开头，除非这个块是数字 0 本身。 返回从 num 拆分出来的任意一组斐波那契式的序列块，如果不能拆分则返回 []。 示例 1： 输入：num = "1101111" 输出：[11,0,11,11] 解释：输出 [110,1,111] 也可以。 示例 2： 输入: num = "112358130" 输出: [] 解释: 无法拆分。 示例 3： 输入："0123" 输出：[] 解释：每个块的数字不能以零开头，因此 "01"，"2"，"3" 不是有效答案。 提示： * 1 <= num.length <= 200 * num 中只含有数字
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用回溯法尝试所有可能的分割方式，并检查是否满足斐波那契条件。

算法步骤:
1. 初始化结果列表和当前路径。
2. 从索引 0 开始，尝试将字符串分割成两个数 a 和 b。
3. 递归地尝试将剩余字符串分割成满足斐波那契条件的数。
4. 如果找到一个满足条件的分割方式，返回结果；否则继续尝试其他分割方式。
5. 如果所有分割方式都不满足条件，返回空列表。

关键点:
- 使用回溯法尝试所有可能的分割方式。
- 每个数不能以 0 开头，除非该数是 0 本身。
- 每个数必须在 32 位有符号整数范围内。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(2^n)，其中 n 是字符串的长度。最坏情况下，需要尝试所有可能的分割方式。
空间复杂度: O(n)，递归调用栈的深度最多为 n。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def split_into_fibonacci(num: str) -> List[int]:
    def backtrack(start, path):
        if start == len(num) and len(path) >= 3:
            result.append(path[:])
            return True
        
        for end in range(start + 1, len(num) + 1):
            s = num[start:end]
            if s.startswith('0') and len(s) > 1:
                break
            if int(s) > 2**31 - 1:
                break
            
            if len(path) >= 2 and int(s) > path[-1] + path[-2]:
                break
            
            if len(path) < 2 or int(s) == path[-1] + path[-2]:
                path.append(int(s))
                if backtrack(end, path):
                    return True
                path.pop()
        
        return False
    
    result = []
    backtrack(0, [])
    return result[0] if result else []

Solution = create_solution(split_into_fibonacci)