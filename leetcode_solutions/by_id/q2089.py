# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2089
标题: Maximum Matrix Sum
难度: medium
链接: https://leetcode.cn/problems/maximum-matrix-sum/
题目类型: 贪心、数组、矩阵
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1975. 最大方阵和 - 给你一个 n x n 的整数方阵 matrix 。你可以执行以下操作 任意次 ： * 选择 matrix 中 相邻 两个元素，并将它们都 乘以 -1 。 如果两个元素有 公共边 ，那么它们就是 相邻 的。 你的目的是 最大化 方阵元素的和。请你在执行以上操作之后，返回方阵的 最大 和。 示例 1： [https://assets.leetcode.com/uploads/2021/07/16/pc79-q2ex1.png] 输入：matrix = [[1,-1],[-1,1]] 输出：4 解释：我们可以执行以下操作使和等于 4 ： - 将第一行的 2 个元素乘以 -1 。 - 将第一列的 2 个元素乘以 -1 。 示例 2： [https://assets.leetcode.com/uploads/2021/07/16/pc79-q2ex2.png] 输入：matrix = [[1,2,3],[-1,-2,-3],[1,2,3]] 输出：16 解释：我们可以执行以下操作使和等于 16 ： - 将第二行的最后 2 个元素乘以 -1 。 提示： * n == matrix.length == matrix[i].length * 2 <= n <= 250 * -105 <= matrix[i][j] <= 105
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过将所有负数变为正数来最大化矩阵和。如果负数的数量为偶数，则可以将所有负数变为正数。如果负数的数量为奇数，则需要保留一个最小的绝对值作为负数。

算法步骤:
1. 计算矩阵中所有元素的绝对值之和。
2. 统计矩阵中的负数数量。
3. 如果负数数量为奇数，则从总和中减去两倍的最小绝对值。

关键点:
- 通过绝对值之和来计算最大可能的矩阵和。
- 根据负数数量的奇偶性来决定是否需要减去最小绝对值。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n^2)，其中 n 是矩阵的维度。我们需要遍历整个矩阵来计算绝对值之和和统计负数数量。
空间复杂度: O(1)，我们只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def max_matrix_sum(matrix: List[List[int]]) -> int:
    """
    函数式接口 - 计算矩阵的最大和
    """
    total_sum = 0
    negative_count = 0
    min_abs_value = float('inf')
    
    for row in matrix:
        for num in row:
            total_sum += abs(num)
            if num < 0:
                negative_count += 1
            min_abs_value = min(min_abs_value, abs(num))
    
    if negative_count % 2 != 0:
        total_sum -= 2 * min_abs_value
    
    return total_sum


Solution = create_solution(max_matrix_sum)