# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1000296
标题: 01 矩阵
难度: medium
链接: https://leetcode.cn/problems/2bCMpM/
题目类型: 广度优先搜索、数组、动态规划、矩阵
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
LCR 107. 01 矩阵 - 给定一个由 0 和 1 组成的矩阵 mat ，请输出一个大小相同的矩阵，其中每一个格子是 mat 中对应位置元素到最近的 0 的距离。 两个相邻元素间的距离为 1。 示例 1： [https://pic.leetcode.cn/1626667201-NCWmuP-image.png] 输入：mat = [[0,0,0],[0,1,0],[0,0,0]] 输出：[[0,0,0],[0,1,0],[0,0,0]] 示例 2： [https://pic.leetcode.cn/1626667205-xFxIeK-image.png] 输入：mat = [[0,0,0],[0,1,0],[1,1,1]] 输出：[[0,0,0],[0,1,0],[1,2,1]] 提示： * m == mat.length * n == mat[i].length * 1 <= m, n <= 104 * 1 <= m * n <= 104 * mat[i][j] is either 0 or 1. * mat 中至少有一个 0 注意：本题与主站 542 题相同：https://leetcode.cn/problems/01-matrix/ [https://leetcode.cn/problems/01-matrix/]
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用广度优先搜索（BFS）从所有的 0 开始同时进行搜索，这样可以确保每个 1 到最近的 0 的距离被正确计算。

算法步骤:
1. 初始化一个队列，将所有 0 的位置加入队列，并将 1 的位置标记为未访问。
2. 使用 BFS 从所有 0 的位置开始扩散，每次扩散一层，更新当前层的所有 1 的距离。
3. 当队列为空时，BFS 结束，返回结果矩阵。

关键点:
- 使用 BFS 可以确保每个 1 到最近的 0 的距离被正确计算。
- 通过从所有 0 同时开始搜索，可以避免重复计算。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n)，其中 m 和 n 分别是矩阵的行数和列数。每个元素最多入队一次，出队一次。
空间复杂度: O(m * n)，队列的最大空间复杂度。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List
from collections import deque

def solution_function_name(mat: List[List[int]]) -> List[List[int]]:
    if not mat or not mat[0]:
        return []

    m, n = len(mat), len(mat[0])
    directions = [(-1, 0), (1, 0), (0, -1), (0, 1)]
    queue = deque()
    result = [[0 if mat[i][j] == 0 else float('inf') for j in range(n)] for i in range(m)]

    # 将所有 0 的位置加入队列
    for i in range(m):
        for j in range(n):
            if mat[i][j] == 0:
                queue.append((i, j))

    # BFS 从所有 0 的位置开始扩散
    while queue:
        x, y = queue.popleft()
        for dx, dy in directions:
            nx, ny = x + dx, y + dy
            if 0 <= nx < m and 0 <= ny < n and result[nx][ny] > result[x][y] + 1:
                result[nx][ny] = result[x][y] + 1
                queue.append((nx, ny))

    return result

Solution = create_solution(solution_function_name)