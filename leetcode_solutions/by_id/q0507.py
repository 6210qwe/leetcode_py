# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 507
标题: Perfect Number
难度: easy
链接: https://leetcode.cn/problems/perfect-number/
题目类型: 数学
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
507. 完美数 - 对于一个 正整数，如果它和除了它自身以外的所有 正因子 之和相等，我们称它为 「完美数」。 给定一个 整数 n， 如果是完美数，返回 true；否则返回 false。 示例 1： 输入：num = 28 输出：true 解释：28 = 1 + 2 + 4 + 7 + 14 1, 2, 4, 7, 和 14 是 28 的所有正因子。 示例 2： 输入：num = 7 输出：false 提示： * 1 <= num <= 108
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过遍历找到所有正因子并计算其和，判断是否等于原数。

算法步骤:
1. 初始化一个变量 `sum_of_factors` 为 1（因为 1 总是任何数的因子）。
2. 从 2 开始遍历到 `sqrt(num)`，检查每个数是否是 `num` 的因子。
3. 如果是因子，则将其及其对应的配对因子（`num // i`）加入 `sum_of_factors`。
4. 最后，检查 `sum_of_factors` 是否等于 `num`。

关键点:
- 只需要遍历到 `sqrt(num)`，因为超过 `sqrt(num)` 的因子可以通过 `num // i` 得到。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(sqrt(n)) - 我们只需要遍历到 sqrt(n)。
空间复杂度: O(1) - 只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def check_perfect_number(num: int) -> bool:
    """
    函数式接口 - 检查一个数是否为完美数
    """
    if num <= 1:
        return False
    
    sum_of_factors = 1
    for i in range(2, int(num ** 0.5) + 1):
        if num % i == 0:
            sum_of_factors += i
            if i != num // i:
                sum_of_factors += num // i
    
    return sum_of_factors == num


Solution = create_solution(check_perfect_number)