# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1302
标题: Delete Characters to Make Fancy String
难度: easy
链接: https://leetcode.cn/problems/delete-characters-to-make-fancy-string/
题目类型: 字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1957. 删除字符使字符串变好 - 一个字符串如果没有 三个连续 相同字符，那么它就是一个 好字符串 。 给你一个字符串 s ，请你从 s 删除 最少 的字符，使它变成一个 好字符串 。 请你返回删除后的字符串。题目数据保证答案总是 唯一的 。 示例 1： 输入：s = "leeetcode" 输出："leetcode" 解释： 从第一组 'e' 里面删除一个 'e' ，得到 "leetcode" 。 没有连续三个相同字符，所以返回 "leetcode" 。 示例 2： 输入：s = "aaabaaaa" 输出："aabaa" 解释： 从第一组 'a' 里面删除一个 'a' ，得到 "aabaaaa" 。 从第二组 'a' 里面删除两个 'a' ，得到 "aabaa" 。 没有连续三个相同字符，所以返回 "aabaa" 。 示例 3： 输入：s = "aab" 输出："aab" 解释：没有连续三个相同字符，所以返回 "aab" 。 提示： * 1 <= s.length <= 105 * s 只包含小写英文字母。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用双指针方法来构建结果字符串，确保没有三个连续相同的字符。

算法步骤:
1. 初始化两个指针 `i` 和 `j`，其中 `i` 用于遍历输入字符串 `s`，`j` 用于构建结果字符串 `result`。
2. 遍历字符串 `s`，对于每个字符：
   - 如果当前字符与前一个字符不同，或者前一个字符在结果字符串中只出现一次，则将当前字符添加到结果字符串中。
   - 否则，跳过当前字符。
3. 返回结果字符串。

关键点:
- 使用双指针方法可以在线性时间内解决问题。
- 通过比较当前字符和前一个字符，确保没有三个连续相同的字符。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串 s 的长度。我们只需要遍历字符串一次。
空间复杂度: O(n)，我们需要存储结果字符串。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(s: str) -> str:
    """
    函数式接口 - 删除字符使字符串变好
    """
    if not s:
        return ""

    result = []
    for char in s:
        if len(result) < 2 or (len(result) >= 2 and not (result[-1] == result[-2] == char)):
            result.append(char)
    
    return "".join(result)


Solution = create_solution(solution_function_name)