# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3532
标题: Time Taken to Mark All Nodes
难度: hard
链接: https://leetcode.cn/problems/time-taken-to-mark-all-nodes/
题目类型: 树、深度优先搜索、图、动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3241. 标记所有节点需要的时间 - 给你一棵 无向 树，树中节点从 0 到 n - 1 编号。同时给你一个长度为 n - 1 的二维整数数组 edges ，其中 edges[i] = [ui, vi] 表示节点 ui 和 vi 在树中有一条边。 一开始，所有 节点都 未标记 。对于节点 i ： * 当 i 是奇数时，如果时刻 x - 1 该节点有 至少 一个相邻节点已经被标记了，那么节点 i 会在时刻 x 被标记。 * 当 i 是偶数时，如果时刻 x - 2 该节点有 至少 一个相邻节点已经被标记了，那么节点 i 会在时刻 x 被标记。 请你返回一个数组 times ，表示如果你在时刻 t = 0 标记节点 i ，那么时刻 times[i] 时，树中所有节点都会被标记。 请注意，每个 times[i] 的答案都是独立的，即当你标记节点 i 时，所有其他节点都未标记。 示例 1： 输入：edges = [[0,1],[0,2]] 输出：[2,4,3] 解释： [https://assets.leetcode.com/uploads/2024/06/01/screenshot-2024-06-02-122236.png] * 对于 i = 0 ： * 节点 1 在时刻 t = 1 被标记，节点 2 在时刻 t = 2 被标记。 * 对于 i = 1 ： * 节点 0 在时刻 t = 2 被标记，节点 2 在时刻 t = 4 被标记。 * 对于 i = 2 ： * 节点 0 在时刻 t = 2 被标记，节点 1 在时刻 t = 3 被标记。 示例 2： 输入：edges = [[0,1]] 输出：[1,2] 解释： [https://assets.leetcode.com/uploads/2024/06/01/screenshot-2024-06-02-122249.png] * 对于 i = 0 ： * 节点 1 在时刻 t = 1 被标记。 * 对于 i = 1 ： * 节点 0 在时刻 t = 2 被标记。 示例 3： 输入：edges = [[2,4],[0,1],[2,3],[0,2]] 输出：[4,6,3,5,5] 解释： [https://assets.leetcode.com/uploads/2024/06/03/screenshot-2024-06-03-210550.png] 提示： * 2 <= n <= 105 * edges.length == n - 1 * edges[i].length == 2 * 0 <= edges[i][0], edges[i][1] <= n - 1 * 输入保证 edges 表示一棵合法的树。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用深度优先搜索 (DFS) 来计算每个节点被标记的时间。

算法步骤:
1. 构建邻接表表示树结构。
2. 定义一个递归函数 `dfs` 来计算从当前节点开始标记所有节点所需的时间。
3. 对于每个节点，分别计算其作为起始节点时的标记时间，并存储在结果数组中。

关键点:
- 使用 DFS 从当前节点开始遍历树，计算每个节点的标记时间。
- 奇数节点和偶数节点的标记时间不同，需要分别处理。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是节点数。每个节点和边都被访问一次。
空间复杂度: O(n)，用于存储邻接表和递归调用栈。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def time_taken_to_mark_all_nodes(edges: List[List[int]]) -> List[int]:
    def dfs(node: int, parent: int, is_odd: bool) -> int:
        # 计算当前节点的最大子树高度
        max_height = 0
        for neighbor in adj_list[node]:
            if neighbor != parent:
                height = dfs(neighbor, node, not is_odd)
                max_height = max(max_height, height)
        
        # 根据节点编号的奇偶性计算标记时间
        if is_odd:
            return max_height + 1
        else:
            return max_height + 2
    
    n = len(edges) + 1
    adj_list = [[] for _ in range(n)]
    for u, v in edges:
        adj_list[u].append(v)
        adj_list[v].append(u)
    
    result = [0] * n
    for i in range(n):
        result[i] = dfs(i, -1, i % 2 == 1)
    
    return result

Solution = create_solution(time_taken_to_mark_all_nodes)