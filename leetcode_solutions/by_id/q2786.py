# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2786
标题: Find the Longest Semi-Repetitive Substring
难度: medium
链接: https://leetcode.cn/problems/find-the-longest-semi-repetitive-substring/
题目类型: 字符串、滑动窗口
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2730. 找到最长的半重复子字符串 - 给你一个下标从 0 开始的字符串 s ，这个字符串只包含 0 到 9 的数字字符。 如果一个字符串 t 中至多有一对相邻字符是相等的，那么称这个字符串 t 是 半重复的 。例如，"0010" 、"002020" 、"0123" 、"2002" 和 "54944" 是半重复字符串，而 "00101022" （相邻的相同数字对是 00 和 22）和 "1101234883" （相邻的相同数字对是 11 和 88）不是半重复字符串。 请你返回 s 中最长 半重复 子字符串 的长度。 示例 1： 输入：s = "52233" 输出：4 解释： 最长的半重复子字符串是 "5223"。整个字符串 "52233" 有两个相邻的相同数字对 22 和 33，但最多只能选取一个。 示例 2： 输入：s = "5494" 输出：4 解释： s 是一个半重复字符串。 示例 3： 输入：s = "1111111" 输出：2 解释： 最长的半重复子字符串是 "11"。子字符串 "111" 有两个相邻的相同数字对，但最多允许选取一个。 提示： * 1 <= s.length <= 50 * '0' <= s[i] <= '9'
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用滑动窗口来找到最长的半重复子字符串。

算法步骤:
1. 初始化两个指针 left 和 right 分别指向窗口的左端和右端。
2. 使用一个变量 count 来记录当前窗口中相邻相同字符对的数量。
3. 移动右指针扩展窗口，同时更新 count。
4. 如果 count 大于 1，则移动左指针缩小窗口，直到 count 不大于 1。
5. 在每次移动右指针时，更新最长子字符串的长度。

关键点:
- 使用滑动窗口来动态调整子字符串的范围。
- 通过 count 变量来控制窗口内相邻相同字符对的数量。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串 s 的长度。每个字符最多被处理两次（一次由右指针，一次由左指针）。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def longest_semi_repetitive_substring(s: str) -> int:
    """
    函数式接口 - 找到最长的半重复子字符串
    """
    n = len(s)
    if n < 2:
        return n

    left, right = 0, 0
    max_length = 0
    count = 0

    while right < n - 1:
        if s[right] == s[right + 1]:
            count += 1
        while count > 1:
            if s[left] == s[left + 1]:
                count -= 1
            left += 1
        max_length = max(max_length, right - left + 2)
        right += 1

    return max_length


Solution = create_solution(longest_semi_repetitive_substring)