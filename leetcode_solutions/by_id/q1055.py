# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1055
标题: Pairs of Songs With Total Durations Divisible by 60
难度: medium
链接: https://leetcode.cn/problems/pairs-of-songs-with-total-durations-divisible-by-60/
题目类型: 数组、哈希表、计数
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1010. 总持续时间可被 60 整除的歌曲 - 在歌曲列表中，第 i 首歌曲的持续时间为 time[i] 秒。 返回其总持续时间（以秒为单位）可被 60 整除的歌曲对的数量。形式上，我们希望下标数字 i 和 j 满足 i < j 且有 (time[i] + time[j]) % 60 == 0。 示例 1： 输入：time = [30,20,150,100,40] 输出：3 解释：这三对的总持续时间可被 60 整除： (time[0] = 30, time[2] = 150): 总持续时间 180 (time[1] = 20, time[3] = 100): 总持续时间 120 (time[1] = 20, time[4] = 40): 总持续时间 60 示例 2： 输入：time = [60,60,60] 输出：3 解释：所有三对的总持续时间都是 120，可以被 60 整除。 提示： * 1 <= time.length <= 6 * 104 * 1 <= time[i] <= 500
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用哈希表来记录每个余数出现的次数，从而快速找到满足条件的配对。

算法步骤:
1. 初始化一个长度为 60 的数组 `count`，用于记录每个余数出现的次数。
2. 遍历 `time` 数组，对于每个元素 `t`，计算其对 60 取余的结果 `remainder`。
3. 对于每个 `remainder`，查找 `count[(60 - remainder) % 60]` 的值，并累加到结果 `res` 中。
4. 更新 `count[remainder]` 的值。

关键点:
- 使用哈希表来记录每个余数出现的次数，从而快速找到满足条件的配对。
- 通过取模运算将时间复杂度降低到 O(n)。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def solution_function_name(time: List[int]) -> int:
    """
    函数式接口 - 返回总持续时间可被 60 整除的歌曲对的数量
    """
    count = [0] * 60
    res = 0
    
    for t in time:
        remainder = t % 60
        res += count[(60 - remainder) % 60]
        count[remainder] += 1
    
    return res

Solution = create_solution(solution_function_name)