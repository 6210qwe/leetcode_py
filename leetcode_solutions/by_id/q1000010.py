# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1000010
标题: BST Sequences LCCI
难度: hard
链接: https://leetcode.cn/problems/bst-sequences-lcci/
题目类型: 树、二叉搜索树、回溯、二叉树
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
面试题 04.09. 二叉搜索树序列 - 从左向右遍历一个数组，通过不断将其中的元素插入树中可以逐步地生成一棵二叉搜索树。 给定一个由不同节点组成的二叉搜索树 root，输出所有可能生成此树的数组。 示例 1： 输入：root = [2,1,3] 输出：[[2,1,3],[2,3,1]] 解释：数组 [2,1,3]、[2,3,1] 均可以通过从左向右遍历元素插入树中形成以下二叉搜索树 2 / \ 1 3 示例 2： 输入：root = [4,1,null,null,3,2] 输出：[[4,1,3,2]] 提示： * 二叉搜索树中的节点数在 [0, 1000] 的范围内 * 1 <= 节点值 <= 106 * 用例保证符合要求的数组数量不超过 5000
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用回溯法生成所有可能的序列。

算法步骤:
1. 定义一个辅助函数 `dfs` 来进行深度优先搜索。
2. 在 `dfs` 函数中，维护当前路径和剩余节点列表。
3. 如果剩余节点列表为空，将当前路径加入结果列表。
4. 否则，遍历剩余节点列表，选择一个节点作为下一个节点，并递归调用 `dfs`。
5. 在递归调用后，恢复状态（回溯）。

关键点:
- 使用队列来存储当前路径和剩余节点。
- 递归过程中，每次选择一个节点并将其子节点加入剩余节点列表。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n! * n)，其中 n 是节点数。因为每个节点都有 n! 种排列方式，每种排列需要 O(n) 时间来生成。
空间复杂度: O(n)，递归调用栈的深度最多为 n。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution

def bst_sequences(root: Optional[TreeNode]) -> List[List[int]]:
    """
    生成所有可能的二叉搜索树序列。
    """
    if not root:
        return [[]]

    def dfs(path, remaining):
        if not remaining:
            result.append(path)
            return

        for i in range(len(remaining)):
            next_node = remaining[i]
            new_path = path + [next_node.val]
            new_remaining = remaining[:i] + remaining[i+1:]
            if next_node.left:
                new_remaining.append(next_node.left)
            if next_node.right:
                new_remaining.append(next_node.right)
            dfs(new_path, new_remaining)

    result = []
    dfs([root.val], [root.left, root.right] if root else [])
    return result

Solution = create_solution(bst_sequences)