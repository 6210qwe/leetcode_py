# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 97
标题: Interleaving String
难度: medium
链接: https://leetcode.cn/problems/interleaving-string/
题目类型: 字符串、动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
97. 交错字符串 - 给定三个字符串 s1、s2、s3，请你帮忙验证 s3 是否是由 s1 和 s2 交错 组成的。 两个字符串 s 和 t 交错 的定义与过程如下，其中每个字符串都会被分割成若干 非空 子字符串： * s = s1 + s2 + ... + sn * t = t1 + t2 + ... + tm * |n - m| <= 1 * 交错 是 s1 + t1 + s2 + t2 + s3 + t3 + ... 或者 t1 + s1 + t2 + s2 + t3 + s3 + ... 注意：a + b 意味着字符串 a 和 b 连接。 示例 1： [https://assets.leetcode.com/uploads/2020/09/02/interleave.jpg] 输入：s1 = "aabcc", s2 = "dbbca", s3 = "aadbbcbcac" 输出：true 示例 2： 输入：s1 = "aabcc", s2 = "dbbca", s3 = "aadbbbaccc" 输出：false 示例 3： 输入：s1 = "", s2 = "", s3 = "" 输出：true 提示： * 0 <= s1.length, s2.length <= 100 * 0 <= s3.length <= 200 * s1、s2、和 s3 都由小写英文字母组成 进阶：您能否仅使用 O(s2.length) 额外的内存空间来解决它?
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 动态规划，dp[i][j]表示s1的前i个字符和s2的前j个字符能否组成s3的前i+j个字符

算法步骤:
1. 如果len(s1) + len(s2) != len(s3)，返回False
2. 初始化：dp[0][0] = True
3. 初始化第一行和第一列
4. 状态转移：
   - dp[i][j] = (dp[i-1][j] and s1[i-1] == s3[i+j-1]) or 
                (dp[i][j-1] and s2[j-1] == s3[i+j-1])
5. 返回dp[m][n]

关键点:
- 可以优化空间复杂度为O(n)，使用一维数组
- 时间复杂度O(m*n)，空间复杂度O(n)
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m*n) - m和n分别为s1和s2的长度
空间复杂度: O(n) - 优化后只使用一维数组
"""

# ============================================================================
# 代码实现
# ============================================================================

from leetcode_solutions.utils.solution import create_solution


def is_interleave(s1: str, s2: str, s3: str) -> bool:
    """
    函数式接口 - 动态规划（空间优化）
    
    实现思路:
    使用动态规划判断s3是否是由s1和s2交错组成的，优化空间复杂度为O(n)。
    
    Args:
        s1: 第一个字符串
        s2: 第二个字符串
        s3: 目标字符串
        
    Returns:
        如果s3是由s1和s2交错组成的返回True，否则返回False
        
    Example:
        >>> is_interleave("aabcc", "dbbca", "aadbbcbcac")
        True
    """
    m, n = len(s1), len(s2)
    if m + n != len(s3):
        return False
    
    # 使用一维数组
    dp = [False] * (n + 1)
    dp[0] = True
    
    # 初始化第一行
    for j in range(1, n + 1):
        dp[j] = dp[j - 1] and s2[j - 1] == s3[j - 1]
    
    # 填充DP表
    for i in range(1, m + 1):
        dp[0] = dp[0] and s1[i - 1] == s3[i - 1]
        for j in range(1, n + 1):
            dp[j] = (dp[j] and s1[i - 1] == s3[i + j - 1]) or \
                    (dp[j - 1] and s2[j - 1] == s3[i + j - 1])
    
    return dp[n]


# 自动生成Solution类（无需手动编写）
Solution = create_solution(is_interleave)
