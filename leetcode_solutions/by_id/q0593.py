# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 593
标题: Valid Square
难度: medium
链接: https://leetcode.cn/problems/valid-square/
题目类型: 几何、数学
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
593. 有效的正方形 - 给定2D空间中四个点的坐标 p1, p2, p3 和 p4，如果这四个点构成一个正方形，则返回 true 。 点的坐标 pi 表示为 [xi, yi] 。 输入没有任何顺序 。 一个 有效的正方形 有四条等边和四个等角(90度角)。 示例 1: 输入: p1 = [0,0], p2 = [1,1], p3 = [1,0], p4 = [0,1] 输出: true 示例 2: 输入：p1 = [0,0], p2 = [1,1], p3 = [1,0], p4 = [0,12] 输出：false 示例 3: 输入：p1 = [1,0], p2 = [-1,0], p3 = [0,1], p4 = [0,-1] 输出：true 提示: * p1.length == p2.length == p3.length == p4.length == 2 * -104 <= xi, yi <= 104
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过计算所有点对之间的距离，并检查是否有四条等边和两条对角线相等来判断是否构成正方形。

算法步骤:
1. 计算所有点对之间的距离。
2. 将这些距离排序。
3. 检查前四个距离是否相等（即四条边）。
4. 检查后两个距离是否相等且大于前四个距离（即两条对角线）。

关键点:
- 使用欧几里得距离公式计算两点之间的距离。
- 排序后的距离数组应满足特定条件以验证正方形。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(1)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def valid_square(p1: List[int], p2: List[int], p3: List[int], p4: List[int]) -> bool:
    def dist(point1, point2):
        return (point1[0] - point2[0]) ** 2 + (point1[1] - point2[1]) ** 2

    # 计算所有点对之间的距离
    distances = [
        dist(p1, p2), dist(p1, p3), dist(p1, p4),
        dist(p2, p3), dist(p2, p4),
        dist(p3, p4)
    ]

    # 排序距离
    distances.sort()

    # 检查前四个距离是否相等（四条边）
    if distances[0] == 0 or distances[0] != distances[1] or distances[1] != distances[2] or distances[2] != distances[3]:
        return False

    # 检查后两个距离是否相等且大于前四个距离（两条对角线）
    if distances[4] != distances[5] or distances[4] != 2 * distances[0]:
        return False

    return True

Solution = create_solution(valid_square)