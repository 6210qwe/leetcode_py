# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 910
标题: Nth Magical Number
难度: hard
链接: https://leetcode.cn/problems/nth-magical-number/
题目类型: 数学、二分查找
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
878. 第 N 个神奇数字 - 一个正整数如果能被 a 或 b 整除，那么它是神奇的。 给定三个整数 n , a , b ，返回第 n 个神奇的数字。因为答案可能很大，所以返回答案 对 109 + 7 取模 后的值。 示例 1： 输入：n = 1, a = 2, b = 3 输出：2 示例 2： 输入：n = 4, a = 2, b = 3 输出：6 提示： * 1 <= n <= 109 * 2 <= a, b <= 4 * 104
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用二分查找和数学方法来找到第 n 个神奇数字。

算法步骤:
1. 计算 a 和 b 的最小公倍数 (LCM)。
2. 使用二分查找来确定第 n 个神奇数字的位置。
3. 在二分查找过程中，计算当前中间值 mid 能被 a 或 b 整除的次数。
4. 根据次数调整二分查找的范围，直到找到第 n 个神奇数字。

关键点:
- 使用二分查找来高效地找到第 n 个神奇数字。
- 计算 a 和 b 的最小公倍数以避免重复计数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(log(max(a, b) * n))
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution

def gcd(x: int, y: int) -> int:
    """计算最大公约数"""
    while y:
        x, y = y, x % y
    return x

def lcm(x: int, y: int) -> int:
    """计算最小公倍数"""
    return x * y // gcd(x, y)

def nthMagicalNumber(n: int, a: int, b: int) -> int:
    """
    函数式接口 - 返回第 n 个神奇数字
    """
    MOD = 10**9 + 7
    l, r = 1, n * min(a, b)
    
    # 二分查找
    while l < r:
        mid = (l + r) // 2
        if mid // a + mid // b - mid // lcm(a, b) < n:
            l = mid + 1
        else:
            r = mid
    
    return l % MOD

Solution = create_solution(nthMagicalNumber)