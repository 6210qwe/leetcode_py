# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 100322
标题: 查找总价格为目标值的两个商品
难度: easy
链接: https://leetcode.cn/problems/he-wei-sde-liang-ge-shu-zi-lcof/
题目类型: 数组、双指针、二分查找
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
LCR 179. 查找总价格为目标值的两个商品 - 购物车内的商品价格按照升序记录于数组 price。请在购物车中找到两个商品的价格总和刚好是 target。若存在多种情况，返回任一结果即可。 示例 1： 输入：price = [3, 9, 12, 15], target = 18 输出：[3,15] 或者 [15,3] 示例 2： 输入：price = [8, 21, 27, 34, 52, 66], target = 61 输出：[27,34] 或者 [34,27] 提示： * 1 <= price.length <= 10^5 * 1 <= price[i] <= 10^6 * 1 <= target <= 2*10^6
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用双指针方法来查找两个商品的价格总和为 target。

算法步骤:
1. 初始化两个指针，一个指向数组的起始位置（left），另一个指向数组的末尾位置（right）。
2. 计算当前 left 和 right 指针所指向元素的和。
3. 如果和等于 target，返回这两个元素。
4. 如果和小于 target，移动左指针向右。
5. 如果和大于 target，移动右指针向左。
6. 重复步骤 2-5，直到找到满足条件的两个元素或两个指针相遇。

关键点:
- 由于数组是有序的，使用双指针可以高效地找到目标值。
- 双指针方法的时间复杂度为 O(n)，空间复杂度为 O(1)。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(price: List[int], target: int) -> List[int]:
    """
    函数式接口 - 使用双指针方法查找两个商品的价格总和为 target。
    """
    left, right = 0, len(price) - 1
    while left < right:
        current_sum = price[left] + price[right]
        if current_sum == target:
            return [price[left], price[right]]
        elif current_sum < target:
            left += 1
        else:
            right -= 1
    return []


Solution = create_solution(solution_function_name)