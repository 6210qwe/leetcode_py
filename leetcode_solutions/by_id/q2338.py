# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2338
标题: Minimum Consecutive Cards to Pick Up
难度: medium
链接: https://leetcode.cn/problems/minimum-consecutive-cards-to-pick-up/
题目类型: 数组、哈希表、滑动窗口
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2260. 必须拿起的最小连续卡牌数 - 给你一个整数数组 cards ，其中 cards[i] 表示第 i 张卡牌的 值 。如果两张卡牌的值相同，则认为这一对卡牌 匹配 。 返回你必须拿起的最小连续卡牌数，以使在拿起的卡牌中有一对匹配的卡牌。如果无法得到一对匹配的卡牌，返回 -1 。 示例 1： 输入：cards = [3,4,2,3,4,7] 输出：4 解释：拿起卡牌 [3,4,2,3] 将会包含一对值为 3 的匹配卡牌。注意，拿起 [4,2,3,4] 也是最优方案。 示例 2： 输入：cards = [1,0,5,3] 输出：-1 解释：无法找出含一对匹配卡牌的一组连续卡牌。 提示： * 1 <= cards.length <= 105 * 0 <= cards[i] <= 106
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用哈希表记录每张卡牌上次出现的位置，通过遍历数组来找到最小的连续卡牌数。

算法步骤:
1. 初始化一个哈希表 `last_seen` 用于存储每张卡牌上次出现的位置。
2. 初始化变量 `min_length` 为无穷大，用于记录最小的连续卡牌数。
3. 遍历数组 `cards`：
   - 如果当前卡牌在 `last_seen` 中存在，则计算当前卡牌与上次出现位置之间的距离，并更新 `min_length`。
   - 更新 `last_seen` 中当前卡牌的位置。
4. 如果 `min_length` 仍为无穷大，说明没有找到匹配的卡牌，返回 -1；否则返回 `min_length`。

关键点:
- 使用哈希表记录每张卡牌上次出现的位置，可以快速找到匹配的卡牌。
- 通过一次遍历数组即可完成计算，时间复杂度为 O(n)。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(n)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def minimumCardPickup(cards: List[int]) -> int:
    """
    函数式接口 - 返回必须拿起的最小连续卡牌数，以使在拿起的卡牌中有一对匹配的卡牌。
    """
    last_seen = {}
    min_length = float('inf')
    
    for i, card in enumerate(cards):
        if card in last_seen:
            min_length = min(min_length, i - last_seen[card] + 1)
        last_seen[card] = i
    
    return min_length if min_length != float('inf') else -1


Solution = create_solution(minimumCardPickup)