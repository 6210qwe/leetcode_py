# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 100229
标题: Check SubTree LCCI
难度: medium
链接: https://leetcode.cn/problems/check-subtree-lcci/
题目类型: 树、深度优先搜索、二叉树、字符串匹配、哈希函数
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
面试题 04.10. 检查子树 - 检查子树。你有两棵非常大的二叉树：T1，有几万个节点；T2，有几万个节点。设计一个算法，判断 T2 是否为 T1 的子树。 如果 T1 有这么一个节点 n，其子树与 T2 一模一样，则 T2 为 T1 的子树，也就是说，从节点 n 处把树砍断，得到的树与 T2 完全相同。 注意：此题相对书上原题略有改动。 示例 1： 输入：t1 = [1, 2, 3], t2 = [2] 输出：true 示例 2： 输入：t1 = [1, null, 2, 4], t2 = [3, 2] 输出：false 提示： 1. 树的节点数目范围为 [0, 20000]。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用递归方法来检查 T2 是否是 T1 的子树。

算法步骤:
1. 如果 T1 或 T2 为空，返回 False。
2. 如果 T1 和 T2 的根节点值相同，检查 T1 的子树是否与 T2 相同。
3. 如果 T1 和 T2 的根节点值不同，递归检查 T1 的左子树和右子树是否包含 T2。

关键点:
- 递归检查子树的结构是否相同。
- 通过比较节点值来判断子树是否相同。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n)，其中 m 是 T1 的节点数，n 是 T2 的节点数。
空间复杂度: O(h1 + h2)，其中 h1 和 h2 分别是 T1 和 T2 的高度。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import Optional
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution

def is_subtree(s: Optional[TreeNode], t: Optional[TreeNode]) -> bool:
    if not s:
        return False
    if is_same_tree(s, t):
        return True
    return is_subtree(s.left, t) or is_subtree(s.right, t)

def is_same_tree(s: Optional[TreeNode], t: Optional[TreeNode]) -> bool:
    if not s and not t:
        return True
    if not s or not t:
        return False
    return s.val == t.val and is_same_tree(s.left, t.left) and is_same_tree(s.right, t.right)

Solution = create_solution(is_subtree)