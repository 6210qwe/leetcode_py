# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3105
标题: Minimum Edge Reversals So Every Node Is Reachable
难度: hard
链接: https://leetcode.cn/problems/minimum-edge-reversals-so-every-node-is-reachable/
题目类型: 深度优先搜索、广度优先搜索、图、动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2858. 可以到达每一个节点的最少边反转次数 - 给你一个 n 个点的 简单有向图 （没有重复边的有向图），节点编号为 0 到 n - 1 。如果这些边是双向边，那么这个图形成一棵 树 。 给你一个整数 n 和一个 二维 整数数组 edges ，其中 edges[i] = [ui, vi] 表示从节点 ui 到节点 vi 有一条 有向边 。 边反转 指的是将一条边的方向反转，也就是说一条从节点 ui 到节点 vi 的边会变为一条从节点 vi 到节点 ui 的边。 对于范围 [0, n - 1] 中的每一个节点 i ，你的任务是分别 独立 计算 最少 需要多少次 边反转 ，从节点 i 出发经过 一系列有向边 ，可以到达所有的节点。 请你返回一个长度为 n 的整数数组 answer ，其中 answer[i]表示从节点 i 出发，可以到达所有节点的 最少边反转 次数。 示例 1： [https://assets.leetcode.com/uploads/2023/08/26/image-20230826221104-3.png] 输入：n = 4, edges = [[2,0],[2,1],[1,3]] 输出：[1,1,0,2] 解释：上图表示了与输入对应的简单有向图。 对于节点 0 ：反转 [2,0] ，从节点 0 出发可以到达所有节点。 所以 answer[0] = 1 。 对于节点 1 ：反转 [2,1] ，从节点 1 出发可以到达所有节点。 所以 answer[1] = 1 。 对于节点 2 ：不需要反转就可以从节点 2 出发到达所有节点。 所以 answer[2] = 0 。 对于节点 3 ：反转 [1,3] 和 [2,1] ，从节点 3 出发可以到达所有节点。 所以 answer[3] = 2 。 示例 2： [https://assets.leetcode.com/uploads/2023/08/26/image-20230826225541-2.png] 输入：n = 3, edges = [[1,2],[2,0]] 输出：[2,0,1] 解释：上图表示了与输入对应的简单有向图。 对于节点 0 ：反转 [2,0] 和 [1,2] ，从节点 0 出发可以到达所有节点。 所以 answer[0] = 2 。 对于节点 1 ：不需要反转就可以从节点 2 出发到达所有节点。 所以 answer[1] = 0 。 对于节点 2 ：反转 [1,2] ，从节点 2 出发可以到达所有节点。 所以 answer[2] = 1 。 提示： * 2 <= n <= 105 * edges.length == n - 1 * edges[i].length == 2 * 0 <= ui == edges[i][0] < n * 0 <= vi == edges[i][1] < n * ui != vi * 输入保证如果边是双向边，可以得到一棵树。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用深度优先搜索 (DFS) 来计算每个节点的最小反转次数。

算法步骤:
1. 构建图的邻接表表示，并记录每条边是否需要反转。
2. 从根节点（假设为节点 0）开始进行 DFS，计算从根节点出发到达所有节点的最小反转次数。
3. 使用动态规划的思想，计算从每个节点出发到达所有节点的最小反转次数。

关键点:
- 使用邻接表存储图结构，并记录每条边的方向。
- 在 DFS 过程中，递归地计算子节点的最小反转次数，并更新父节点的结果。
- 使用记忆化搜索来避免重复计算。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(n)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def min_edge_reversals(n: int, edges: List[List[int]]) -> List[int]:
    # 构建邻接表
    graph = [[] for _ in range(n)]
    for u, v in edges:
        graph[u].append((v, 0))  # 正向边
        graph[v].append((u, 1))  # 反向边

    # 记忆化搜索
    memo = {}

    def dfs(node: int, parent: int) -> int:
        if (node, parent) in memo:
            return memo[(node, parent)]
        
        result = 0
        for neighbor, cost in graph[node]:
            if neighbor == parent:
                continue
            result += cost + dfs(neighbor, node)
        
        memo[(node, parent)] = result
        return result

    # 从根节点开始计算
    root_cost = dfs(0, -1)

    # 计算从每个节点出发的最小反转次数
    answer = [0] * n
    answer[0] = root_cost

    def dfs2(node: int, parent: int):
        for neighbor, cost in graph[node]:
            if neighbor == parent:
                continue
            answer[neighbor] = answer[node] + (1 if cost == 0 else -1)
            dfs2(neighbor, node)

    dfs2(0, -1)
    return answer

Solution = create_solution(min_edge_reversals)