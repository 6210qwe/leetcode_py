# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2878
标题: Apply Operations to Make All Array Elements Equal to Zero
难度: medium
链接: https://leetcode.cn/problems/apply-operations-to-make-all-array-elements-equal-to-zero/
题目类型: 数组、前缀和
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2772. 使数组中的所有元素都等于零 - 给你一个下标从 0 开始的整数数组 nums 和一个正整数 k 。 你可以对数组执行下述操作 任意次 ： * 从数组中选出长度为 k 的 任一 子数组，并将子数组中每个元素都 减去 1 。 如果你可以使数组中的所有元素都等于 0 ，返回 true ；否则，返回 false 。 子数组 是数组中的一个非空连续元素序列。 示例 1： 输入：nums = [2,2,3,1,1,0], k = 3 输出：true 解释：可以执行下述操作： - 选出子数组 [2,2,3] ，执行操作后，数组变为 nums = [1,1,2,1,1,0] 。 - 选出子数组 [2,1,1] ，执行操作后，数组变为 nums = [1,1,1,0,0,0] 。 - 选出子数组 [1,1,1] ，执行操作后，数组变为 nums = [0,0,0,0,0,0] 。 示例 2： 输入：nums = [1,3,1,1], k = 2 输出：false 解释：无法使数组中的所有元素等于 0 。 提示： * 1 <= k <= nums.length <= 105 * 0 <= nums[i] <= 106
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用差分数组来记录每次操作的影响，并通过前缀和来检查是否可以将所有元素变为 0。

算法步骤:
1. 初始化一个差分数组 `diff`，长度为 `n + 1`。
2. 遍历数组 `nums`，对于每个元素 `nums[i]`，在 `diff` 中记录 `+nums[i]` 和 `-nums[i]` 的位置。
3. 计算前缀和数组 `prefix_sum`，并检查 `prefix_sum` 是否全为 0。

关键点:
- 使用差分数组来记录每次操作的影响。
- 通过前缀和来检查是否可以将所有元素变为 0。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(n)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def canMakeAllZero(nums: List[int], k: int) -> bool:
    n = len(nums)
    diff = [0] * (n + 1)

    for i in range(n):
        if nums[i] > 0:
            if i + k > n:
                return False
            diff[i] += nums[i]
            diff[i + k] -= nums[i]

    prefix_sum = 0
    for i in range(n):
        prefix_sum += diff[i]
        if prefix_sum != nums[i]:
            return False

    return True

Solution = create_solution(canMakeAllZero)