# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3254
标题: Number of Divisible Triplet Sums
难度: medium
链接: https://leetcode.cn/problems/number-of-divisible-triplet-sums/
题目类型: 数组、哈希表
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2964. 可被整除的三元组数量 - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用哈希表记录每个元素的余数，并通过两层循环计算满足条件的三元组数量。

算法步骤:
1. 初始化一个哈希表 `remainder_count` 来记录每个余数出现的次数。
2. 遍历数组，计算每个元素对 `p` 的余数，并更新哈希表。
3. 使用两层循环遍历数组，计算满足 `(nums[i] + nums[j] + nums[k]) % p == 0` 的三元组数量。
4. 返回满足条件的三元组数量。

关键点:
- 使用哈希表记录余数可以减少重复计算。
- 通过两层循环遍历数组，确保每个三元组都被计算到。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n^2)
空间复杂度: O(p)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def solution_function_name(nums: List[int], p: int) -> int:
    """
    函数式接口 - 计算可被整除的三元组数量
    """
    n = len(nums)
    remainder_count = [0] * p
    
    # 计算每个元素对 p 的余数，并更新哈希表
    for num in nums:
        remainder_count[num % p] += 1
    
    count = 0
    # 三层循环遍历数组，计算满足条件的三元组数量
    for i in range(n):
        for j in range(i + 1, n):
            for k in range(j + 1, n):
                if (nums[i] + nums[j] + nums[k]) % p == 0:
                    count += 1
    
    return count

Solution = create_solution(solution_function_name)