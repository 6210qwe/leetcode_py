# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1876
标题: Map of Highest Peak
难度: medium
链接: https://leetcode.cn/problems/map-of-highest-peak/
题目类型: 广度优先搜索、数组、矩阵
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1765. 地图中的最高点 - 给你一个大小为 m x n 的整数矩阵 isWater ，它代表了一个由 陆地 和 水域 单元格组成的地图。 * 如果 isWater[i][j] == 0 ，格子 (i, j) 是一个 陆地 格子。 * 如果 isWater[i][j] == 1 ，格子 (i, j) 是一个 水域 格子。 你需要按照如下规则给每个单元格安排高度： * 每个格子的高度都必须是非负的。 * 如果一个格子是 水域 ，那么它的高度必须为 0 。 * 任意相邻的格子高度差 至多 为 1 。当两个格子在正东、南、西、北方向上相互紧挨着，就称它们为相邻的格子。（也就是说它们有一条公共边） 找到一种安排高度的方案，使得矩阵中的最高高度值 最大 。 请你返回一个大小为 m x n 的整数矩阵 height ，其中 height[i][j] 是格子 (i, j) 的高度。如果有多种解法，请返回 任意一个 。 示例 1： [https://assets.leetcode.com/uploads/2021/01/10/screenshot-2021-01-11-at-82045-am.png] 输入：isWater = [[0,1],[0,0]] 输出：[[1,0],[2,1]] 解释：上图展示了给各个格子安排的高度。 蓝色格子是水域格，绿色格子是陆地格。 示例 2： [https://assets.leetcode.com/uploads/2021/01/10/screenshot-2021-01-11-at-82050-am.png] 输入：isWater = [[0,0,1],[1,0,0],[0,0,0]] 输出：[[1,1,0],[0,1,1],[1,2,2]] 解释：所有安排方案中，最高可行高度为 2 。 任意安排方案中，只要最高高度为 2 且符合上述规则的，都为可行方案。 提示： * m == isWater.length * n == isWater[i].length * 1 <= m, n <= 1000 * isWater[i][j] 要么是 0 ，要么是 1 。 * 至少有 1 个水域格子。 注意：本题与 542 [https://leetcode.cn/problems/01-matrix/] 题相同。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用广度优先搜索（BFS）从所有水域格子开始，逐层向外扩展，确保相邻格子的高度差不超过1。

算法步骤:
1. 初始化一个队列，将所有水域格子加入队列，并将这些格子的高度设为0。
2. 使用一个二维数组 `heights` 来存储每个格子的高度，初始时所有格子的高度设为-1。
3. 从队列中取出一个格子，检查其四个方向的相邻格子：
   - 如果相邻格子未被访问过（高度为-1），则将其高度设为当前格子高度+1，并将其加入队列。
4. 重复步骤3直到队列为空。

关键点:
- 使用BFS确保从水域格子开始逐层向外扩展，保证高度差不超过1。
- 使用一个二维数组 `heights` 来存储每个格子的高度，并初始化为-1表示未访问。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n)，其中m和n分别是矩阵的行数和列数。每个格子最多会被访问一次。
空间复杂度: O(m * n)，队列和高度矩阵的空间复杂度都是O(m * n)。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List
from collections import deque

def solution_function_name(isWater: List[List[int]]) -> List[List[int]]:
    """
    函数式接口 - 使用广度优先搜索（BFS）从所有水域格子开始，逐层向外扩展，确保相邻格子的高度差不超过1。
    """
    if not isWater:
        return []

    m, n = len(isWater), len(isWater[0])
    heights = [[-1] * n for _ in range(m)]
    queue = deque()

    # 将所有水域格子加入队列，并将这些格子的高度设为0
    for i in range(m):
        for j in range(n):
            if isWater[i][j] == 1:
                heights[i][j] = 0
                queue.append((i, j))

    # 定义四个方向
    directions = [(-1, 0), (1, 0), (0, -1), (0, 1)]

    # BFS遍历
    while queue:
        x, y = queue.popleft()
        for dx, dy in directions:
            nx, ny = x + dx, y + dy
            if 0 <= nx < m and 0 <= ny < n and heights[nx][ny] == -1:
                heights[nx][ny] = heights[x][y] + 1
                queue.append((nx, ny))

    return heights

Solution = create_solution(solution_function_name)