# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1000336
标题: x 的平方根
难度: easy
链接: https://leetcode.cn/problems/jJ0w9p/
题目类型: 数学、二分查找
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
LCR 072. x 的平方根 - 给定一个非负整数 x ，计算并返回 x 的平方根，即实现 int sqrt(int x) 函数。 正数的平方根有两个，只输出其中的正数平方根。 如果平方根不是整数，输出只保留整数的部分，小数部分将被舍去。 示例 1： 输入: x = 4 输出: 2 示例 2： 输入: x = 8 输出: 2 解释: 8 的平方根是 2.82842...，由于小数部分将被舍去，所以返回 2 提示： * 0 <= x <= 231 - 1 注意：本题与主站 69 题相同： https://leetcode.cn/problems/sqrtx/ [https://leetcode.cn/problems/sqrtx/]
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用二分查找来找到 x 的平方根。

算法步骤:
1. 初始化左右指针 left 和 right，分别指向 0 和 x。
2. 进行二分查找：
   - 计算中间值 mid。
   - 检查 mid 的平方是否等于 x，如果是则返回 mid。
   - 如果 mid 的平方小于 x，则将 left 移动到 mid + 1。
   - 如果 mid 的平方大于 x，则将 right 移动到 mid - 1。
3. 返回 right，因为当 left > right 时，right 是最大的满足条件的值。

关键点:
- 使用二分查找可以有效地缩小搜索范围。
- 通过检查 mid 的平方与 x 的关系来调整左右指针。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(log x)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(x: int) -> int:
    """
    函数式接口 - 实现 x 的平方根
    """
    if x == 0 or x == 1:
        return x
    
    left, right = 0, x
    while left <= right:
        mid = (left + right) // 2
        mid_squared = mid * mid
        
        if mid_squared == x:
            return mid
        elif mid_squared < x:
            left = mid + 1
        else:
            right = mid - 1
    
    return right


Solution = create_solution(solution_function_name)