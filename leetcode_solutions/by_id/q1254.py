# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1254
标题: Deepest Leaves Sum
难度: medium
链接: https://leetcode.cn/problems/deepest-leaves-sum/
题目类型: 树、深度优先搜索、广度优先搜索、二叉树
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1302. 层数最深叶子节点的和 - 给你一棵二叉树的根节点 root ，请你返回 层数最深的叶子节点的和 。 示例 1： [https://assets.leetcode.cn/aliyun-lc-upload/uploads/2019/12/28/1483_ex1.png] 输入：root = [1,2,3,4,5,null,6,7,null,null,null,null,8] 输出：15 示例 2： 输入：root = [6,7,8,2,7,1,3,9,null,1,4,null,null,null,5] 输出：19 提示： * 树中节点数目在范围 [1, 104] 之间。 * 1 <= Node.val <= 100
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用深度优先搜索（DFS）来遍历树，并记录每个节点的深度。在遍历过程中，维护一个字典来存储每个深度的节点值之和。最后，返回最大深度的节点值之和。

算法步骤:
1. 定义一个递归函数 `dfs`，用于遍历树并记录每个节点的深度。
2. 在 `dfs` 函数中，更新当前深度的节点值之和。
3. 递归调用 `dfs` 函数遍历左子树和右子树。
4. 在主函数中调用 `dfs` 函数，并返回最大深度的节点值之和。

关键点:
- 使用字典来存储每个深度的节点值之和。
- 递归遍历树时，传递当前深度作为参数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是树中节点的数量。每个节点只被访问一次。
空间复杂度: O(h)，其中 h 是树的高度。递归调用栈的深度最多为树的高度。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import Optional
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution

def deepest_leaves_sum(root: Optional[TreeNode]) -> int:
    """
    返回层数最深叶子节点的和
    """
    depth_sum = {}

    def dfs(node: Optional[TreeNode], depth: int) -> None:
        if not node:
            return
        if depth not in depth_sum:
            depth_sum[depth] = 0
        depth_sum[depth] += node.val
        dfs(node.left, depth + 1)
        dfs(node.right, depth + 1)

    dfs(root, 0)
    max_depth = max(depth_sum.keys())
    return depth_sum[max_depth]

Solution = create_solution(deepest_leaves_sum)