# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1148
标题: Adding Two Negabinary Numbers
难度: medium
链接: https://leetcode.cn/problems/adding-two-negabinary-numbers/
题目类型: 数组、数学
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1073. 负二进制数相加 - 给出基数为 -2 的两个数 arr1 和 arr2，返回两数相加的结果。 数字以 数组形式 给出：数组由若干 0 和 1 组成，按最高有效位到最低有效位的顺序排列。例如，arr = [1,1,0,1] 表示数字 (-2)^3 + (-2)^2 + (-2)^0 = -3。数组形式 中的数字 arr 也同样不含前导零：即 arr == [0] 或 arr[0] == 1。 返回相同表示形式的 arr1 和 arr2 相加的结果。两数的表示形式为：不含前导零、由若干 0 和 1 组成的数组。 示例 1： 输入：arr1 = [1,1,1,1,1], arr2 = [1,0,1] 输出：[1,0,0,0,0] 解释：arr1 表示 11，arr2 表示 5，输出表示 16 。 示例 2： 输入：arr1 = [0], arr2 = [0] 输出：[0] 示例 3： 输入：arr1 = [0], arr2 = [1] 输出：[1] 提示： * 1 <= arr1.length, arr2.length <= 1000 * arr1[i] 和 arr2[i] 都是 0 或 1 * arr1 和 arr2 都没有前导0
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用模拟加法的方法，逐位相加并处理进位。

算法步骤:
1. 将两个数组反转，以便从最低有效位开始处理。
2. 初始化结果数组和进位变量。
3. 逐位相加，并处理进位。
4. 去掉结果数组中的前导零。
5. 反转结果数组，得到最终结果。

关键点:
- 处理负二进制的进位规则：当前位和为 2 时，进位为 -1；当前位和为 3 时，进位为 1。
- 去掉结果数组中的前导零。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(max(len(arr1), len(arr2)))，因为我们需要遍历两个数组的最大长度。
空间复杂度: O(max(len(arr1), len(arr2)))，结果数组的长度最多为输入数组的最大长度加一。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List


def add_negabinary(arr1: List[int], arr2: List[int]) -> List[int]:
    # 反转数组，方便从最低有效位开始处理
    arr1 = arr1[::-1]
    arr2 = arr2[::-1]

    carry = 0
    result = []
    i = 0

    while i < len(arr1) or i < len(arr2) or carry != 0:
        # 计算当前位的和
        sum_ = (arr1[i] if i < len(arr1) else 0) + (arr2[i] if i < len(arr2) else 0) + carry
        if sum_ >= 2:
            result.append(sum_ - 2)
            carry = -1
        elif sum_ >= 0:
            result.append(sum_)
            carry = 0
        else:
            result.append(1)
            carry = 1
        i += 1

    # 去掉结果数组中的前导零
    while len(result) > 1 and result[-1] == 0:
        result.pop()

    # 反转结果数组，得到最终结果
    return result[::-1]


Solution = create_solution(add_negabinary)