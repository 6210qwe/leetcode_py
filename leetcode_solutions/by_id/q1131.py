# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1131
标题: Count Substrings with Only One Distinct Letter
难度: easy
链接: https://leetcode.cn/problems/count-substrings-with-only-one-distinct-letter/
题目类型: 数学、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1180. 统计只含单一字母的子串 - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用滑动窗口来统计连续相同字符的子串数量。

算法步骤:
1. 初始化结果变量 `result` 为 0。
2. 使用两个指针 `start` 和 `end` 来表示当前窗口的起始和结束位置。
3. 遍历字符串，当 `s[end]` 与 `s[start]` 相同时，扩展窗口，并更新结果。
4. 当 `s[end]` 与 `s[start]` 不同时，重置窗口的起始位置 `start` 为 `end`。
5. 返回结果 `result`。

关键点:
- 使用滑动窗口来统计连续相同字符的子串数量。
- 每次遇到不同字符时，重置窗口的起始位置。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串的长度。每个字符最多被访问两次。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def count_homogenous(s: str) -> int:
    """
    函数式接口 - 统计只含单一字母的子串数量
    """
    result = 0
    start = 0
    n = len(s)
    
    for end in range(n):
        if s[end] != s[start]:
            start = end
        result += end - start + 1
    
    return result % (10**9 + 7)


Solution = create_solution(count_homogenous)