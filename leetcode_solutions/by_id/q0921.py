# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 921
标题: Spiral Matrix III
难度: medium
链接: https://leetcode.cn/problems/spiral-matrix-iii/
题目类型: 数组、矩阵、模拟
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
885. 螺旋矩阵 III - 在 rows x cols 的网格上，你从单元格 (rStart, cStart) 面朝东面开始。网格的西北角位于第一行第一列，网格的东南角位于最后一行最后一列。 你需要以顺时针按螺旋状行走，访问此网格中的每个位置。每当移动到网格的边界之外时，需要继续在网格之外行走（但稍后可能会返回到网格边界）。 最终，我们到过网格的所有 rows x cols 个空间。 按照访问顺序返回表示网格位置的坐标列表。 示例 1： [https://s3-lc-upload.s3.amazonaws.com/uploads/2018/08/24/example_1.png] 输入：rows = 1, cols = 4, rStart = 0, cStart = 0 输出：[[0,0],[0,1],[0,2],[0,3]] 示例 2： [https://s3-lc-upload.s3.amazonaws.com/uploads/2018/08/24/example_2.png] 输入：rows = 5, cols = 6, rStart = 1, cStart = 4 输出：[[1,4],[1,5],[2,5],[2,4],[2,3],[1,3],[0,3],[0,4],[0,5],[3,5],[3,4],[3,3],[3,2],[2,2],[1,2],[0,2],[4,5],[4,4],[4,3],[4,2],[4,1],[3,1],[2,1],[1,1],[0,1],[4,0],[3,0],[2,0],[1,0],[0,0]] 提示： * 1 <= rows, cols <= 100 * 0 <= rStart < rows * 0 <= cStart < cols
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用方向数组来控制移动方向，并逐步扩展步长。

算法步骤:
1. 初始化方向数组和当前方向。
2. 从起始位置开始，按照当前方向移动，并记录访问过的坐标。
3. 每次移动完一个方向后，改变方向并增加步长。
4. 当访问的坐标数量达到 rows * cols 时，结束循环。

关键点:
- 使用方向数组来控制移动方向。
- 逐步扩展步长，确保覆盖所有网格。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(rows * cols)
空间复杂度: O(rows * cols)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def spiralMatrixIII(rows: int, cols: int, rStart: int, cStart: int) -> List[List[int]]:
    """
    函数式接口 - 返回按照访问顺序表示网格位置的坐标列表
    """
    # 方向数组：右、下、左、上
    directions = [(0, 1), (1, 0), (0, -1), (-1, 0)]
    # 当前方向索引
    direction_index = 0
    # 当前步长
    step = 1
    # 访问过的坐标
    visited = []
    # 当前坐标
    r, c = rStart, cStart
    
    while len(visited) < rows * cols:
        # 在当前方向上移动 step 步
        for _ in range(step):
            if 0 <= r < rows and 0 <= c < cols:
                visited.append([r, c])
            r += directions[direction_index][0]
            c += directions[direction_index][1]
        
        # 改变方向
        direction_index = (direction_index + 1) % 4
        
        # 每两个方向后增加步长
        if direction_index % 2 == 0:
            step += 1
    
    return visited

Solution = create_solution(spiralMatrixIII)