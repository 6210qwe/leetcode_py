# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1000289
标题: 零钱兑换
难度: medium
链接: https://leetcode.cn/problems/gaM7Ch/
题目类型: 广度优先搜索、数组、动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
LCR 103. 零钱兑换 - 给定不同面额的硬币 coins 和一个总金额 amount。编写一个函数来计算可以凑成总金额所需的最少的硬币个数。如果没有任何一种硬币组合能组成总金额，返回 -1。 你可以认为每种硬币的数量是无限的。 示例 1： 输入：coins = [1, 2, 5], amount = 11 输出：3 解释：11 = 5 + 5 + 1 示例 2： 输入：coins = [2], amount = 3 输出：-1 示例 3： 输入：coins = [1], amount = 0 输出：0 示例 4： 输入：coins = [1], amount = 1 输出：1 示例 5： 输入：coins = [1], amount = 2 输出：2 提示： * 1 <= coins.length <= 12 * 1 <= coins[i] <= 231 - 1 * 0 <= amount <= 104 注意：本题与主站 322 题相同： https://leetcode.cn/problems/coin-change/ [https://leetcode.cn/problems/coin-change/]
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划来解决这个问题。我们定义一个数组 dp，其中 dp[i] 表示凑成金额 i 所需的最少硬币数。通过遍历每个金额 i，并尝试使用每种硬币来更新 dp[i] 的值，最终得到 dp[amount] 的值。

算法步骤:
1. 初始化一个大小为 amount+1 的数组 dp，所有元素初始化为 amount+1（表示一个不可能达到的大数），dp[0] 初始化为 0。
2. 遍历从 1 到 amount 的每个金额 i。
3. 对于每个金额 i，遍历每种硬币 coin，如果 coin 小于等于 i，则更新 dp[i] 为 min(dp[i], dp[i - coin] + 1)。
4. 最后检查 dp[amount] 是否仍为初始值 amount+1，如果是则返回 -1，否则返回 dp[amount]。

关键点:
- 动态规划的状态转移方程：dp[i] = min(dp[i], dp[i - coin] + 1)
- 初始化 dp 数组时，dp[0] 设为 0，其他设为 amount+1
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n * m)，其中 n 是 amount，m 是 coins 的长度。
空间复杂度: O(n)，因为我们使用了一个大小为 amount+1 的数组 dp。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List


def coin_change(coins: List[int], amount: int) -> int:
    """
    函数式接口 - 实现零钱兑换问题
    """
    # 初始化 dp 数组
    dp = [amount + 1] * (amount + 1)
    dp[0] = 0

    # 遍历每个金额 i
    for i in range(1, amount + 1):
        # 遍历每种硬币
        for coin in coins:
            if coin <= i:
                dp[i] = min(dp[i], dp[i - coin] + 1)

    # 返回结果
    return dp[amount] if dp[amount] != amount + 1 else -1


Solution = create_solution(coin_change)