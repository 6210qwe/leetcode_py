# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3418
标题: Count Pairs That Form a Complete Day II
难度: medium
链接: https://leetcode.cn/problems/count-pairs-that-form-a-complete-day-ii/
题目类型: 数组、哈希表、计数
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3185. 构成整天的下标对数目 II - 给你一个整数数组 hours，表示以 小时 为单位的时间，返回一个整数，表示满足 i < j 且 hours[i] + hours[j] 构成 整天 的下标对 i, j 的数目。 整天 定义为时间持续时间是 24 小时的 整数倍 。 例如，1 天是 24 小时，2 天是 48 小时，3 天是 72 小时，以此类推。 示例 1： 输入： hours = [12,12,30,24,24] 输出： 2 解释： 构成整天的下标对分别是 (0, 1) 和 (3, 4)。 示例 2： 输入： hours = [72,48,24,3] 输出： 3 解释： 构成整天的下标对分别是 (0, 1)、(0, 2) 和 (1, 2)。 提示： * 1 <= hours.length <= 5 * 105 * 1 <= hours[i] <= 109
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用哈希表记录每个小时数的余数出现次数，通过查找互补的余数来找到构成整天的对数。

算法步骤:
1. 初始化一个哈希表 `hour_count` 来记录每个小时数的余数出现次数。
2. 遍历 `hours` 数组，对于每个小时数 `hour`，计算其模 24 的余数 `remainder`。
3. 查找 `hour_count` 中是否存在 `24 - remainder`，如果存在则将对应的值加到结果中。
4. 更新 `hour_count` 中 `remainder` 的计数。
5. 返回结果。

关键点:
- 使用哈希表来记录每个余数的出现次数，可以快速查找互补的余数。
- 模 24 的操作确保了我们只关心 24 小时内的余数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是 `hours` 的长度。我们只需要遍历一次 `hours` 数组。
空间复杂度: O(1)，哈希表的大小最多为 24，因此空间复杂度是常数级别的。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def count_complete_pairs(hours: List[int]) -> int:
    """
    函数式接口 - 计算构成整天的下标对数目
    """
    hour_count = [0] * 24
    result = 0
    
    for hour in hours:
        remainder = hour % 24
        complement = (24 - remainder) % 24
        result += hour_count[complement]
        hour_count[remainder] += 1
    
    return result

Solution = create_solution(count_complete_pairs)