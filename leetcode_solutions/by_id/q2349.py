# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2349
标题: Check if There Is a Valid Parentheses String Path
难度: hard
链接: https://leetcode.cn/problems/check-if-there-is-a-valid-parentheses-string-path/
题目类型: 数组、动态规划、矩阵
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2267. 检查是否有合法括号字符串路径 - 一个括号字符串是一个 非空 且只包含 '(' 和 ')' 的字符串。如果下面 任意 条件为 真 ，那么这个括号字符串就是 合法的 。 * 字符串是 () 。 * 字符串可以表示为 AB（A 连接 B），A 和 B 都是合法括号序列。 * 字符串可以表示为 (A) ，其中 A 是合法括号序列。 给你一个 m x n 的括号网格图矩阵 grid 。网格图中一个 合法括号路径 是满足以下所有条件的一条路径： * 路径开始于左上角格子 (0, 0) 。 * 路径结束于右下角格子 (m - 1, n - 1) 。 * 路径每次只会向 下 或者向 右 移动。 * 路径经过的格子组成的括号字符串是 合法 的。 如果网格图中存在一条 合法括号路径 ，请返回 true ，否则返回 false 。 示例 1： [https://assets.leetcode.com/uploads/2022/03/15/example1drawio.png] 输入：grid = [["(","(","("],[")","(",")"],["(","(",")"],["(","(",")"]] 输出：true 解释：上图展示了两条路径，它们都是合法括号字符串路径。 第一条路径得到的合法字符串是 "()(())" 。 第二条路径得到的合法字符串是 "((()))" 。 注意可能有其他的合法括号字符串路径。 示例 2： [https://assets.leetcode.com/uploads/2022/03/15/example2drawio.png] 输入：grid = [[")",")"],["(","("]] 输出：false 解释：两条可行路径分别得到 "))(" 和 ")((" 。由于它们都不是合法括号字符串，我们返回 false 。 提示： * m == grid.length * n == grid[i].length * 1 <= m, n <= 100 * grid[i][j] 要么是 '(' ，要么是 ')' 。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划来检查是否存在合法括号路径。使用一个三维数组 dp[i][j][k] 表示从 (0, 0) 到 (i, j) 时，当前未匹配的左括号数量为 k 的路径是否存在。

算法步骤:
1. 初始化 dp 数组，dp[0][0][1] = True，因为起点 (0, 0) 必须有一个左括号。
2. 遍历每个格子 (i, j)，更新 dp 数组：
   - 如果 grid[i][j] 是 '(', 则 dp[i][j][k] = dp[i-1][j][k-1] or dp[i][j-1][k-1]
   - 如果 grid[i][j] 是 ')', 则 dp[i][j][k] = dp[i-1][j][k+1] or dp[i][j-1][k+1]
3. 最后检查 dp[m-1][n-1][0] 是否为 True，即在终点 (m-1, n-1) 时，未匹配的左括号数量为 0。

关键点:
- 使用三维 dp 数组来记录状态。
- 通过遍历和状态转移方程来更新 dp 数组。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n * (m + n))，其中 m 和 n 分别是 grid 的行数和列数。
空间复杂度: O(m * n * (m + n))，用于存储 dp 数组。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def hasValidPath(grid: List[List[str]]) -> bool:
    m, n = len(grid), len(grid[0])
    if (m + n - 1) % 2 != 0 or grid[0][0] == ')' or grid[m-1][n-1] == '(':
        return False
    
    max_open = (m + n - 1) // 2
    dp = [[[False] * (max_open + 1) for _ in range(n)] for _ in range(m)]
    
    dp[0][0][1] = True
    
    for i in range(m):
        for j in range(n):
            for k in range(max_open + 1):
                if i > 0 and k > 0:
                    dp[i][j][k] |= dp[i-1][j][k-1] if grid[i][j] == '(' else dp[i-1][j][k+1]
                if j > 0 and k > 0:
                    dp[i][j][k] |= dp[i][j-1][k-1] if grid[i][j] == '(' else dp[i][j-1][k+1]
    
    return dp[m-1][n-1][0]

Solution = create_solution(hasValidPath)