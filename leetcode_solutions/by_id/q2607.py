# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2607
标题: Minimum Subarrays in a Valid Split
难度: medium
链接: https://leetcode.cn/problems/minimum-subarrays-in-a-valid-split/
题目类型: 数组、数学、动态规划、数论
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2464. 有效分割中的最少子数组数目 - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用前缀和与最大公约数 (GCD) 来确定有效的分割点。

算法步骤:
1. 计算数组的前缀和数组。
2. 遍历前缀和数组，使用 GCD 来确定有效的分割点。
3. 统计最少的子数组数目。

关键点:
- 前缀和用于快速计算任意子数组的和。
- GCD 用于确保每个子数组的和是相同的。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log k)，其中 n 是数组长度，k 是数组中元素的最大值。
空间复杂度: O(n)，用于存储前缀和数组。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List
import math


def solution_function_name(nums: List[int]) -> int:
    """
    函数式接口 - 实现
    """
    # 计算前缀和数组
    prefix_sum = [0]
    for num in nums:
        prefix_sum.append(prefix_sum[-1] + num)
    
    # 计算整个数组的和
    total_sum = prefix_sum[-1]
    
    # 初始化最少子数组数目
    min_subarrays = len(nums)
    
    # 遍历前缀和数组，使用 GCD 来确定有效的分割点
    for i in range(1, len(prefix_sum)):
        if math.gcd(total_sum, prefix_sum[i]) == prefix_sum[i]:
            min_subarrays = min(min_subarrays, (len(prefix_sum) - 1) // i)
    
    return min_subarrays


Solution = create_solution(solution_function_name)