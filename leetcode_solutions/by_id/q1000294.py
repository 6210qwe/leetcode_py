# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1000294
标题: 判断二分图
难度: medium
链接: https://leetcode.cn/problems/vEAB3K/
题目类型: 深度优先搜索、广度优先搜索、并查集、图
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
LCR 106. 判断二分图 - 存在一个 无向图 ，图中有 n 个节点。其中每个节点都有一个介于 0 到 n - 1 之间的唯一编号。 给定一个二维数组 graph ，表示图，其中 graph[u] 是一个节点数组，由节点 u 的邻接节点组成。形式上，对于 graph[u] 中的每个 v ，都存在一条位于节点 u 和节点 v 之间的无向边。该无向图同时具有以下属性： * 不存在自环（graph[u] 不包含 u）。 * 不存在平行边（graph[u] 不包含重复值）。 * 如果 v 在 graph[u] 内，那么 u 也应该在 graph[v] 内（该图是无向图） * 这个图可能不是连通图，也就是说两个节点 u 和 v 之间可能不存在一条连通彼此的路径。 二分图 定义：如果能将一个图的节点集合分割成两个独立的子集 A 和 B ，并使图中的每一条边的两个节点一个来自 A 集合，一个来自 B 集合，就将这个图称为 二分图 。 如果图是二分图，返回 true ；否则，返回 false 。 示例 1： [https://assets.leetcode.com/uploads/2020/10/21/bi2.jpg] 输入：graph = [[1,2,3],[0,2],[0,1,3],[0,2]] 输出：false 解释：不能将节点分割成两个独立的子集，以使每条边都连通一个子集中的一个节点与另一个子集中的一个节点。 示例 2： [https://assets.leetcode.com/uploads/2020/10/21/bi1.jpg] 输入：graph = [[1,3],[0,2],[1,3],[0,2]] 输出：true 解释：可以将节点分成两组: {0, 2} 和 {1, 3} 。 提示： * graph.length == n * 1 <= n <= 100 * 0 <= graph[u].length < n * 0 <= graph[u][i] <= n - 1 * graph[u] 不会包含 u * graph[u] 的所有值 互不相同 * 如果 graph[u] 包含 v，那么 graph[v] 也会包含 u 注意：本题与主站 785 题相同： https://leetcode.cn/problems/is-graph-bipartite/ [https://leetcode.cn/problems/is-graph-bipartite/]
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用深度优先搜索 (DFS) 或广度优先搜索 (BFS) 对图进行染色，判断是否可以将图染成二分图。

算法步骤:
1. 初始化颜色数组 `colors`，用于记录每个节点的颜色，初始值为 0 表示未染色。
2. 遍历每个节点，如果该节点未被染色，则使用 DFS 或 BFS 进行染色。
3. 在染色过程中，如果发现相邻节点已经被染成相同的颜色，则说明图不是二分图，返回 False。
4. 如果所有节点都能成功染色，则返回 True。

关键点:
- 使用两种颜色（1 和 -1）交替染色，确保相邻节点颜色不同。
- 处理图中可能存在多个连通分量的情况。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(V + E)，其中 V 是节点数，E 是边数。每个节点和每条边最多访问一次。
空间复杂度: O(V)，用于存储颜色数组和递归调用栈。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List


def is_bipartite(graph: List[List[int]]) -> bool:
    """
    判断给定的无向图是否为二分图。
    """
    def dfs(node: int, color: int) -> bool:
        if colors[node] != 0:
            return colors[node] == color
        colors[node] = color
        for neighbor in graph[node]:
            if not dfs(neighbor, -color):
                return False
        return True

    n = len(graph)
    colors = [0] * n  # 0: 未染色, 1: 一种颜色, -1: 另一种颜色

    for i in range(n):
        if colors[i] == 0 and not dfs(i, 1):
            return False

    return True


Solution = create_solution(is_bipartite)