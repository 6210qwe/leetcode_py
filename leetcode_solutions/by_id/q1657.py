# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1657
标题: Find the Winner of an Array Game
难度: medium
链接: https://leetcode.cn/problems/find-the-winner-of-an-array-game/
题目类型: 数组、模拟
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1535. 找出数组游戏的赢家 - 给你一个由 不同 整数组成的整数数组 arr 和一个整数 k 。 每回合游戏都在数组的前两个元素（即 arr[0] 和 arr[1] ）之间进行。比较 arr[0] 与 arr[1] 的大小，较大的整数将会取得这一回合的胜利并保留在位置 0 ，较小的整数移至数组的末尾。当一个整数赢得 k 个连续回合时，游戏结束，该整数就是比赛的 赢家 。 返回赢得比赛的整数。 题目数据 保证 游戏存在赢家。 示例 1： 输入：arr = [2,1,3,5,4,6,7], k = 2 输出：5 解释：一起看一下本场游戏每回合的情况： [https://assets.leetcode.cn/aliyun-lc-upload/uploads/2020/07/30/q-example.png] 因此将进行 4 回合比赛，其中 5 是赢家，因为它连胜 2 回合。 示例 2： 输入：arr = [3,2,1], k = 10 输出：3 解释：3 将会在前 10 个回合中连续获胜。 示例 3： 输入：arr = [1,9,8,2,3,7,6,4,5], k = 7 输出：9 示例 4： 输入：arr = [1,11,22,33,44,55,66,77,88,99], k = 1000000000 输出：99 提示： * 2 <= arr.length <= 10^5 * 1 <= arr[i] <= 10^6 * arr 所含的整数 各不相同 。 * 1 <= k <= 10^9
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用双指针和计数器来模拟游戏过程，找到连续获胜 k 次的整数。

算法步骤:
1. 初始化当前最大值为 arr[0]，计数器 count 为 0。
2. 遍历数组，从第二个元素开始，比较当前最大值和下一个元素：
   - 如果当前最大值大于下一个元素，计数器加 1，并将下一个元素移到数组末尾。
   - 如果当前最大值小于下一个元素，更新当前最大值为下一个元素，重置计数器为 1。
3. 当计数器达到 k 时，返回当前最大值。
4. 如果遍历完数组仍未找到连续获胜 k 次的整数，则继续从头遍历，直到找到为止。

关键点:
- 使用双指针和计数器来模拟游戏过程，避免实际移动元素，从而提高效率。
- 通过循环遍历数组，确保找到连续获胜 k 次的整数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是数组的长度。每个元素最多被访问两次。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def find_winner(arr: List[int], k: int) -> int:
    """
    函数式接口 - 找出数组游戏的赢家
    """
    n = len(arr)
    max_val = arr[0]
    count = 0
    i = 1
    
    while True:
        if max_val > arr[i % n]:
            count += 1
        else:
            max_val = arr[i % n]
            count = 1
        
        if count == k:
            return max_val
        
        i += 1

Solution = create_solution(find_winner)