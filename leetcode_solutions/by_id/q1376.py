# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1376
标题: Selling Pieces of Wood
难度: hard
链接: https://leetcode.cn/problems/selling-pieces-of-wood/
题目类型: 记忆化搜索、数组、动态规划
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2312. 卖木头块 - 给你两个整数 m 和 n ，分别表示一块矩形木块的高和宽。同时给你一个二维整数数组 prices ，其中 prices[i] = [hi, wi, pricei] 表示你可以以 pricei 元的价格卖一块高为 hi 宽为 wi 的矩形木块。 每一次操作中，你必须按下述方式之一执行切割操作，以得到两块更小的矩形木块： * 沿垂直方向按高度 完全 切割木块，或 * 沿水平方向按宽度 完全 切割木块 在将一块木块切成若干小木块后，你可以根据 prices 卖木块。你可以卖多块同样尺寸的木块。你不需要将所有小木块都卖出去。你 不能 旋转切好后木块来交换它的高度值和宽度值。 请你返回切割一块大小为 m x n 的木块后，能得到的 最多 钱数。 注意你可以切割木块任意次。 示例 1： [https://assets.leetcode.com/uploads/2022/04/27/ex1.png] 输入：m = 3, n = 5, prices = [[1,4,2],[2,2,7],[2,1,3]] 输出：19 解释：上图展示了一个可行的方案。包括： - 2 块 2 x 2 的小木块，售出 2 * 7 = 14 元。 - 1 块 2 x 1 的小木块，售出 1 * 3 = 3 元。 - 1 块 1 x 4 的小木块，售出 1 * 2 = 2 元。 总共售出 14 + 3 + 2 = 19 元。 19 元是最多能得到的钱数。 示例 2： [https://assets.leetcode.com/uploads/2022/04/27/ex2new.png] 输入：m = 4, n = 6, prices = [[3,2,10],[1,4,2],[4,1,3]] 输出：32 解释：上图展示了一个可行的方案。包括： - 3 块 3 x 2 的小木块，售出 3 * 10 = 30 元。 - 1 块 1 x 4 的小木块，售出 1 * 2 = 2 元。 总共售出 30 + 2 = 32 元。 32 元是最多能得到的钱数。 注意我们不能旋转 1 x 4 的木块来得到 4 x 1 的木块。 提示： * 1 <= m, n <= 200 * 1 <= prices.length <= 2 * 104 * prices[i].length == 3 * 1 <= hi <= m * 1 <= wi <= n * 1 <= pricei <= 106 * 所有 (hi, wi) 互不相同 。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划和记忆化搜索来解决这个问题。通过递归地尝试每一种可能的切割方式，并记录每个子问题的最优解。

算法步骤:
1. 初始化一个二维数组 dp，其中 dp[i][j] 表示高度为 i 和宽度为 j 的木块的最大收益。
2. 遍历 prices 数组，初始化 dp 数组中对应位置的值。
3. 使用记忆化搜索递归地计算每个子问题的最优解。
4. 对于每个子问题，尝试沿垂直和水平方向进行切割，并更新 dp 数组中的值。
5. 返回 dp[m][n] 作为最终结果。

关键点:
- 使用记忆化搜索来避免重复计算。
- 递归地尝试每一种可能的切割方式，并记录每个子问题的最优解。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n * (m + n))，其中 m 和 n 分别是木块的高度和宽度。每个子问题最多需要计算 m + n 次。
空间复杂度: O(m * n)，用于存储 dp 数组。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution
from functools import lru_cache

def selling_wood(m: int, n: int, prices: List[List[int]]) -> int:
    # 初始化 dp 数组
    dp = [[0] * (n + 1) for _ in range(m + 1)]
    
    # 根据 prices 初始化 dp 数组
    for h, w, price in prices:
        dp[h][w] = max(dp[h][w], price)
    
    @lru_cache(None)
    def dfs(height: int, width: int) -> int:
        if height == 0 or width == 0:
            return 0
        
        # 尝试沿垂直方向切割
        for i in range(1, height // 2 + 1):
            dp[height][width] = max(dp[height][width], dfs(i, width) + dfs(height - i, width))
        
        # 尝试沿水平方向切割
        for j in range(1, width // 2 + 1):
            dp[height][width] = max(dp[height][width], dfs(height, j) + dfs(height, width - j))
        
        return dp[height][width]
    
    return dfs(m, n)

Solution = create_solution(selling_wood)