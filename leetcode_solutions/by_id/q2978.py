# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2978
标题: Check if Strings Can be Made Equal With Operations II
难度: medium
链接: https://leetcode.cn/problems/check-if-strings-can-be-made-equal-with-operations-ii/
题目类型: 哈希表、字符串、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2840. 判断通过操作能否让字符串相等 II - 给你两个字符串 s1 和 s2 ，两个字符串长度都为 n ，且只包含 小写 英文字母。 你可以对两个字符串中的 任意一个 执行以下操作 任意 次： * 选择两个下标 i 和 j ，满足 i < j 且 j - i 是 偶数，然后 交换 这个字符串中两个下标对应的字符。 如果你可以让字符串 s1 和 s2 相等，那么返回 true ，否则返回 false 。 示例 1： 输入：s1 = "abcdba", s2 = "cabdab" 输出：true 解释：我们可以对 s1 执行以下操作： - 选择下标 i = 0 ，j = 2 ，得到字符串 s1 = "cbadba" 。 - 选择下标 i = 2 ，j = 4 ，得到字符串 s1 = "cbbdaa" 。 - 选择下标 i = 1 ，j = 5 ，得到字符串 s1 = "cabdab" = s2 。 示例 2： 输入：s1 = "abe", s2 = "bea" 输出：false 解释：无法让两个字符串相等。 提示： * n == s1.length == s2.length * 1 <= n <= 105 * s1 和 s2 只包含小写英文字母。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 我们可以通过分别统计两个字符串中偶数位置和奇数位置的字符频率来判断它们是否可以通过交换操作变得相等。

算法步骤:
1. 分别统计 s1 和 s2 中偶数位置和奇数位置的字符频率。
2. 比较这两个频率字典，如果它们完全相同，则说明可以通过交换操作使两个字符串相等，否则不能。

关键点:
- 通过字符频率统计来简化问题。
- 交换操作只影响偶数位置和奇数位置的字符，因此可以分别处理。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串的长度。我们需要遍历字符串一次来统计字符频率。
空间复杂度: O(1)，因为字符集是固定的（26个小写字母），所以频率字典的空间是常数级别的。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def can_be_equal(s1: str, s2: str) -> bool:
    """
    函数式接口 - 判断通过操作能否让字符串相等 II
    """
    # 统计 s1 和 s2 中偶数位置和奇数位置的字符频率
    count1_even, count1_odd = [0] * 26, [0] * 26
    count2_even, count2_odd = [0] * 26, [0] * 26
    
    for i in range(len(s1)):
        if i % 2 == 0:
            count1_even[ord(s1[i]) - ord('a')] += 1
            count2_even[ord(s2[i]) - ord('a')] += 1
        else:
            count1_odd[ord(s1[i]) - ord('a')] += 1
            count2_odd[ord(s2[i]) - ord('a')] += 1
    
    # 比较两个频率字典
    return count1_even == count2_even and count1_odd == count2_odd


Solution = create_solution(can_be_equal)