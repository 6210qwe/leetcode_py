# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 777
标题: Toeplitz Matrix
难度: easy
链接: https://leetcode.cn/problems/toeplitz-matrix/
题目类型: 数组、矩阵
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
766. 托普利茨矩阵 - 给你一个 m x n 的矩阵 matrix 。如果这个矩阵是托普利茨矩阵，返回 true ；否则，返回 false 。 如果矩阵上每一条由左上到右下的对角线上的元素都相同，那么这个矩阵是 托普利茨矩阵 。 示例 1： [https://assets.leetcode.com/uploads/2020/11/04/ex1.jpg] 输入：matrix = [[1,2,3,4],[5,1,2,3],[9,5,1,2]] 输出：true 解释： 在上述矩阵中, 其对角线为: "[9]", "[5, 5]", "[1, 1, 1]", "[2, 2, 2]", "[3, 3]", "[4]"。 各条对角线上的所有元素均相同, 因此答案是 True 。 示例 2： [https://assets.leetcode.com/uploads/2020/11/04/ex2.jpg] 输入：matrix = [[1,2],[2,2]] 输出：false 解释： 对角线 "[1, 2]" 上的元素不同。 提示： * m == matrix.length * n == matrix[i].length * 1 <= m, n <= 20 * 0 <= matrix[i][j] <= 99 进阶： * 如果矩阵存储在磁盘上，并且内存有限，以至于一次最多只能将矩阵的一行加载到内存中，该怎么办？ * 如果矩阵太大，以至于一次只能将不完整的一行加载到内存中，该怎么办？
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 检查每一条从左上到右下的对角线上的元素是否相同。

算法步骤:
1. 遍历矩阵的第一行和第一列，分别作为每条对角线的起点。
2. 对于每个起点，检查该对角线上的所有元素是否相同。
3. 如果所有对角线上的元素都相同，则返回 True，否则返回 False。

关键点:
- 只需要遍历第一行和第一列，因为这些位置包含了所有对角线的起点。
- 使用双指针方法来检查对角线上的元素。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n)，其中 m 和 n 分别是矩阵的行数和列数。我们需要遍历整个矩阵来检查每条对角线。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def is_toeplitz_matrix(matrix: List[List[int]]) -> bool:
    """
    检查给定的矩阵是否为托普利茨矩阵。
    """
    if not matrix or not matrix[0]:
        return True

    rows, cols = len(matrix), len(matrix[0])

    # 检查从第一行开始的对角线
    for col in range(cols):
        i, j = 0, col
        while i < rows - 1 and j < cols - 1:
            if matrix[i][j] != matrix[i + 1][j + 1]:
                return False
            i += 1
            j += 1

    # 检查从第一列开始的对角线
    for row in range(1, rows):
        i, j = row, 0
        while i < rows - 1 and j < cols - 1:
            if matrix[i][j] != matrix[i + 1][j + 1]:
                return False
            i += 1
            j += 1

    return True

Solution = create_solution(is_toeplitz_matrix)