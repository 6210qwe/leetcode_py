# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 189
标题: Rotate Array
难度: medium
链接: https://leetcode.cn/problems/rotate-array/
题目类型: 数组、数学、双指针
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
189. 轮转数组 - 给定一个整数数组 nums，将数组中的元素向右轮转 k 个位置，其中 k 是非负数。 示例 1: 输入: nums = [1,2,3,4,5,6,7], k = 3 输出: [5,6,7,1,2,3,4] 解释: 向右轮转 1 步: [7,1,2,3,4,5,6] 向右轮转 2 步: [6,7,1,2,3,4,5] 向右轮转 3 步: [5,6,7,1,2,3,4] 示例 2: 输入：nums = [-1,-100,3,99], k = 2 输出：[3,99,-1,-100] 解释: 向右轮转 1 步: [99,-1,-100,3] 向右轮转 2 步: [3,99,-1,-100] 提示： * 1 <= nums.length <= 105 * -231 <= nums[i] <= 231 - 1 * 0 <= k <= 105 进阶： * 尽可能想出更多的解决方案，至少有 三种 不同的方法可以解决这个问题。 * 你可以使用空间复杂度为 O(1) 的 原地 算法解决这个问题吗？
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 三次反转，先反转整个数组，再反转前k个，最后反转后n-k个

算法步骤:
1. 先对k取模，因为k可能大于数组长度
2. 反转整个数组
3. 反转前k个元素
4. 反转后n-k个元素

关键点:
- 三次反转实现O(1)空间复杂度
- 时间复杂度O(n)，空间复杂度O(1)
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n) - 需要反转数组三次
空间复杂度: O(1) - 只使用常数额外空间
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List
from leetcode_solutions.utils.solution import create_solution


def rotate_array(nums: List[int], k: int) -> None:
    """
    函数式接口 - 轮转数组（原地修改）
    
    实现思路:
    三次反转，先反转整个数组，再反转前k个，最后反转后n-k个。
    
    Args:
        nums: 整数数组（原地修改）
        k: 轮转次数
        
    Returns:
        None（原地修改）
        
    Example:
        >>> nums = [1, 2, 3, 4, 5, 6, 7]
        >>> rotate_array(nums, 3)
        >>> nums
        [5, 6, 7, 1, 2, 3, 4]
    """
    n = len(nums)
    k %= n  # 处理k大于n的情况
    
    def reverse(start: int, end: int):
        while start < end:
            nums[start], nums[end] = nums[end], nums[start]
            start += 1
            end -= 1
    
    reverse(0, n - 1)
    reverse(0, k - 1)
    reverse(k, n - 1)


# 自动生成Solution类（无需手动编写）
Solution = create_solution(rotate_array)
