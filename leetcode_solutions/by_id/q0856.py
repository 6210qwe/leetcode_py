# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 856
标题: Consecutive Numbers Sum
难度: hard
链接: https://leetcode.cn/problems/consecutive-numbers-sum/
题目类型: 数学、枚举
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
829. 连续整数求和 - 给定一个正整数 n，返回 连续正整数满足所有数字之和为 n 的组数 。 示例 1: 输入: n = 5 输出: 2 解释: 5 = 2 + 3，共有两组连续整数([5],[2,3])求和后为 5。 示例 2: 输入: n = 9 输出: 3 解释: 9 = 4 + 5 = 2 + 3 + 4 示例 3: 输入: n = 15 输出: 4 解释: 15 = 8 + 7 = 4 + 5 + 6 = 1 + 2 + 3 + 4 + 5 提示: * 1 <= n <= 109
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用数学公式来计算连续整数的和，并通过枚举可能的长度来找到所有满足条件的连续整数组。

算法步骤:
1. 初始化计数器 `count` 为 0。
2. 枚举可能的连续整数序列的长度 `k`，从 1 到 sqrt(2 * n)。
3. 对于每个 `k`，检查是否存在一个起始整数 `a`，使得连续 `k` 个整数的和为 `n`。
4. 如果存在这样的 `a`，则增加计数器 `count`。
5. 返回计数器 `count`。

关键点:
- 使用公式 (k * (k - 1)) // 2 来计算前 k 个整数的和。
- 检查 (n - (k * (k - 1)) // 2) 是否能被 k 整除，以确定是否存在满足条件的起始整数 `a`。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(sqrt(n))
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def consecutive_numbers_sum(n: int) -> int:
    """
    函数式接口 - 计算连续整数和为 n 的组数
    """
    count = 0
    for k in range(1, int((2 * n) ** 0.5) + 1):
        if (n - (k * (k - 1)) // 2) % k == 0:
            count += 1
    return count


Solution = create_solution(consecutive_numbers_sum)