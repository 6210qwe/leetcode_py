# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1950
标题: Sign of the Product of an Array
难度: easy
链接: https://leetcode.cn/problems/sign-of-the-product-of-an-array/
题目类型: 数组、数学
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1822. 数组元素积的符号 - 已知函数 signFunc(x) 将会根据 x 的正负返回特定值： * 如果 x 是正数，返回 1 。 * 如果 x 是负数，返回 -1 。 * 如果 x 是等于 0 ，返回 0 。 给你一个整数数组 nums 。令 product 为数组 nums 中所有元素值的乘积。 返回 signFunc(product) 。 示例 1： 输入：nums = [-1,-2,-3,-4,3,2,1] 输出：1 解释：数组中所有值的乘积是 144 ，且 signFunc(144) = 1 示例 2： 输入：nums = [1,5,0,2,-3] 输出：0 解释：数组中所有值的乘积是 0 ，且 signFunc(0) = 0 示例 3： 输入：nums = [-1,1,-1,1,-1] 输出：-1 解释：数组中所有值的乘积是 -1 ，且 signFunc(-1) = -1 提示： * 1 <= nums.length <= 1000 * -100 <= nums[i] <= 100
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 计算数组中负数的个数和是否存在零。如果存在零，则乘积为零；否则，根据负数的个数判断乘积的符号。

算法步骤:
1. 初始化负数计数器 `neg_count` 为 0。
2. 遍历数组 `nums`：
   - 如果遇到 0，直接返回 0。
   - 如果遇到负数，增加 `neg_count`。
3. 根据 `neg_count` 的奇偶性返回结果：
   - 如果 `neg_count` 是偶数，返回 1（因为负数的个数是偶数时，乘积为正）。
   - 如果 `neg_count` 是奇数，返回 -1（因为负数的个数是奇数时，乘积为负）。

关键点:
- 无需实际计算乘积，只需关注负数的个数和是否存在零。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是数组 `nums` 的长度。我们只需要遍历数组一次。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def array_sign(nums: List[int]) -> int:
    """
    函数式接口 - 计算数组元素积的符号
    """
    neg_count = 0
    for num in nums:
        if num == 0:
            return 0
        elif num < 0:
            neg_count += 1
    
    return 1 if neg_count % 2 == 0 else -1


Solution = create_solution(array_sign)