# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 100203
标题: Binary Number to String LCCI
难度: medium
链接: https://leetcode.cn/problems/binary-number-to-string-lcci/
题目类型: 位运算、数学、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
面试题 05.02. 二进制数转字符串 - 二进制数转字符串。给定一个介于 0 和 1 之间的实数（如 0.72），类型为 double，打印它的二进制表达式。如果该数字无法精确地用 32 位以内的二进制表示，则打印“ERROR”。 示例 1： 输入：0.625 输出："0.101" 示例 2： 输入：0.1 输出："ERROR" 提示：0.1 无法被二进制准确表示 提示： * 32位包括输出中的 "0." 这两位。 * 题目保证输入用例的小数位数最多只有 6 位
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用乘2取整法将小数转换为二进制，并检查是否能在32位内完成转换。

算法步骤:
1. 初始化结果字符串为 "0."
2. 循环32次：
   - 将当前小数乘以2。
   - 如果结果大于等于1，说明当前位是1，否则是0。
   - 更新小数部分。
   - 如果小数部分变为0，提前结束循环。
3. 如果循环结束后小数部分仍不为0，返回 "ERROR"；否则返回结果字符串。

关键点:
- 通过乘2取整法逐步构建二进制表示。
- 检查是否能在32位内完成转换。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(1) - 最多进行32次循环。
空间复杂度: O(1) - 只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(num: float) -> str:
    """
    函数式接口 - 将给定的小数转换为二进制字符串。
    """
    if num <= 0 or num >= 1:
        return "ERROR"

    result = "0."
    while len(result) <= 32 and num != 0:
        num *= 2
        if num >= 1:
            result += '1'
            num -= 1
        else:
            result += '0'

    if len(result) > 32:
        return "ERROR"
    return result


Solution = create_solution(solution_function_name)