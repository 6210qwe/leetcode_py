# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 953
标题: Reverse Only Letters
难度: easy
链接: https://leetcode.cn/problems/reverse-only-letters/
题目类型: 双指针、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
917. 仅仅反转字母 - 给你一个字符串 s ，根据下述规则反转字符串： * 所有非英文字母保留在原有位置。 * 所有英文字母（小写或大写）位置反转。 返回反转后的 s 。 示例 1： 输入：s = "ab-cd" 输出："dc-ba" 示例 2： 输入：s = "a-bC-dEf-ghIj" 输出："j-Ih-gfE-dCba" 示例 3： 输入：s = "Test1ng-Leet=code-Q!" 输出："Qedo1ct-eeLg=ntse-T!" 提示 * 1 <= s.length <= 100 * s 仅由 ASCII 值在范围 [33, 122] 的字符组成 * s 不含 '\"' 或 '\\'
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用双指针方法，从字符串的两端向中间移动，交换遇到的字母。

算法步骤:
1. 初始化两个指针 left 和 right，分别指向字符串的开头和结尾。
2. 使用 while 循环，当 left 小于 right 时：
   - 如果 left 指向的字符不是字母，则左指针右移。
   - 如果 right 指向的字符不是字母，则右指针左移。
   - 如果 left 和 right 指向的字符都是字母，则交换它们，并移动两个指针。
3. 返回修改后的字符串。

关键点:
- 使用双指针可以高效地完成字母的反转，同时保持非字母字符的位置不变。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串的长度。每个字符最多被访问两次。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def reverse_only_letters(s: str) -> str:
    """
    函数式接口 - 实现反转字符串中的字母，非字母字符保持原位
    """
    s = list(s)  # 将字符串转换为列表以便修改
    left, right = 0, len(s) - 1
    
    while left < right:
        if not s[left].isalpha():
            left += 1
        elif not s[right].isalpha():
            right -= 1
        else:
            s[left], s[right] = s[right], s[left]
            left += 1
            right -= 1
    
    return ''.join(s)


Solution = create_solution(reverse_only_letters)