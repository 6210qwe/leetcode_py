# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1636
标题: Number of Substrings With Only 1s
难度: medium
链接: https://leetcode.cn/problems/number-of-substrings-with-only-1s/
题目类型: 数学、字符串
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1513. 仅含 1 的子串数 - 给你一个二进制字符串 s（仅由 '0' 和 '1' 组成的字符串）。 返回所有字符都为 1 的子字符串的数目。 由于答案可能很大，请你将它对 10^9 + 7 取模后返回。 示例 1： 输入：s = "0110111" 输出：9 解释：共有 9 个子字符串仅由 '1' 组成 "1" -> 5 次 "11" -> 3 次 "111" -> 1 次 示例 2： 输入：s = "101" 输出：2 解释：子字符串 "1" 在 s 中共出现 2 次 示例 3： 输入：s = "111111" 输出：21 解释：每个子字符串都仅由 '1' 组成 示例 4： 输入：s = "000" 输出：0 提示： * s[i] == '0' 或 s[i] == '1' * 1 <= s.length <= 10^5
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过遍历字符串，统计连续 '1' 的长度，并计算这些连续 '1' 子串的所有子串数量。

算法步骤:
1. 初始化计数器 `count` 为 0，用于记录结果。
2. 初始化变量 `current_length` 为 0，用于记录当前连续 '1' 的长度。
3. 遍历字符串 `s`：
   - 如果当前字符是 '1'，则增加 `current_length`。
   - 如果当前字符是 '0'，则根据 `current_length` 计算当前连续 '1' 子串的所有子串数量，并将其加到 `count` 中，然后重置 `current_length` 为 0。
4. 遍历结束后，再处理一次 `current_length`，以确保最后一个连续 '1' 子串也被计算在内。
5. 返回 `count` 对 10^9 + 7 取模的结果。

关键点:
- 使用公式 `(n * (n + 1)) // 2` 来计算长度为 `n` 的连续 '1' 子串的所有子串数量。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串 s 的长度。我们只需要遍历字符串一次。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(s: str) -> int:
    """
    函数式接口 - 计算所有字符都为 1 的子字符串的数目
    """
    MOD = 10**9 + 7
    count = 0
    current_length = 0

    for char in s:
        if char == '1':
            current_length += 1
        else:
            count += (current_length * (current_length + 1)) // 2
            current_length = 0

    # 处理最后一个连续 '1' 子串
    count += (current_length * (current_length + 1)) // 2

    return count % MOD


Solution = create_solution(solution_function_name)