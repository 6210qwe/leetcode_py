# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3860
标题: Resulting String After Adjacent Removals
难度: medium
链接: https://leetcode.cn/problems/resulting-string-after-adjacent-removals/
题目类型: 栈、字符串、模拟
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3561. 移除相邻字符 - 给你一个由小写英文字母组成的字符串 s。 你 必须 在字符串 s 中至少存在两个 连续 字符时，反复执行以下操作： * 移除字符串中 最左边 的一对按照字母表 连续 的相邻字符（无论是按顺序还是逆序，例如 'a' 和 'b'，或 'b' 和 'a'）。 * 将剩余字符向左移动以填补空隙。 当无法再执行任何操作时，返回最终的字符串。 注意：字母表是循环的，因此 'a' 和 'z' 也视为连续。 示例 1： 输入: s = "abc" 输出: "c" 解释: * 从字符串中移除 "ab"，剩下 "c"。 * 无法进行进一步操作。因此，所有可能移除操作后的最终字符串为 "c"。 示例 2： 输入: s = "adcb" 输出: "" 解释: * 从字符串中移除 "dc"，剩下 "ab"。 * 从字符串中移除 "ab"，剩下 ""。 * 无法进行进一步操作。因此，所有可能移除操作后的最终字符串为 ""。 示例 3： 输入: s = "zadb" 输出: "db" 解释: * 从字符串中移除 "za"，剩下 "db"。 * 无法进行进一步操作。因此，所有可能移除操作后的最终字符串为 "db"。 提示: * 1 <= s.length <= 105 * s 仅由小写英文字母组成。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用栈来处理相邻字符的移除操作。通过遍历字符串，将字符压入栈中，并在压入前检查栈顶元素是否与当前字符相邻。如果是，则弹出栈顶元素并跳过当前字符。

算法步骤:
1. 初始化一个空栈。
2. 遍历字符串中的每个字符：
   - 如果栈不为空且栈顶元素与当前字符相邻，则弹出栈顶元素并跳过当前字符。
   - 否则，将当前字符压入栈中。
3. 最后，将栈中的字符拼接成结果字符串并返回。

关键点:
- 使用栈来处理相邻字符的移除操作。
- 字母表是循环的，因此 'a' 和 'z' 也视为连续。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串的长度。每个字符最多只会被压入和弹出栈一次。
空间复杂度: O(n)，最坏情况下，栈中可能会存储所有的字符。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(s: str) -> str:
    """
    函数式接口 - 移除相邻字符
    """
    def is_adjacent(a: str, b: str) -> bool:
        return abs(ord(a) - ord(b)) == 1 or (a == 'a' and b == 'z') or (a == 'z' and b == 'a')

    stack = []
    for char in s:
        if stack and is_adjacent(stack[-1], char):
            stack.pop()
        else:
            stack.append(char)
    
    return ''.join(stack)


Solution = create_solution(solution_function_name)