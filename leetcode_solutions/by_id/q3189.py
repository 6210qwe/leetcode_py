# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3189
标题: Find Champion II
难度: medium
链接: https://leetcode.cn/problems/find-champion-ii/
题目类型: 图
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2924. 找到冠军 II - 一场比赛中共有 n 支队伍，按从 0 到 n - 1 编号。每支队伍也是 有向无环图（DAG） 上的一个节点。 给你一个整数 n 和一个下标从 0 开始、长度为 m 的二维整数数组 edges 表示这个有向无环图，其中 edges[i] = [ui, vi] 表示图中存在一条从 ui 队到 vi 队的有向边。 从 a 队到 b 队的有向边意味着 a 队比 b 队 强 ，也就是 b 队比 a 队 弱 。 在这场比赛中，如果不存在某支强于 a 队的队伍，则认为 a 队将会是 冠军 。 如果这场比赛存在 唯一 一个冠军，则返回将会成为冠军的队伍。否则，返回 -1 。 注意 * 环 是形如 a1, a2, ..., an, an+1 的一个序列，且满足：节点 a1 与节点 an+1 是同一个节点；节点 a1, a2, ..., an 互不相同；对于范围 [1, n] 中的每个 i ，均存在一条从节点 ai 到节点 ai+1 的有向边。 * 有向无环图 是不存在任何环的有向图。 示例 1： [https://assets.leetcode.com/uploads/2023/10/19/graph-3.png] 输入：n = 3, edges = [[0,1],[1,2]] 输出：0 解释：1 队比 0 队弱。2 队比 1 队弱。所以冠军是 0 队。 示例 2： [https://assets.leetcode.com/uploads/2023/10/19/graph-4.png] 输入：n = 4, edges = [[0,2],[1,3],[1,2]] 输出：-1 解释：2 队比 0 队和 1 队弱。3 队比 1 队弱。但是 1 队和 0 队之间不存在强弱对比。所以答案是 -1 。 提示： * 1 <= n <= 100 * m == edges.length * 0 <= m <= n * (n - 1) / 2 * edges[i].length == 2 * 0 <= edge[i][j] <= n - 1 * edges[i][0] != edges[i][1] * 生成的输入满足：如果 a 队比 b 队强，就不存在 b 队比 a 队强 * 生成的输入满足：如果 a 队比 b 队强，b 队比 c 队强，那么 a 队比 c 队强
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过记录每个节点的入度来找到唯一的冠军。如果某个节点的入度为0且只有一个这样的节点，则该节点为冠军。

算法步骤:
1. 初始化一个入度数组，记录每个节点的入度。
2. 遍历所有边，更新每个节点的入度。
3. 检查入度数组，找到入度为0的节点。
4. 如果入度为0的节点数量不为1，则返回-1，否则返回该节点。

关键点:
- 使用入度数组来记录每个节点的入度。
- 通过遍历入度数组来找到唯一的冠军。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n + m)，其中 n 是节点数，m 是边数。我们需要遍历所有节点和边。
空间复杂度: O(n)，用于存储入度数组。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def find_champion(n: int, edges: List[List[int]]) -> int:
    """
    函数式接口 - 找到唯一冠军
    """
    # 初始化入度数组
    in_degree = [0] * n
    
    # 更新每个节点的入度
    for u, v in edges:
        in_degree[v] += 1
    
    # 找到入度为0的节点
    champions = [i for i in range(n) if in_degree[i] == 0]
    
    # 如果有多个或没有入度为0的节点，返回-1
    if len(champions) != 1:
        return -1
    else:
        return champions[0]


Solution = create_solution(find_champion)