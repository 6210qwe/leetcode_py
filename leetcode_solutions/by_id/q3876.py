# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3876
标题: Transform Array to All Equal Elements
难度: medium
链接: https://leetcode.cn/problems/transform-array-to-all-equal-elements/
题目类型: 贪心、数组
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3576. 数组元素相等转换 - 给你一个大小为 n 的整数数组 nums，其中只包含 1 和 -1，以及一个整数 k。 你可以最多进行 k 次以下操作： * 选择一个下标 i（0 <= i < n - 1），然后将 nums[i] 和 nums[i + 1] 同时 乘以 -1。 注意：你可以在 不同 的操作中多次选择相同的下标 i。 如果在最多 k 次操作后可以使数组的所有元素相等，则返回 true；否则，返回 false。 示例 1： 输入： nums = [1,-1,1,-1,1], k = 3 输出： true 解释： 我们可以通过以下两次操作使数组的所有元素相等： * 选择下标 i = 1，将 nums[1] 和 nums[2] 同时乘以 -1。此时 nums = [1,1,-1,-1,1]。 * 选择下标 i = 2，将 nums[2] 和 nums[3] 同时乘以 -1。此时 nums = [1,1,1,1,1]。 示例 2： 输入： nums = [-1,-1,-1,1,1,1], k = 5 输出： false 解释： 在最多 5 次操作内，无法使数组的所有元素相等。 提示： * 1 <= n == nums.length <= 105 * nums[i] 的值为 -1 或 1。 * 1 <= k <= n
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过计算数组中 -1 的数量来判断是否可以通过 k 次操作使所有元素相等。

算法步骤:
1. 计算数组中 -1 的数量。
2. 如果 -1 的数量是偶数，则可以通过 k 次操作使所有元素相等。
3. 如果 -1 的数量是奇数，则需要检查 k 是否足够大，使得可以通过 k 次操作使所有元素相等。

关键点:
- 通过计算 -1 的数量来判断是否可以通过 k 次操作使所有元素相等。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是数组的长度，因为我们需要遍历整个数组来计算 -1 的数量。
空间复杂度: O(1)，因为我们只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def can_transform_to_equal(nums: List[int], k: int) -> bool:
    """
    判断是否可以通过 k 次操作使数组的所有元素相等。
    """
    # 计算数组中 -1 的数量
    count_neg_ones = sum(1 for num in nums if num == -1)
    
    # 如果 -1 的数量是偶数，则可以通过 k 次操作使所有元素相等
    if count_neg_ones % 2 == 0:
        return True
    
    # 如果 -1 的数量是奇数，则需要检查 k 是否足够大
    # 每次操作可以减少两个 -1，因此需要 (count_neg_ones - 1) // 2 次操作
    required_operations = (count_neg_ones - 1) // 2
    return required_operations <= k


Solution = create_solution(can_transform_to_equal)