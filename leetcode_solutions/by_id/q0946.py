# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 946
标题: Smallest Range II
难度: medium
链接: https://leetcode.cn/problems/smallest-range-ii/
题目类型: 贪心、数组、数学、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
910. 最小差值 II - 给你一个整数数组 nums，和一个整数 k 。 对于每个下标 i（0 <= i < nums.length），将 nums[i] 变成 nums[i] + k 或 nums[i] - k 。 nums 的 分数 是 nums 中最大元素和最小元素的差值。 在更改每个下标对应的值之后，返回 nums 的最小 分数 。 示例 1： 输入：nums = [1], k = 0 输出：0 解释：分数 = max(nums) - min(nums) = 1 - 1 = 0 。 示例 2： 输入：nums = [0,10], k = 2 输出：6 解释：将数组变为 [2, 8] 。分数 = max(nums) - min(nums) = 8 - 2 = 6 。 示例 3： 输入：nums = [1,3,6], k = 3 输出：3 解释：将数组变为 [4, 6, 3] 。分数 = max(nums) - min(nums) = 6 - 3 = 3 。 提示： * 1 <= nums.length <= 104 * 0 <= nums[i] <= 104 * 0 <= k <= 104
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过贪心算法找到最优解。首先对数组进行排序，然后遍历数组，尝试将前半部分元素减去 k，后半部分元素加上 k，计算每种情况下的最大值和最小值的差值，取最小的差值。

算法步骤:
1. 对数组进行排序。
2. 初始化最小差值为原数组的最大值和最小值之差。
3. 遍历数组，对于每个位置 i，假设前 i 个元素都减去 k，后 (n-i) 个元素都加上 k，计算当前的最大值和最小值，并更新最小差值。

关键点:
- 通过排序和贪心策略，可以有效地找到最小的差值。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log n)，其中 n 是数组的长度。主要的时间开销在于排序操作。
空间复杂度: O(1)，只需要常数级别的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def smallest_range_ii(nums: List[int], k: int) -> int:
    """
    函数式接口 - 实现最小差值 II
    """
    # 对数组进行排序
    nums.sort()
    
    # 初始化最小差值为原数组的最大值和最小值之差
    min_diff = nums[-1] - nums[0]
    
    # 遍历数组，尝试将前半部分元素减去 k，后半部分元素加上 k
    for i in range(len(nums) - 1):
        # 计算当前的最大值和最小值
        max_val = max(nums[-1] - k, nums[i] + k)
        min_val = min(nums[0] + k, nums[i + 1] - k)
        
        # 更新最小差值
        min_diff = min(min_diff, max_val - min_val)
    
    return min_diff


Solution = create_solution(smallest_range_ii)