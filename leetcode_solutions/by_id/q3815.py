# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3815
标题: Sum of Largest Prime Substrings
难度: medium
链接: https://leetcode.cn/problems/sum-of-largest-prime-substrings/
题目类型: 哈希表、数学、字符串、数论、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3556. 最大质数子字符串之和 - 给定一个字符串 s，找出可以由其 子字符串 组成的 3个最大的不同质数 的和。 返回这些质数的 总和 ，如果少于 3 个不同的质数，则返回 所有 不同质数的和。 质数是大于 1 且只有两个因数的自然数：1和它本身。 子字符串 是字符串中的一个连续字符序列。 注意：每个质数即使出现在 多个 子字符串中，也只能计算 一次 。此外，将子字符串转换为整数时，忽略任何前导零。 示例 1： 输入： s = "12234" 输出： 1469 解释： * 由 "12234" 的子字符串形成的不同质数为 2 ，3 ，23 ，223 和 1223。 * 最大的 3 个质数是 1223、223 和 23。它们的和是 1469。 示例 2： 输入： s = "111" 输出： 11 解释： * 由 "111" 的子字符串形成的不同质数是 11。 * 由于只有一个质数，所以结果是 11。 提示： * 1 <= s.length <= 10 * s 仅由数字组成。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用滑动窗口生成所有可能的子字符串，并检查每个子字符串是否为质数。使用一个集合来存储所有不同的质数，最后返回最大的三个质数之和。

算法步骤:
1. 定义一个函数 `is_prime` 来检查一个数是否为质数。
2. 使用滑动窗口生成所有可能的子字符串，并将其转换为整数。
3. 检查每个整数是否为质数，并将其添加到集合中。
4. 将集合中的质数排序，取最大的三个质数并求和。

关键点:
- 使用滑动窗口生成所有可能的子字符串。
- 使用集合来存储不同的质数，避免重复。
- 对集合中的质数进行排序，取最大的三个质数。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n^3 * log(n))，其中 n 是字符串的长度。生成所有子字符串的时间复杂度为 O(n^2)，检查每个子字符串是否为质数的时间复杂度为 O(√m)，其中 m 是子字符串的整数值。总的质数检查次数为 O(n^3)。
空间复杂度: O(n^2)，用于存储所有不同的质数。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import Set, List

def is_prime(num: int) -> bool:
    if num < 2:
        return False
    for i in range(2, int(num ** 0.5) + 1):
        if num % i == 0:
            return False
    return True

def solution_function_name(s: str) -> int:
    """
    函数式接口 - 计算给定字符串 s 中最大的三个不同质数的和
    """
    primes: Set[int] = set()
    
    # 生成所有可能的子字符串
    for i in range(len(s)):
        for j in range(i + 1, len(s) + 1):
            substring = s[i:j]
            if substring[0] != '0':  # 忽略前导零
                num = int(substring)
                if is_prime(num):
                    primes.add(num)
    
    # 取最大的三个质数
    largest_primes = sorted(primes, reverse=True)[:3]
    
    # 返回质数之和
    return sum(largest_primes)

Solution = create_solution(solution_function_name)