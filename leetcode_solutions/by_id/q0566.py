# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 566
标题: Reshape the Matrix
难度: easy
链接: https://leetcode.cn/problems/reshape-the-matrix/
题目类型: 数组、矩阵、模拟
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
566. 重塑矩阵 - 在 MATLAB 中，有一个非常有用的函数 reshape ，它可以将一个 m x n 矩阵重塑为另一个大小不同（r x c）的新矩阵，但保留其原始数据。 给你一个由二维数组 mat 表示的 m x n 矩阵，以及两个正整数 r 和 c ，分别表示想要的重构的矩阵的行数和列数。 重构后的矩阵需要将原始矩阵的所有元素以相同的 行遍历顺序 填充。 如果具有给定参数的 reshape 操作是可行且合理的，则输出新的重塑矩阵；否则，输出原始矩阵。 示例 1： [https://assets.leetcode.com/uploads/2021/04/24/reshape1-grid.jpg] 输入：mat = [[1,2],[3,4]], r = 1, c = 4 输出：[[1,2,3,4]] 示例 2： [https://assets.leetcode.com/uploads/2021/04/24/reshape2-grid.jpg] 输入：mat = [[1,2],[3,4]], r = 2, c = 4 输出：[[1,2],[3,4]] 提示： * m == mat.length * n == mat[i].length * 1 <= m, n <= 100 * -1000 <= mat[i][j] <= 1000 * 1 <= r, c <= 300
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 将原矩阵展平成一维数组，然后按新矩阵的形状重新排列。

算法步骤:
1. 检查重塑是否可行，即原矩阵的元素总数是否等于新矩阵的元素总数。
2. 如果可行，将原矩阵展平成一维数组。
3. 按新矩阵的形状重新排列一维数组中的元素。
4. 返回重塑后的矩阵。

关键点:
- 使用列表推导式来展平和重新排列矩阵。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n)，其中 m 和 n 分别是原矩阵的行数和列数。
空间复杂度: O(m * n)，因为需要存储展平后的一维数组。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def matrix_reshape(mat: List[List[int]], r: int, c: int) -> List[List[int]]:
    """
    重塑矩阵 - 将一个 m x n 矩阵重塑为另一个大小不同（r x c）的新矩阵，但保留其原始数据。
    """
    m, n = len(mat), len(mat[0])
    
    # 检查重塑是否可行
    if m * n != r * c:
        return mat
    
    # 展平原矩阵
    flat_list = [element for row in mat for element in row]
    
    # 重新排列成新矩阵
    reshaped_matrix = [flat_list[i * c:(i + 1) * c] for i in range(r)]
    
    return reshaped_matrix

Solution = create_solution(matrix_reshape)