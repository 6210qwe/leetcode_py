# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2702
标题: Maximum Number of Integers to Choose From a Range II
难度: medium
链接: https://leetcode.cn/problems/maximum-number-of-integers-to-choose-from-a-range-ii/
题目类型: 贪心、数组、二分查找、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2557. 从一个范围内选择最多整数 II - 备战技术面试？力扣提供海量技术面试资源，帮助你高效提升编程技能,轻松拿下世界 IT 名企 Dream Offer。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用贪心算法和滑动窗口来解决这个问题。我们可以通过维护一个滑动窗口来找到满足条件的最大子数组长度。

算法步骤:
1. 将区间按左端点排序。
2. 使用滑动窗口来遍历区间，维护当前窗口内的最小右端点。
3. 对于每个区间，更新窗口的右边界，并计算当前窗口的长度。
4. 记录最大窗口长度作为结果。

关键点:
- 通过排序和滑动窗口来高效地找到满足条件的最大子数组长度。
- 维护窗口的最小右端点来确保窗口内的所有区间都满足条件。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n log n)，其中 n 是区间的数量。排序操作的时间复杂度为 O(n log n)，滑动窗口的遍历操作为 O(n)。
空间复杂度: O(1)，只使用了常数级的额外空间。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def max_count(nums1: List[int], nums2: List[int], k: int) -> int:
    """
    函数式接口 - 实现最优解法
    """
    # 合并两个数组并去重
    nums = list(set(nums1 + nums2))
    
    # 按左端点排序
    nums.sort()
    
    # 初始化滑动窗口
    left = 0
    right = 0
    min_right = 0
    max_length = 0
    
    while right < len(nums):
        # 更新窗口的最小右端点
        min_right = min(min_right, nums[right])
        
        # 检查当前窗口是否满足条件
        if nums[right] - min_right > k:
            # 不满足条件，移动左边界
            left += 1
            min_right = min(nums[left:right+1])
        
        # 更新最大窗口长度
        max_length = max(max_length, right - left + 1)
        
        # 移动右边界
        right += 1
    
    return max_length

Solution = create_solution(max_count)