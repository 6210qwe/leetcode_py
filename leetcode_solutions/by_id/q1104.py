# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 1104
标题: Coloring A Border
难度: medium
链接: https://leetcode.cn/problems/coloring-a-border/
题目类型: 深度优先搜索、广度优先搜索、数组、矩阵
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1034. 边界着色 - 给你一个大小为 m x n 的整数矩阵 grid ，表示一个网格。另给你三个整数 row、col 和 color 。网格中的每个值表示该位置处的网格块的颜色。 如果两个方块在任意 4 个方向上相邻，则称它们 相邻 。 如果两个方块具有相同的颜色且相邻，它们则属于同一个 连通分量 。 连通分量的边界 是指连通分量中满足下述条件之一的所有网格块： * 在上、下、左、右任意一个方向上与不属于同一连通分量的网格块相邻 * 在网格的边界上（第一行/列或最后一行/列） 请你使用指定颜色 color 为所有包含网格块 grid[row][col] 的 连通分量的边界 进行着色。 并返回最终的网格 grid 。 示例 1： 输入：grid = [[1,1],[1,2]], row = 0, col = 0, color = 3 输出：[[3,3],[3,2]] 示例 2： 输入：grid = [[1,2,2],[2,3,2]], row = 0, col = 1, color = 3 输出：[[1,3,3],[2,3,3]] 示例 3： 输入：grid = [[1,1,1],[1,1,1],[1,1,1]], row = 1, col = 1, color = 2 输出：[[2,2,2],[2,1,2],[2,2,2]] 提示： * m == grid.length * n == grid[i].length * 1 <= m, n <= 50 * 1 <= grid[i][j], color <= 1000 * 0 <= row < m * 0 <= col < n
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用深度优先搜索 (DFS) 来找到连通分量，并标记边界的网格块。

算法步骤:
1. 初始化一个集合 `visited` 来记录访问过的节点。
2. 定义一个递归函数 `dfs`，用于遍历连通分量并标记边界。
3. 在 `dfs` 函数中，检查当前节点是否在边界上或相邻节点颜色不同，如果是则标记为边界。
4. 从给定的起始点开始调用 `dfs` 函数。
5. 最后，将所有标记为边界的节点着色为指定颜色。

关键点:
- 使用 DFS 遍历连通分量。
- 标记边界节点。
- 重新着色边界节点。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(m * n)，其中 m 和 n 分别是网格的行数和列数。每个节点最多访问一次。
空间复杂度: O(m * n)，递归调用栈的深度最多为 m * n。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def colorBorder(grid: List[List[int]], row: int, col: int, color: int) -> List[List[int]]:
    if not grid or not grid[0]:
        return grid

    m, n = len(grid), len(grid[0])
    original_color = grid[row][col]
    visited = set()
    borders = []

    def dfs(x, y):
        if (x, y) in visited:
            return
        visited.add((x, y))
        is_border = False

        for dx, dy in [(-1, 0), (1, 0), (0, -1), (0, 1)]:
            nx, ny = x + dx, y + dy
            if 0 <= nx < m and 0 <= ny < n:
                if grid[nx][ny] != original_color:
                    is_border = True
                else:
                    dfs(nx, ny)
            else:
                is_border = True

        if is_border:
            borders.append((x, y))

    dfs(row, col)

    for x, y in borders:
        grid[x][y] = color

    return grid

Solution = create_solution(colorBorder)