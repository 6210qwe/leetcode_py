# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2053
标题: Check if All Characters Have Equal Number of Occurrences
难度: easy
链接: https://leetcode.cn/problems/check-if-all-characters-have-equal-number-of-occurrences/
题目类型: 哈希表、字符串、计数
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
1941. 检查是否所有字符出现次数相同 - 给你一个字符串 s ，如果 s 是一个 好 字符串，请你返回 true ，否则请返回 false 。 如果 s 中出现过的 所有 字符的出现次数 相同 ，那么我们称字符串 s 是 好 字符串。 示例 1： 输入：s = "abacbc" 输出：true 解释：s 中出现过的字符为 'a'，'b' 和 'c' 。s 中所有字符均出现 2 次。 示例 2： 输入：s = "aaabb" 输出：false 解释：s 中出现过的字符为 'a' 和 'b' 。 'a' 出现了 3 次，'b' 出现了 2 次，两者出现次数不同。 提示： * 1 <= s.length <= 1000 * s 只包含小写英文字母。
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用哈希表统计每个字符的出现次数，然后检查这些次数是否都相同。

算法步骤:
1. 使用 collections.Counter 统计字符串中每个字符的出现次数。
2. 将所有字符的出现次数存储在一个集合中。
3. 如果集合的大小为 1，则说明所有字符的出现次数相同，返回 True；否则返回 False。

关键点:
- 使用 Counter 统计字符出现次数。
- 使用集合来检查所有字符的出现次数是否相同。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)，其中 n 是字符串的长度。我们需要遍历字符串一次来统计字符出现次数。
空间复杂度: O(1)，因为字符集是固定的（只有 26 个小写字母），所以使用的额外空间是常数级别的。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution
from collections import Counter


def are_occurrences_equal(s: str) -> bool:
    """
    函数式接口 - 检查字符串中的所有字符是否出现次数相同
    """
    # 使用 Counter 统计每个字符的出现次数
    char_count = Counter(s)
    
    # 将所有字符的出现次数存储在一个集合中
    occurrences = set(char_count.values())
    
    # 如果集合的大小为 1，则说明所有字符的出现次数相同
    return len(occurrences) == 1


Solution = create_solution(are_occurrences_equal)