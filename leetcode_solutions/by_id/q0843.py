# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 843
标题: Binary Trees With Factors
难度: medium
链接: https://leetcode.cn/problems/binary-trees-with-factors/
题目类型: 数组、哈希表、动态规划、排序
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
823. 带因子的二叉树 - 给出一个含有不重复整数元素的数组 arr ，每个整数 arr[i] 均大于 1。 用这些整数来构建二叉树，每个整数可以使用任意次数。其中：每个非叶结点的值应等于它的两个子结点的值的乘积。 满足条件的二叉树一共有多少个？答案可能很大，返回 对 109 + 7 取余 的结果。 示例 1: 输入: arr = [2, 4] 输出: 3 解释: 可以得到这些二叉树: [2], [4], [4, 2, 2] 示例 2: 输入: arr = [2, 4, 5, 10] 输出: 7 解释: 可以得到这些二叉树: [2], [4], [5], [10], [4, 2, 2], [10, 2, 5], [10, 5, 2]. 提示： * 1 <= arr.length <= 1000 * 2 <= arr[i] <= 109 * arr 中的所有值 互不相同
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用动态规划和哈希表来记录每个数字可以构成的二叉树数量。

算法步骤:
1. 将数组排序，以便后续查找。
2. 使用一个哈希表 `dp` 来记录每个数字可以构成的二叉树数量。
3. 遍历数组中的每个数字，对于每个数字 `arr[i]`，检查是否存在两个数字 `a` 和 `b` 使得 `a * b == arr[i]`。
4. 如果存在这样的 `a` 和 `b`，则更新 `dp[arr[i]]` 的值。
5. 最后，返回 `dp` 中所有值的总和对 10^9 + 7 取余的结果。

关键点:
- 使用哈希表来快速查找因子。
- 动态规划的思想，逐步构建解。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n^2) - 其中 n 是数组的长度，因为我们需要遍历数组并进行两两比较。
空间复杂度: O(n) - 使用了一个哈希表来存储每个数字的二叉树数量。
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(arr: List[int]) -> int:
    """
    函数式接口 - 计算给定数组可以构成的满足条件的二叉树数量。
    """
    MOD = 10**9 + 7
    arr.sort()
    dp = {x: 1 for x in arr}  # 每个数字至少可以单独构成一棵树
    num_set = set(arr)

    for i in range(len(arr)):
        for j in range(i):
            if arr[i] % arr[j] == 0 and (arr[i] // arr[j]) in num_set:
                dp[arr[i]] += dp[arr[j]] * dp[arr[i] // arr[j]]
                dp[arr[i]] %= MOD

    return sum(dp.values()) % MOD


Solution = create_solution(solution_function_name)