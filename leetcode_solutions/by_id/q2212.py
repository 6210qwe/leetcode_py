# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 2212
标题: Removing Minimum and Maximum From Array
难度: medium
链接: https://leetcode.cn/problems/removing-minimum-and-maximum-from-array/
题目类型: 贪心、数组
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
2091. 从数组中移除最大值和最小值 - 给你一个下标从 0 开始的数组 nums ，数组由若干 互不相同 的整数组成。 nums 中有一个值最小的元素和一个值最大的元素。分别称为 最小值 和 最大值 。你的目标是从数组中移除这两个元素。 一次 删除 操作定义为从数组的 前面 移除一个元素或从数组的 后面 移除一个元素。 返回将数组中最小值和最大值 都 移除需要的最小删除次数。 示例 1： 输入：nums = [2,10,7,5,4,1,8,6] 输出：5 解释： 数组中的最小元素是 nums[5] ，值为 1 。 数组中的最大元素是 nums[1] ，值为 10 。 将最大值和最小值都移除需要从数组前面移除 2 个元素，从数组后面移除 3 个元素。 结果是 2 + 3 = 5 ，这是所有可能情况中的最小删除次数。 示例 2： 输入：nums = [0,-4,19,1,8,-2,-3,5] 输出：3 解释： 数组中的最小元素是 nums[1] ，值为 -4 。 数组中的最大元素是 nums[2] ，值为 19 。 将最大值和最小值都移除需要从数组前面移除 3 个元素。 结果是 3 ，这是所有可能情况中的最小删除次数。 示例 3： 输入：nums = [101] 输出：1 解释： 数组中只有这一个元素，那么它既是数组中的最小值又是数组中的最大值。 移除它只需要 1 次删除操作。 提示： * 1 <= nums.length <= 105 * -105 <= nums[i] <= 105 * nums 中的整数 互不相同
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 找到最小值和最大值的位置，然后计算从数组前面和后面移除这些元素所需的最小删除次数。

算法步骤:
1. 找到数组中的最小值和最大值及其对应的索引。
2. 计算从数组前面移除最小值和最大值所需的删除次数。
3. 计算从数组后面移除最小值和最大值所需的删除次数。
4. 取上述两种情况中的最小值作为结果。

关键点:
- 通过比较不同移除方式的删除次数来找到最优解。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List

def min_deletions_to_remove_min_max(nums: List[int]) -> int:
    """
    函数式接口 - 从数组中移除最大值和最小值所需的最小删除次数
    """
    if not nums:
        return 0
    
    min_index = max_index = 0
    for i in range(len(nums)):
        if nums[i] < nums[min_index]:
            min_index = i
        elif nums[i] > nums[max_index]:
            max_index = i
    
    # 确保 min_index 是较小的索引
    if min_index > max_index:
        min_index, max_index = max_index, min_index
    
    # 从前面移除
    front_deletions = max_index + 1
    # 从后面移除
    back_deletions = len(nums) - min_index
    # 从两边移除
    both_sides_deletions = (min_index + 1) + (len(nums) - max_index)
    
    return min(front_deletions, back_deletions, both_sides_deletions)

Solution = create_solution(min_deletions_to_remove_min_max)