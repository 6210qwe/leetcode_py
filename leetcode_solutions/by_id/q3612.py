# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 3612
标题: Adjacent Increasing Subarrays Detection I
难度: easy
链接: https://leetcode.cn/problems/adjacent-increasing-subarrays-detection-i/
题目类型: 数组
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
3349. 检测相邻递增子数组 I - 给你一个由 n 个整数组成的数组 nums 和一个整数 k，请你确定是否存在 两个 相邻 且长度为 k 的 严格递增 子数组。具体来说，需要检查是否存在从下标 a 和 b (a < b) 开始的 两个 子数组，并满足下述全部条件： * 这两个子数组 nums[a..a + k - 1] 和 nums[b..b + k - 1] 都是 严格递增 的。 * 这两个子数组必须是 相邻的，即 b = a + k。 如果可以找到这样的 两个 子数组，请返回 true；否则返回 false。 子数组 是数组中的一个连续 非空 的元素序列。 示例 1： 输入：nums = [2,5,7,8,9,2,3,4,3,1], k = 3 输出：true 解释： * 从下标 2 开始的子数组为 [7, 8, 9]，它是严格递增的。 * 从下标 5 开始的子数组为 [2, 3, 4]，它也是严格递增的。 * 两个子数组是相邻的，因此结果为 true。 示例 2： 输入：nums = [1,2,3,4,4,4,4,5,6,7], k = 5 输出：false 提示： * 2 <= nums.length <= 100 * 1 <= 2 * k <= nums.length * -1000 <= nums[i] <= 1000
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 通过一次遍历检查每个长度为 k 的子数组是否严格递增，并检查相邻的子数组是否也严格递增。

算法步骤:
1. 遍历数组，检查每个长度为 k 的子数组是否严格递增。
2. 如果当前子数组严格递增，检查下一个长度为 k 的子数组是否也严格递增。
3. 如果找到符合条件的相邻子数组，返回 True。
4. 如果遍历完数组没有找到符合条件的相邻子数组，返回 False。

关键点:
- 通过一次遍历和简单的比较操作来实现 O(n) 的时间复杂度。
- 使用常数空间来存储中间结果，实现 O(1) 的空间复杂度。
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n)
空间复杂度: O(1)
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List, Optional
from leetcode_solutions.utils.linked_list import ListNode
from leetcode_solutions.utils.tree import TreeNode
from leetcode_solutions.utils.solution import create_solution


def solution_function_name(nums: List[int], k: int) -> bool:
    """
    函数式接口 - 检查是否存在两个相邻且长度为 k 的严格递增子数组
    """
    n = len(nums)
    
    def is_strictly_increasing(subarray: List[int]) -> bool:
        """检查子数组是否严格递增"""
        return all(subarray[i] < subarray[i + 1] for i in range(len(subarray) - 1))
    
    for i in range(n - 2 * k + 1):
        if is_strictly_increasing(nums[i:i + k]) and is_strictly_increasing(nums[i + k:i + 2 * k]):
            return True
    
    return False


Solution = create_solution(solution_function_name)