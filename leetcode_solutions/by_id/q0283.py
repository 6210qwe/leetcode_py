# -*- coding:utf-8 -*-
# ============================================================================
# 题目信息
# ============================================================================
"""
题号: 283
标题: Move Zeroes
难度: easy
链接: https://leetcode.cn/problems/move-zeroes/
题目类型: 数组、双指针
"""

# ============================================================================
# 问题描述
# ============================================================================
"""
283. 移动零 - 给定一个数组 nums，编写一个函数将所有 0 移动到数组的末尾，同时保持非零元素的相对顺序。 请注意 ，必须在不复制数组的情况下原地对数组进行操作。 示例 1: 输入: nums = [0,1,0,3,12] 输出: [1,3,12,0,0] 示例 2: 输入: nums = [0] 输出: [0] 提示: * 1 <= nums.length <= 104 * -231 <= nums[i] <= 231 - 1 进阶：你能尽量减少完成的操作次数吗？
"""

# ============================================================================
# 实现思路
# ============================================================================
"""
核心思想: 使用双指针，一个指针遍历数组，另一个指针指向下一个非零元素应该放置的位置

算法步骤:
1. 使用两个指针，left指向下一个非零元素应该放置的位置
2. 遍历数组，如果遇到非零元素，将其与left位置的元素交换
3. 最后将left之后的所有元素设为0（或直接交换即可）

关键点:
- 使用双指针，一次遍历完成
- 时间复杂度O(n)，空间复杂度O(1)
"""

# ============================================================================
# 复杂度分析
# ============================================================================
"""
时间复杂度: O(n) - 需要遍历数组一次
空间复杂度: O(1) - 只使用常数额外空间
"""

# ============================================================================
# 代码实现
# ============================================================================

from typing import List
from leetcode_solutions.utils.solution import create_solution


def move_zeroes(nums: List[int]) -> None:
    """
    函数式接口 - 移动零
    
    实现思路:
    使用双指针，一个指针遍历数组，另一个指针指向下一个非零元素应该放置的位置。
    
    Args:
        nums: 整数数组（原地修改）
        
    Returns:
        None（直接修改数组）
        
    Example:
        >>> nums = [0,1,0,3,12]
        >>> move_zeroes(nums)
        >>> nums
        [1, 3, 12, 0, 0]
    """
    left = 0
    for right in range(len(nums)):
        if nums[right] != 0:
            nums[left], nums[right] = nums[right], nums[left]
            left += 1


# 自动生成Solution类（无需手动编写）
Solution = create_solution(move_zeroes)
